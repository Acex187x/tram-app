// Full-screen "about this tram model" reference route (registered in _layout as
// model-info/[id]). The screen itself is a native SwiftUI grouped-inset list —
// see src/components/modelinfo/ModelInfoScreen.tsx. Navigate with
// router.push('/model-info/' + modelId).
export { default } from '@/components/modelinfo/ModelInfoScreen';
