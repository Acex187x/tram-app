# Calibration analysis — 2026-07-13 (MONDAY — first weekday!)

Continues [analysis-2026-07-12.md](analysis-2026-07-12.md) (Sunday: rounds 0–23, full
résumé at its end). Record schema v2 (see plan.md). Sessions: sim-2026-07-13.jsonl
(rolled over automatically at midnight).

## Monday priorities (carried over + new)

1. **WEEKDAY PEAKS — the headline test of the whole TOD hypothesis.** Sunday measured all
   TOD buckets = 1.0, but Sunday peaks are weak by nature. Monday h7–h9 and h15–h18 with
   real commuter load + car traffic are the first true test: run the full double gate
   (norms vs 0.62-frame day-caps as established, paired within-tram tests, replay) —
   if weekday peaks read non-1.0 robustly, that's the program's first real TOD shipment.
   Morning boundary check 07:00 (mirror of the 19:00 cap-boundary artifact) pre-registered.
2. **R8 live gate-2** (zonal dwell, evidence complete 16×): parity A/B behind a dev flag
   per round-22 pre-registration — needs a weekday window ≥2 h incl. a peak, fresh app.
3. **R11 decisive**: entry condition (fleet ≥23–24 km/h or deep-recovery <52%) most likely
   fires in the weekday early night; steady −25 follow-up rides along.
4. **TOD h22/h23 finals** (carried from Sunday round 23).
5. **Device ride recording** (user, when possible) — ground truth outranks sim data.

## Ops rules (accumulated)

- Stream everything with --since; never whole-file passes over closed sessions (542 MB!).
- Exclude windows overlapping host load (local builds, heavy analysis) from probe-level readings.
- Prophylactic app restart at ~6 h uptime (uptime CPU-degradation class, see performance.md);
  restart at a window boundary and note the time.
- Post-19:00 / pre-07:00 norms live in the capless frame (vs 0.62), day norms vs 0.65–0.66.
- **HOST SLEEP (round 24): check `pmset -g log` at round start.** A closed-lid MacBook
  Maintenance-sleeps regardless of `caffeinate -s` (verified: machine re-slept 45 s after a
  live PreventSystemSleep assertion). During host sleep the sim+app are suspended; the file
  only gets 45-s DarkWake fragments every ~15 min. Windows overlapping host sleep: feed-side
  observables (delayS, obsDist) sampled inside a fragment are valid but sparse; ALL sim-side
  probes are contaminated — exclude. The harvester's `sleep 600` is suspended too (one cycle
  ran 2 h 11 m); a stale sim file with a live harvester PID = suspected host sleep, not a
  harvester crash.

## Rounds

## Round 24 (Mon 02:16 → 03:35, executed in wake-bursts; host-sleep incident round)

**Ops incident — clamshell sleep killed the night program.** Lid closed at 23:16:39 Sunday
(`Clamshell Sleep`, pmset log); from then until this round woke the machine at 02:16:40 the
host only DarkWoke for ~45 s every ~15 min. Consequences: h23 Sunday has 16 clean minutes
(23:00–23:16); h0–h2 Monday effectively don't exist (fragments); round 23's 23:49 reading was
itself a fragment. Recovery actions: harvest loop kicked (02:17, file was stale 2 h with a
suspended sleep-timer); `caffeinate -s -w <harvester>` started — **proved ineffective against
clamshell sleep** (machine re-slept 02:18:05), left running as it still prevents idle sleep
once the lid opens; manual drain + prophylactic app restart **03:25:26** (new PID 24460; old
app wall-uptime 6.8 h of which ~3 h suspended mid-run — restart also resets post-suspension
state). **USER ACTION REQUIRED: open the lid (or attach an external display) before ~06:30,
otherwise the headline weekday-peak program (h7–h9, Monday priority #1) collects nothing.**

Analysis salvaged from the Sunday tail (streamed via `tail -c 64M` extract, 21:18→23:49; h22
hour is 100% clean — sleep began 23:16):

- **TOD_PACE h22 = 1.0 FINAL (full hour 22:00–23:00, capless frame).** (1) norm vs 0.62
  **x1.016** FULL (130/115, 245 trams), halves x1.016/x1.032, parity x1.024/x1.016, 4-way
  x1.000–x1.048 — first hour reading at-or-above in every subsample, BUT round-22's
  first-33-min halves read x0.968/x0.984 below → straddle within the hour, magnitude ≤3.2%
  typical; likely coupled to the end-of-service delayS pulse (bias drifts up as the schedule
  slips, half B p50 0.640–0.650). (2) paired h21p→h22 (231 trams) Δbias **+0.010** (119/97),
  Δspeed **+0.36** — near-null, familiar zonal split (centre −0.020/−1.72 vs out
  +0.020/+0.47). (3) replay D52/D53 on the full-hour extract: both flat-to-worse (frs p50
  122/122 vs NEW 121) — gate record in replay.py. Triple gate unanimous: neutral.
- **TOD_DWELL h22 = 1.0 FINAL**: dwell/stop uncensored **[9.6, 24.2]** s (5664 stops), 18
  inside; stationary 11–29% of time.
- **R8 band RESTORED on the full hour — round-22 x1.17 caveat resolved as a half-window
  artifact.** Censored per-dwell MID p50 centre/outskirts **98/82 s = x1.20** (n=504/888),
  back inside x1.20–x1.27 (17th reproduction). Uncensored budgets centre [12.4, 31.5] vs out
  [8.4, 21.3] s/stop. Centre dwell watch: 98 < 115, zonal entry still not triggered.
- **TOD h23: NOT FINALIZABLE** (16 clean min of 60; host sleep). Clean-window reading
  (23:00–23:16, 163 trams, at-fix n=2132): norm vs 0.62 **x1.032** (94/69), halves
  x1.016/x1.032 — weak-up, direction consistent with the h22 half-B drift; day-fleet moving
  p50 21.2. Recorded as first reading; **stays 1.0**; full re-measure needs a lid-open night.
- **TOD h0 Monday: NO measurement** (≈5.5 clean min + fragments). First weekday-night h0
  remains unmeasured.
- **Late-night delayS slippage — RESOLVED (Sunday side): transient end-of-service pulse, not
  a night-transition ramp.** Day-fleet per-fix delayS p50 by 10-min bucket: 41 (22:00) → 51 →
  60 → **65 (22:30 peak)** → 64 → 59 → 55 → 46 (23:10) → 37 (23:40, sparse fragments). Round
  22 caught the rising edge only. Monday-night cleanliness check still open (blocked, no
  data).
- **R11 entry — threshold nominally crossed, decisive test still unpowered.** Window
  22:00–23:16 clean, fresh app: deep-recovery share ΔdelayS<−10 **47.4%** (2283/4814) — first
  reading below the 52% constant (round 22: 49.4%), BUT fleet true feed speed h22 **22.3**
  km/h (< 23–24) and the fast population is tiny: night lines h23 = **12 trams, p50 27.3
  km/h, bias p50 0.69, at-fix n=64 ≪ 300**. Mechanism 8th reproduction: OLS slope **+4.58**
  m/ΔdelayS-s (band +4.07…+4.59, top edge), bands monotonic (−107/−23/+6/+46/+105), at_stop
  control −69. Honest note: steady ΣΔdelayS∈[−5,+5) **−24.0** (n=384) — second consecutive
  reading below the predicted −10…−15 band (round 22: −25.0, n=252); probe-level all p50
  −2.0, review trigger (all < −20) not hit; now a named follow-up. Weekday-night R11 window
  = first lid-open night, target the 91–99 segment.
- Health/context: at-fix signed p50 **−2.0** across h22 + h23-clean (app fresh ~2 h) — night
  levels healthy; centre true-feed floor **19.2/19.3** (h22/h23) — no upward release through
  23:16 (23+ h); night-route share ~1% of records through 23:16. Replay gate re-validation
  on h22 full extract: OLD 118.8 → NEW 115.5 (**−2.8%, twenty-fifth**), frs p90 377.6 → 362.7
  (−4.0%).

**Applied (engine): nothing — no gate passed** (h22 finals are neutral verdicts; everything
else blocked on data). replay.py: comment-only gate-record note for the D52/D53 h22-final
rerun. jest + tsc green.

### Open items after round 24

- **BLOCKED ON LID: everything Monday.** Weekday peaks (priority #1, needs h7–h9), R8 live
  gate-2 (needs ≥2 h weekday incl. peak, fresh app), R11 weekday-night decisive, h0/h23
  weekday re-measures, Monday slippage cleanliness. Harvester + caffeinate + fresh app (PID
  24460) are standing by; pipeline resumes by itself once the lid opens.
- **Steady −24…−25 follow-up** (2 consecutive reads below the −10…−15 designed-drag band,
  n=384/252) — re-read on the first healthy weekday window; escalate only if probe-level
  all p50 also sinks (trigger < −20 stands).
- **R11 deep-recovery watch**: 52% constant broke (49.4% → 47.4% late-Sunday) — decide
  whether the entry condition means "weekday-night fleet" (original intent) explicitly when
  re-registering; don't fire on Sunday wind-down data.
- Carried: TOD h23 final + h0 first measure (weekday night, lid open); centre upward-release
  watch (floor 18.8–19.3, 23+ h); %ahead-vs-device (device ride); 19:00/07:00 cap-boundary
  methodology caveat; night fleet 91–99 first real measurement (still n≪gate).

### Round 25 (03:33, orchestrator minimal — host still lid-closed)
Mac remains in the clamshell sleep / DarkWake cycle (pmset: 45 s bursts every ~15 min).
Data trickles in fragments during bursts only — feed-valid but sim-contaminated per the
round-24 rule; night fleet is tiny anyway. No analysis attempted, no agent spawned.
Pipeline standing by (harvester alive, app PID 24460 fresh from 03:25). Blocking item
unchanged: **lid must be opened (or external display attached) before ~06:30** or the
Monday weekday-peak headline test (h7–h9) collects nothing. Carried: h23/h0 finals,
R11 decisive, weekday peaks, R8 live gate-2.

## Round 26 (Mon 04:34 → 04:50; HEADLINE: host awake since 03:24 VERIFIED → first clean
## weekday-night window; first-ever NIGHT-FLEET (91–99) measurements: TOD h3/h4 = 1.0
## (reproduce Sunday exactly), R8 zonal band x1.26 REPRODUCED on the new population,
## R11 entry marginal + NO plateau on healthy app (9th mechanism repro, first night-fleet);
## city wake-up observed: first day tram 04:11:58, 41 trams/22 lines by 04:30)

### Continuity verification (round entry requirement)

- **pmset**: last event = DarkWake 03:24:15 (rtc/SleepService); **zero `Entering Sleep`
  entries after** — the DarkWake was held into full wake by session activity (keyboard
  tickles in the assertion log). The 23:16→03:24 clamshell cycle ENDED at 03:24.
- **Gap-scan sim-2026-07-13.jsonl** (streamed, line pass over the fresh 11 MB file):
  15 gaps >20 s in the file, ALL ending ≤03:24:15 (the DarkWake fragments); **zero gaps
  >20 s from 03:24:15 → end of data (04:40:53)**. Poll cadence continuous.
- **Clean window: 03:25:26 (app restart, PID 24460) → 04:40:53 (~75 min).** App fresh the
  whole window → probe-level VALID. User health-check 04:34 was light — not excluded
  (per ops note). My own analysis pass 04:35–04:50 was light (11 MB streams + one
  `tail -c 64M` Sunday extract + jest/tsc ~4 s) — flagging ~04:44–04:47 for the next
  round's discretion, no exclusion claimed.

### Window & fleet

Full window 03:25→04:40: 75 trams, ~38 k records; **night-route (91–99) records 95%**
(56 night trams) — first true night-fleet dataset of the program (weekday night). Day
lines reappear from 04:11:58 (see wake-up section). Cuts: h3 = 03:25–04:00 (partial,
35 min); h4 = 04:00–04:45.

### Tooling (this round): `--fleet day|night|all` flag

`plateau_diag.py`, `day_prior.py`, `tod_paired.py`, `dwell_v2.py` all hard-excluded
NIGHT_LINES — fine for the day-fleet program, blind to the night fleet. Added a shared
`--fleet` flag (default `day` = historical behavior). **Regression: bit-exact
reproduction of round-24 numbers on the Sunday 22:00–23:16 extract** — plateau_diag
mechanism bands n=2283/745/580/471/735, OLS +4.58; day_prior h22 x1.016 (130/115,
245 trams); dwell_v2 h22 MID 98/82 (n=504/888); tod_paired 231 paired trams, Δbias
+0.010. Also pinned the **deep-recovery share definition** (was computed ad hoc in
rounds 17–24): share = n(ΔdelayS<−10) / Σn over the five on_track mechanism bands of
plateau_diag §3 — reproduces round-24's 2283/4814 = 47.4% exactly.

### (б) TOD h3/h4 Monday — first weekday-night readings (night fleet, capless frame): both stay 1.0, reproduce Sunday

- **h3 (partial 03:25–04:00)**: norm vs 0.62 **x1.032** FULL (35/21, 56 trams), halves
  x0.984/x1.056, parity x1.065/x1.032, 4-way x0.952–x1.097 — straddles. **Identical to
  Sunday h3 (0.640 → x1.032).**
- **h4**: norm vs 0.62 **x1.016** FULL (31/24, 55 trams), halves x1.024/x1.016, parity
  x1.016/x1.032, 4-way x1.000–x1.048 — weak-up ≤4.8%, one subsample at exactly 1.000.
  Sunday h4 read x1.032 (x1.016 depot-filtered) — same ballpark.
- **Paired h3→h4 (55 trams)**: Δbias **−0.050** (25/30), Δspeed **−0.84** km/h —
  near-null-DOWN, contradicting the weak-up norms; familiar zonal split (centre-majority
  −0.100/−2.76 (2/18) vs outskirts +0.040/+0.29). Evidence gate fails 2 of 3 legs →
  replay unnecessary. **TOD_PACE h3/h4 weekday-night = 1.0** (h3 flagged partial-hour).
- **TOD_DWELL h3/h4 = 1.0**: uncensored dwell/stop **[14.5, 28.2]** s (h03 [16.7, 31.3],
  h04 [13.6, 27.2]) — 18 s inside. Night-fleet stationary share 16–31% of time.
- Sample gates: at-fix n h3=1216, h4=1263 (≥300 PASS); night-fleet trams 56 ≫ round-24's
  12 — the "n≪gate" carry-over item is CLOSED.
- **«Хасанят»-гипотеза: опровергнута и на будничной ночи** (day-fleet h0–h4 Sunday +
  night-fleet h3/h4 Monday): night-fleet true feed speed (Δobs/ΔobsAt, moving p50)
  h3 **23.4** / h4 **22.1** km/h — быстрее дневного центра, но никакого x1.1–1.25.

### (а) R11 decisive — entry MARGINAL, no plateau on healthy app; mechanism 9th reproduction (first night-fleet)

- **Entry condition (weekday-night, night fleet — the pre-registered target segment)**:
  true feed speed h3 **23.4** km/h (touches the 23–24 lower edge), h4 **22.1** (below);
  deep-recovery share **49.3%** (630/1277, pinned definition) vs constant 52% — 2.7 pt
  below, same order as r22's 49.4% / r24's 47.4%. **Verdict: entry fires MARGINALLY at
  best; the weekday night did NOT deliver the fast-feed regime.** Recommend
  re-registering the entry as unmet-by-night: only a genuinely fast feed (≥24 sustained
  + deep-recovery well under 47%) re-activates the decisive test.
- **Level test (the decisive read, healthy app 0–1.3 h uptime)**: at-fix **all p50 −4.0**
  full / −3.0 second half; 5-min buckets mostly −2 with isolated dips −24…−27 (03:45,
  04:05–04:10) that immediately recover — **NO plateau**. prev=at_stop −2.0 invariant
  intact; prev=on_track −41 full/−44 second half (inside the designed-drag prediction
  −45…−55). **R11 stays FILED (downgraded)** — 4th consecutive healthy-app read at
  target level.
- **Mechanism 9th reproduction, FIRST on the night fleet**: OLS slope **+4.64**
  m/ΔdelayS-s (historical band +4.07…+4.59 — new top edge, band now +4.07…+4.64), bands
  monotonic (−106/−22/−2/+36.5/+101), at_stop control −77 under recovery. The designed
  projection drag is fleet-independent — strengthens the "design, not bug" verdict.
- **delayS Monday-night cleanliness (open item): CLEAN.** Fleet delayS p50 per 5-min
  bucket 12–34 s, no end-of-service pulse analog (Sunday's 22:30 peak 65 confirmed as a
  transient, not a night-regime feature).

### (г) steady −24/−25 follow-up — day-fleet third reading UNPOWERED; night-fleet first baseline −43

- Day fleet this window: steady ΣΔdelayS∈[−5,+5) n=4 (−25.5) — no third day-fleet
  reading (03–04 h has almost no day trams). **Follow-up stays open** for the first
  healthy day window.
- Night fleet (NEW population, separate baseline, do not mix with the day-fleet band):
  steady **−43.0** (n=103), steady-by-level [0,60) −39.0 (n=69) — deeper than the
  day-fleet designed band −10…−15. Review trigger (probe all p50 < −20) **NOT hit**
  (all −4.0). Plausible driver: longer fix cadence + transfer-hub holds (below) inflate
  per-fix drag on night routes; watch on the next night round before theorizing.

### (в) R8 weekday-night band — REPRODUCED on the night fleet (18th reproduction, first on the new population)

- Censored per-dwell MID p50 centre/outskirts **97/77 s = x1.26** (n=212/266) — inside
  the day-fleet band x1.20–x1.27 without adjustment. Fix cadence p50 46 s (same
  censoring floor).
- Uncensored budgets: centre **[27.9, 47.1]** vs out **[8.1, 19.4]** s/stop — the zonal
  split is STRONGER at night than day (~24 vs ~17): night-line transfer-hub holds (top
  dwell sites 348–376 s MID at night-network junction stops; timed-transfer behavior).
  Fleet-level budget still brackets 18 s (see TOD_DWELL above) — the zonal candidate
  multipliers for gate-2 (centre x1.30 / out x0.90) look conservative for a night regime
  but gate-2 is pre-registered as a DAY experiment; no change.
- Centre dwell watch: 97 < 115 — zonal entry still not triggered.

### City wake-up status (for peak-round planning)

- **First day-line tram: 04:11:58 (line 17, key 9064)**; then 6 (04:13), 3 (04:15),
  11 (04:19). By bucket: 04:10s = 4 trams/4 lines → 04:20s = 17/12 → **04:30s = 41
  trams/22 lines** — the morning rollout is fully underway.
- Host awake, harvester ticking (10-min cadence), app fresh (PID 24460, 03:25). **The
  h7–h9 weekday-peak headline program is GO.** Ops for the peak round: prophylactic app
  restart at a window boundary ~06:45–07:00 (otherwise the 6-h uptime mark lands ~09:25,
  mid-peak); R8 live gate-2 (weekday ≥2 h incl. peak, fresh app, parity A/B) is now
  actionable — recompute the centre/out multipliers on the day aggregate before start.

### Applied (engine)

**Nothing — no gate passed** (TOD h3/h4 neutral verdicts on double-gate; R8 is an
observation round for a new population; R11 entry marginal + level clean). Tooling only:
`--fleet` flag ×4 scripts (regression bit-exact), deep-recovery definition pinned.
jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### Open items after round 26

- **WEEKDAY PEAKS (priority #1) — GO for h7–h9**: host awake, pipeline healthy; restart
  app ~06:45–07:00; morning 07:00 cap-boundary check pre-registered (round-24 note).
- **R8 live gate-2 — actionable today** (weekday ≥2 h incl. peak, fresh app, parity A/B,
  centre x1.30 / out x0.90 recomputed on day aggregate).
- **R11**: entry re-registration proposal above (weekday night did NOT deliver fast
  feed; night-fleet mechanism repro in the bag). Decisive test dormant until a genuinely
  fast feed window.
- **Steady follow-up**: day-fleet third reading still pending (first day window);
  night-fleet baseline −43 (n=103) recorded separately, watch next night round.
- Carried: TOD h23/h0 weekday finals (tonight, host must stay awake); h5/h6 Monday
  first readings (data accumulating now); centre upward-release watch (day-fleet floor
  19.2–19.3 — night-fleet centre reads 19.3–19.6, consistent); %ahead-vs-device (device
  ride); 19:00/07:00 cap-boundary methodology caveat.

## Round 27 (Mon 05:11 → 05:50; dawn transition, pre-peak: night h4-tail/h5-remnant = 1.0
## (weekday mirrors Sunday's truncation structure), FIRST weekday day-fleet readings —
## prior norm 0.660/x1.065 reproduces Sunday, dawn feed +1.4–1.8 km/h FASTER than Sunday
## (outskirts-led; centre floor holds), R8 day dawn x1.31 first read ABOVE band,
## steady day-fleet third read POWERED −24.0 (n=165), mechanism 10th repro +4.54)

### Pipeline health (runbook step 1)

- Harvester alive (PID 31497), `harvest.log` ticking every 10 min through 05:31.
- App PID 24460 (started 03:25, uptime ~2.2 h at analysis — inside cap), Metro 8081 up.
  Nothing relaunched.
- **Host stayed awake**: pmset shows NO `Entering Sleep` after the 03:24:15 DarkWake —
  the whole 03:24→05:35 window is continuous (lid still closed; session activity holding
  it up). Gap-scan of `sim-2026-07-13.jsonl` since 04:35: zero gaps >30 s; file streams
  through 05:30:51 (32.2 MB, 125,240 records).

### Window & fleet

`--since 2026-07-13 04:40 --until 05:31`: 50.8 min. Day fleet ramp 69 → 159 trams
(57,162 records, 167 trams total); night fleet 46 → 0 (16,482 records, 46 trams),
**last night-line record 05:28:06 — weekday night service ends at the same ~05:28 as
Sunday**. Segmented: night frame (h4-tail 04:40–05:00, h5 remnant 05:00–05:28) vs day
fleet (full window, first weekday day readings — observations only, per the
pre-registration that a dawn tail is unfit for day finals).

### (а) Night frame: h4-tail and h5 remnant — both 1.0, weekday reproduces Sunday's structure

- **h4-tail (night fleet, 45 trams)**: norm vs 0.62 **x1.016** FULL (23/22 — coin flip),
  subsamples straddle x0.871–x1.097 (even-parity below, odd above). Confirms round-26's
  **h4 weekday-night = 1.0** on the closing 20 min of the hour.
- **h5 night remnant (05:00–05:28, 27 trams)**: **x1.065** FULL (17/10), subsamples
  straddle x0.968–x1.242 with 4-way cells down to n=5 — and the bucket is structurally
  truncated at 28 min (night service end), exactly Sunday's verdict. **No night-h5 TOD
  entry; the h5 owner is the day fleet.** Weekday independently reproduces the Sunday
  finding.
- **TOD_DWELL night h4/h5 = 1.0**: uncensored dwell/stop h04 [10.5, 21.1], h05
  [7.4, 18.1] s — 18 inside/at edge. R8 on the night window: centre/out MID 88/79 =
  x1.11 (n=48/87) — below band on a small truncated tail; noted only, no band update.

### (б) FIRST weekday day-fleet readings (h4-tail + h5; observations, no finals)

- **Prior norm: 0.660 = x1.065 vs 0.62** (155 trams, 98/57 above/below), **all 8 disjoint
  subsamples above** (x1.048–x1.081), no coin flip. Models: 15t 0.675, kt8d5 0.660,
  t3rplf 0.630, t3rp/14t 0.610. **Reproduces Sunday's day norm 0.650–0.660 on a weekday**
  — the R5 candidate-prior 0.66 case gains weekday independence; ship condition
  unchanged (needs all-day sample incl. peak + midday, then replay + live gates).
- **Paired h4-tail→h5 (87 trams)**: Δbias **−0.030** (26/55), Δspeed **−1.36** km/h,
  centre-led (centre −0.070/−2.46 vs out −0.010/−0.75) — mild DOWN, contradicting the
  static up-norm → no separable h5 TOD signal on the double gate. h5 day stays an
  observation (as pre-registered).
- **At-fix (day)**: all p50 **−2.0** (n=4354), fresh(seed) −2.0, %ahead 39.5 (37–40 band
  holds). New model-skew note: **15t reads −11.0 / %ahead 32.4** (n=2177) vs −1…−2 for
  the other four families — watch, first time split this way on a weekday.
- delayS weekday-morning cleanliness: fleet p50 20–40 s per 5-min bucket, no pulse —
  morning ramp is clean (mirrors round-26's night cleanliness).

### (в) Pre-registered question — are weekday dawn speeds faster than Sunday? YES, outskirts-led

- True feed speed (Δobs/ΔobsAt, moving p50): **h4 24.1 vs Sunday 22.3 (+1.8, +8%)**;
  **h5 23.0 vs Sunday 21.6 (+1.4, +6.5%)**; p90 42–44 vs Sunday's 38–40.
- Zonal split h5: centre **19.6** vs outskirts **24.2** — the centre sits ON the
  established day-fleet floor (19.2–19.6 across 23+ h now, no dawn release); **the
  weekday speed-up lives in the outskirts**.
- Implication for the peak block: weekday feed ≠ Sunday feed already at dawn; h7–h9 TOD
  deviations must be measured against weekday norms (0.66 fleet prior level), not
  Sunday-derived expectations.

### (г) R8 — day-fleet dawn read x1.31: first read ABOVE the x1.20–x1.27 band

- Censored per-dwell MID p50 centre/outskirts **102/78 s = x1.31** (n=181/568) — 19th
  consecutive zonal-direction reproduction, first ABOVE the band top (dawn window;
  fix cadence p50 44 s, same censoring floor). Uncensored budgets centre **[13.0, 32.9]**
  vs out **[8.9, 22.2]** s/stop. Fleet budget [9.5, 23.9] brackets 18 → TOD_DWELL day
  h4/h5 no change.
- Supports the gate-2 centre candidate **x1.30** (the pre-start recompute on the weekday
  day aggregate stands). Centre dwell watch: 102 < 115 — zonal entry still not triggered.

### (д) Steady −24/−25 follow-up — day-fleet THIRD consecutive reproduction, now POWERED

- **Steady ΣΔdelayS∈[−5,+5) day fleet = −24.0 (n=165)** (by level: [0,60) −37.0 n=91,
  [60,∞) −31.0 n=52) — vs r22 −25.0 (n=252) and r24 −24.0 (n=384). Three consecutive
  windows (Sunday evening ×2, weekday dawn) at −24…−25, below the −10…−15 designed-drag
  prediction. Escalation trigger (probe all p50 < −20) **NOT hit** — all p50 −2.0, no
  5-min bucket below −6.0. **Reclassified: stable day-fleet characteristic (~−24), passive
  watch**; escalate only on the standing trigger.
- Night-fleet steady this window **−12.0** (n=48) vs round-26's −43.0 (n=103) — the night
  baseline is NOT stable across windows; keep separate, do not average, watch next night.

### (е) R11 — entry not met on the day fleet either; mechanism 10th reproduction

- Entry: day feed h4 24.1 (touches ≥24) but h5 23.0 (not sustained); deep-recovery share
  (pinned def) day **46.0%** (902/1959), night **47.0%** (239/508) — under r24's 47.4%
  but not "well under". **Entry NOT met; R11 stays FILED/dormant** per the round-26
  re-registration proposal.
- Level test anyway: all p50 −2.0 full window, no plateau — **5th consecutive healthy-app
  read at target level**.
- **Mechanism 10th reproduction, first on a weekday day-fleet dawn**: OLS **+4.54**
  m/ΔdelayS-s (band +4.07…+4.64 holds), bands monotonic (−108/−15.5/+2/+61/+101),
  at_stop control −67 under recovery. Night-fleet OLS this window +3.98 (n=409, truncated
  tail) — first point below the band's low edge; small-n, band unchanged, noted.

### Applied (engine)

**Nothing — no gate passed** (night h4/h5 neutral on straddle + truncation; day h5 fails
the paired leg of the double gate and is pre-registered as observation-only; R8/steady/R11
are watches). No code changes at all this round (existing scripts covered every cut).
jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### PRE-REGISTERED PEAK BLOCK PLAN (next round, priority #1 — h7–h9)

1. **Prophylactic app restart 06:45–07:00** (window boundary; otherwise the 6-h uptime
   cap lands ~09:25 mid-peak). Note the new PID in the round log; do not restart again
   inside the peak.
2. **07:00 cap-boundary MIRROR check** (methodology caveat, round-19/24): the daytime
   centre cap engages at 07:00 and the bias basis shifts the OPPOSITE way vs the 19:00
   release. Read at-fix + norm on the same fleet 06:40–07:00 vs 07:00–07:20; a step
   across the boundary = artifact confirmation. **No TOD bookings from the two
   boundary-crossing 20-min windows.**
3. **R8 live gate-2** (pre-registered DAY experiment): weekday ≥2 h incl. peak, fresh
   app (see item 1), parity A/B; recompute centre/out candidates on the weekday day
   aggregate before start (dawn x1.31 supports centre x1.30 / out x0.90).
4. **h7–h9 TOD**: measure deviations against the weekday day-fleet norm (~0.66), not the
   0.62 shipped prior; double gate as always; expect the first genuinely non-neutral
   candidates of the program (peak boarding + traffic).
5. R11 dormant unless feed ≥24 sustained AND deep-recovery well under 47%.

### Open items after round 27

- **WEEKDAY PEAK BLOCK — GO** (plan above; host awake, pipeline healthy, app fresh).
- Steady day-fleet ~−24: passive watch (trigger: probe all p50 < −20). Night steady
  baseline unstable (−43 → −12): watch next night round.
- 15t model at-fix skew (−11.0 vs −1…−2 others): new watch, recheck on peak data.
- R8 dawn top-edge breach x1.31: fold into the gate-2 aggregate recompute.
- Carried: TOD h23/h0 weekday finals (tonight); h6 weekday first reading (accumulating
  now — peak round covers it); centre upward-release watch (floor 19.2–19.6, 23+ h);
  %ahead-vs-device (device ride); R5 prior-0.66 (now weekday-corroborated, needs all-day
  sample); 19:00/07:00 cap-boundary caveat (mirror check scheduled above).
