# Calibration analysis — 2026-07-13 (MONDAY — first weekday!)

Continues [analysis-2026-07-12.md](analysis-2026-07-12.md) (Sunday: rounds 0–23, full
résumé at its end). Record schema v2 (see plan.md). Sessions: sim-2026-07-13.jsonl
(rolled over automatically at midnight).

## Monday priorities (carried over + new)

1. **WEEKDAY PEAKS — the headline test of the whole TOD hypothesis.** Sunday measured all
   TOD buckets = 1.0, but Sunday peaks are weak by nature. Monday h7–h9 and h15–h18 with
   real commuter load + car traffic are the first true test: run the full double gate
   (norms vs 0.62-frame day-caps as established, paired within-tram tests, replay) —
   if weekday peaks read non-1.0 robustly, that's the program's first real TOD shipment.
   Morning boundary check 07:00 (mirror of the 19:00 cap-boundary artifact) pre-registered.
2. **R8 live gate-2** (zonal dwell, evidence complete 16×): parity A/B behind a dev flag
   per round-22 pre-registration — needs a weekday window ≥2 h incl. a peak, fresh app.
3. **R11 decisive**: entry condition (fleet ≥23–24 km/h or deep-recovery <52%) most likely
   fires in the weekday early night; steady −25 follow-up rides along.
4. **TOD h22/h23 finals** (carried from Sunday round 23).
5. **Device ride recording** (user, when possible) — ground truth outranks sim data.

## Ops rules (accumulated)

- Stream everything with --since; never whole-file passes over closed sessions (542 MB!).
- Exclude windows overlapping host load (local builds, heavy analysis) from probe-level readings.
- Prophylactic app restart at ~6 h uptime (uptime CPU-degradation class, see performance.md);
  restart at a window boundary and note the time.
- Post-19:00 / pre-07:00 norms live in the capless frame (vs 0.62), day norms vs 0.65–0.66.
- **HOST SLEEP (round 24): check `pmset -g log` at round start.** A closed-lid MacBook
  Maintenance-sleeps regardless of `caffeinate -s` (verified: machine re-slept 45 s after a
  live PreventSystemSleep assertion). During host sleep the sim+app are suspended; the file
  only gets 45-s DarkWake fragments every ~15 min. Windows overlapping host sleep: feed-side
  observables (delayS, obsDist) sampled inside a fragment are valid but sparse; ALL sim-side
  probes are contaminated — exclude. The harvester's `sleep 600` is suspended too (one cycle
  ran 2 h 11 m); a stale sim file with a live harvester PID = suspected host sleep, not a
  harvester crash.

## Rounds

## Round 24 (Mon 02:16 → 03:35, executed in wake-bursts; host-sleep incident round)

**Ops incident — clamshell sleep killed the night program.** Lid closed at 23:16:39 Sunday
(`Clamshell Sleep`, pmset log); from then until this round woke the machine at 02:16:40 the
host only DarkWoke for ~45 s every ~15 min. Consequences: h23 Sunday has 16 clean minutes
(23:00–23:16); h0–h2 Monday effectively don't exist (fragments); round 23's 23:49 reading was
itself a fragment. Recovery actions: harvest loop kicked (02:17, file was stale 2 h with a
suspended sleep-timer); `caffeinate -s -w <harvester>` started — **proved ineffective against
clamshell sleep** (machine re-slept 02:18:05), left running as it still prevents idle sleep
once the lid opens; manual drain + prophylactic app restart **03:25:26** (new PID 24460; old
app wall-uptime 6.8 h of which ~3 h suspended mid-run — restart also resets post-suspension
state). **USER ACTION REQUIRED: open the lid (or attach an external display) before ~06:30,
otherwise the headline weekday-peak program (h7–h9, Monday priority #1) collects nothing.**

Analysis salvaged from the Sunday tail (streamed via `tail -c 64M` extract, 21:18→23:49; h22
hour is 100% clean — sleep began 23:16):

- **TOD_PACE h22 = 1.0 FINAL (full hour 22:00–23:00, capless frame).** (1) norm vs 0.62
  **x1.016** FULL (130/115, 245 trams), halves x1.016/x1.032, parity x1.024/x1.016, 4-way
  x1.000–x1.048 — first hour reading at-or-above in every subsample, BUT round-22's
  first-33-min halves read x0.968/x0.984 below → straddle within the hour, magnitude ≤3.2%
  typical; likely coupled to the end-of-service delayS pulse (bias drifts up as the schedule
  slips, half B p50 0.640–0.650). (2) paired h21p→h22 (231 trams) Δbias **+0.010** (119/97),
  Δspeed **+0.36** — near-null, familiar zonal split (centre −0.020/−1.72 vs out
  +0.020/+0.47). (3) replay D52/D53 on the full-hour extract: both flat-to-worse (frs p50
  122/122 vs NEW 121) — gate record in replay.py. Triple gate unanimous: neutral.
- **TOD_DWELL h22 = 1.0 FINAL**: dwell/stop uncensored **[9.6, 24.2]** s (5664 stops), 18
  inside; stationary 11–29% of time.
- **R8 band RESTORED on the full hour — round-22 x1.17 caveat resolved as a half-window
  artifact.** Censored per-dwell MID p50 centre/outskirts **98/82 s = x1.20** (n=504/888),
  back inside x1.20–x1.27 (17th reproduction). Uncensored budgets centre [12.4, 31.5] vs out
  [8.4, 21.3] s/stop. Centre dwell watch: 98 < 115, zonal entry still not triggered.
- **TOD h23: NOT FINALIZABLE** (16 clean min of 60; host sleep). Clean-window reading
  (23:00–23:16, 163 trams, at-fix n=2132): norm vs 0.62 **x1.032** (94/69), halves
  x1.016/x1.032 — weak-up, direction consistent with the h22 half-B drift; day-fleet moving
  p50 21.2. Recorded as first reading; **stays 1.0**; full re-measure needs a lid-open night.
- **TOD h0 Monday: NO measurement** (≈5.5 clean min + fragments). First weekday-night h0
  remains unmeasured.
- **Late-night delayS slippage — RESOLVED (Sunday side): transient end-of-service pulse, not
  a night-transition ramp.** Day-fleet per-fix delayS p50 by 10-min bucket: 41 (22:00) → 51 →
  60 → **65 (22:30 peak)** → 64 → 59 → 55 → 46 (23:10) → 37 (23:40, sparse fragments). Round
  22 caught the rising edge only. Monday-night cleanliness check still open (blocked, no
  data).
- **R11 entry — threshold nominally crossed, decisive test still unpowered.** Window
  22:00–23:16 clean, fresh app: deep-recovery share ΔdelayS<−10 **47.4%** (2283/4814) — first
  reading below the 52% constant (round 22: 49.4%), BUT fleet true feed speed h22 **22.3**
  km/h (< 23–24) and the fast population is tiny: night lines h23 = **12 trams, p50 27.3
  km/h, bias p50 0.69, at-fix n=64 ≪ 300**. Mechanism 8th reproduction: OLS slope **+4.58**
  m/ΔdelayS-s (band +4.07…+4.59, top edge), bands monotonic (−107/−23/+6/+46/+105), at_stop
  control −69. Honest note: steady ΣΔdelayS∈[−5,+5) **−24.0** (n=384) — second consecutive
  reading below the predicted −10…−15 band (round 22: −25.0, n=252); probe-level all p50
  −2.0, review trigger (all < −20) not hit; now a named follow-up. Weekday-night R11 window
  = first lid-open night, target the 91–99 segment.
- Health/context: at-fix signed p50 **−2.0** across h22 + h23-clean (app fresh ~2 h) — night
  levels healthy; centre true-feed floor **19.2/19.3** (h22/h23) — no upward release through
  23:16 (23+ h); night-route share ~1% of records through 23:16. Replay gate re-validation
  on h22 full extract: OLD 118.8 → NEW 115.5 (**−2.8%, twenty-fifth**), frs p90 377.6 → 362.7
  (−4.0%).

**Applied (engine): nothing — no gate passed** (h22 finals are neutral verdicts; everything
else blocked on data). replay.py: comment-only gate-record note for the D52/D53 h22-final
rerun. jest + tsc green.

### Open items after round 24

- **BLOCKED ON LID: everything Monday.** Weekday peaks (priority #1, needs h7–h9), R8 live
  gate-2 (needs ≥2 h weekday incl. peak, fresh app), R11 weekday-night decisive, h0/h23
  weekday re-measures, Monday slippage cleanliness. Harvester + caffeinate + fresh app (PID
  24460) are standing by; pipeline resumes by itself once the lid opens.
- **Steady −24…−25 follow-up** (2 consecutive reads below the −10…−15 designed-drag band,
  n=384/252) — re-read on the first healthy weekday window; escalate only if probe-level
  all p50 also sinks (trigger < −20 stands).
- **R11 deep-recovery watch**: 52% constant broke (49.4% → 47.4% late-Sunday) — decide
  whether the entry condition means "weekday-night fleet" (original intent) explicitly when
  re-registering; don't fire on Sunday wind-down data.
- Carried: TOD h23 final + h0 first measure (weekday night, lid open); centre upward-release
  watch (floor 18.8–19.3, 23+ h); %ahead-vs-device (device ride); 19:00/07:00 cap-boundary
  methodology caveat; night fleet 91–99 first real measurement (still n≪gate).

### Round 25 (03:33, orchestrator minimal — host still lid-closed)
Mac remains in the clamshell sleep / DarkWake cycle (pmset: 45 s bursts every ~15 min).
Data trickles in fragments during bursts only — feed-valid but sim-contaminated per the
round-24 rule; night fleet is tiny anyway. No analysis attempted, no agent spawned.
Pipeline standing by (harvester alive, app PID 24460 fresh from 03:25). Blocking item
unchanged: **lid must be opened (or external display attached) before ~06:30** or the
Monday weekday-peak headline test (h7–h9) collects nothing. Carried: h23/h0 finals,
R11 decisive, weekday peaks, R8 live gate-2.

## Round 26 (Mon 04:34 → 04:50; HEADLINE: host awake since 03:24 VERIFIED → first clean
## weekday-night window; first-ever NIGHT-FLEET (91–99) measurements: TOD h3/h4 = 1.0
## (reproduce Sunday exactly), R8 zonal band x1.26 REPRODUCED on the new population,
## R11 entry marginal + NO plateau on healthy app (9th mechanism repro, first night-fleet);
## city wake-up observed: first day tram 04:11:58, 41 trams/22 lines by 04:30)

### Continuity verification (round entry requirement)

- **pmset**: last event = DarkWake 03:24:15 (rtc/SleepService); **zero `Entering Sleep`
  entries after** — the DarkWake was held into full wake by session activity (keyboard
  tickles in the assertion log). The 23:16→03:24 clamshell cycle ENDED at 03:24.
- **Gap-scan sim-2026-07-13.jsonl** (streamed, line pass over the fresh 11 MB file):
  15 gaps >20 s in the file, ALL ending ≤03:24:15 (the DarkWake fragments); **zero gaps
  >20 s from 03:24:15 → end of data (04:40:53)**. Poll cadence continuous.
- **Clean window: 03:25:26 (app restart, PID 24460) → 04:40:53 (~75 min).** App fresh the
  whole window → probe-level VALID. User health-check 04:34 was light — not excluded
  (per ops note). My own analysis pass 04:35–04:50 was light (11 MB streams + one
  `tail -c 64M` Sunday extract + jest/tsc ~4 s) — flagging ~04:44–04:47 for the next
  round's discretion, no exclusion claimed.

### Window & fleet

Full window 03:25→04:40: 75 trams, ~38 k records; **night-route (91–99) records 95%**
(56 night trams) — first true night-fleet dataset of the program (weekday night). Day
lines reappear from 04:11:58 (see wake-up section). Cuts: h3 = 03:25–04:00 (partial,
35 min); h4 = 04:00–04:45.

### Tooling (this round): `--fleet day|night|all` flag

`plateau_diag.py`, `day_prior.py`, `tod_paired.py`, `dwell_v2.py` all hard-excluded
NIGHT_LINES — fine for the day-fleet program, blind to the night fleet. Added a shared
`--fleet` flag (default `day` = historical behavior). **Regression: bit-exact
reproduction of round-24 numbers on the Sunday 22:00–23:16 extract** — plateau_diag
mechanism bands n=2283/745/580/471/735, OLS +4.58; day_prior h22 x1.016 (130/115,
245 trams); dwell_v2 h22 MID 98/82 (n=504/888); tod_paired 231 paired trams, Δbias
+0.010. Also pinned the **deep-recovery share definition** (was computed ad hoc in
rounds 17–24): share = n(ΔdelayS<−10) / Σn over the five on_track mechanism bands of
plateau_diag §3 — reproduces round-24's 2283/4814 = 47.4% exactly.

### (б) TOD h3/h4 Monday — first weekday-night readings (night fleet, capless frame): both stay 1.0, reproduce Sunday

- **h3 (partial 03:25–04:00)**: norm vs 0.62 **x1.032** FULL (35/21, 56 trams), halves
  x0.984/x1.056, parity x1.065/x1.032, 4-way x0.952–x1.097 — straddles. **Identical to
  Sunday h3 (0.640 → x1.032).**
- **h4**: norm vs 0.62 **x1.016** FULL (31/24, 55 trams), halves x1.024/x1.016, parity
  x1.016/x1.032, 4-way x1.000–x1.048 — weak-up ≤4.8%, one subsample at exactly 1.000.
  Sunday h4 read x1.032 (x1.016 depot-filtered) — same ballpark.
- **Paired h3→h4 (55 trams)**: Δbias **−0.050** (25/30), Δspeed **−0.84** km/h —
  near-null-DOWN, contradicting the weak-up norms; familiar zonal split (centre-majority
  −0.100/−2.76 (2/18) vs outskirts +0.040/+0.29). Evidence gate fails 2 of 3 legs →
  replay unnecessary. **TOD_PACE h3/h4 weekday-night = 1.0** (h3 flagged partial-hour).
- **TOD_DWELL h3/h4 = 1.0**: uncensored dwell/stop **[14.5, 28.2]** s (h03 [16.7, 31.3],
  h04 [13.6, 27.2]) — 18 s inside. Night-fleet stationary share 16–31% of time.
- Sample gates: at-fix n h3=1216, h4=1263 (≥300 PASS); night-fleet trams 56 ≫ round-24's
  12 — the "n≪gate" carry-over item is CLOSED.
- **«Хасанят»-гипотеза: опровергнута и на будничной ночи** (day-fleet h0–h4 Sunday +
  night-fleet h3/h4 Monday): night-fleet true feed speed (Δobs/ΔobsAt, moving p50)
  h3 **23.4** / h4 **22.1** km/h — быстрее дневного центра, но никакого x1.1–1.25.

### (а) R11 decisive — entry MARGINAL, no plateau on healthy app; mechanism 9th reproduction (first night-fleet)

- **Entry condition (weekday-night, night fleet — the pre-registered target segment)**:
  true feed speed h3 **23.4** km/h (touches the 23–24 lower edge), h4 **22.1** (below);
  deep-recovery share **49.3%** (630/1277, pinned definition) vs constant 52% — 2.7 pt
  below, same order as r22's 49.4% / r24's 47.4%. **Verdict: entry fires MARGINALLY at
  best; the weekday night did NOT deliver the fast-feed regime.** Recommend
  re-registering the entry as unmet-by-night: only a genuinely fast feed (≥24 sustained
  + deep-recovery well under 47%) re-activates the decisive test.
- **Level test (the decisive read, healthy app 0–1.3 h uptime)**: at-fix **all p50 −4.0**
  full / −3.0 second half; 5-min buckets mostly −2 with isolated dips −24…−27 (03:45,
  04:05–04:10) that immediately recover — **NO plateau**. prev=at_stop −2.0 invariant
  intact; prev=on_track −41 full/−44 second half (inside the designed-drag prediction
  −45…−55). **R11 stays FILED (downgraded)** — 4th consecutive healthy-app read at
  target level.
- **Mechanism 9th reproduction, FIRST on the night fleet**: OLS slope **+4.64**
  m/ΔdelayS-s (historical band +4.07…+4.59 — new top edge, band now +4.07…+4.64), bands
  monotonic (−106/−22/−2/+36.5/+101), at_stop control −77 under recovery. The designed
  projection drag is fleet-independent — strengthens the "design, not bug" verdict.
- **delayS Monday-night cleanliness (open item): CLEAN.** Fleet delayS p50 per 5-min
  bucket 12–34 s, no end-of-service pulse analog (Sunday's 22:30 peak 65 confirmed as a
  transient, not a night-regime feature).

### (г) steady −24/−25 follow-up — day-fleet third reading UNPOWERED; night-fleet first baseline −43

- Day fleet this window: steady ΣΔdelayS∈[−5,+5) n=4 (−25.5) — no third day-fleet
  reading (03–04 h has almost no day trams). **Follow-up stays open** for the first
  healthy day window.
- Night fleet (NEW population, separate baseline, do not mix with the day-fleet band):
  steady **−43.0** (n=103), steady-by-level [0,60) −39.0 (n=69) — deeper than the
  day-fleet designed band −10…−15. Review trigger (probe all p50 < −20) **NOT hit**
  (all −4.0). Plausible driver: longer fix cadence + transfer-hub holds (below) inflate
  per-fix drag on night routes; watch on the next night round before theorizing.

### (в) R8 weekday-night band — REPRODUCED on the night fleet (18th reproduction, first on the new population)

- Censored per-dwell MID p50 centre/outskirts **97/77 s = x1.26** (n=212/266) — inside
  the day-fleet band x1.20–x1.27 without adjustment. Fix cadence p50 46 s (same
  censoring floor).
- Uncensored budgets: centre **[27.9, 47.1]** vs out **[8.1, 19.4]** s/stop — the zonal
  split is STRONGER at night than day (~24 vs ~17): night-line transfer-hub holds (top
  dwell sites 348–376 s MID at night-network junction stops; timed-transfer behavior).
  Fleet-level budget still brackets 18 s (see TOD_DWELL above) — the zonal candidate
  multipliers for gate-2 (centre x1.30 / out x0.90) look conservative for a night regime
  but gate-2 is pre-registered as a DAY experiment; no change.
- Centre dwell watch: 97 < 115 — zonal entry still not triggered.

### City wake-up status (for peak-round planning)

- **First day-line tram: 04:11:58 (line 17, key 9064)**; then 6 (04:13), 3 (04:15),
  11 (04:19). By bucket: 04:10s = 4 trams/4 lines → 04:20s = 17/12 → **04:30s = 41
  trams/22 lines** — the morning rollout is fully underway.
- Host awake, harvester ticking (10-min cadence), app fresh (PID 24460, 03:25). **The
  h7–h9 weekday-peak headline program is GO.** Ops for the peak round: prophylactic app
  restart at a window boundary ~06:45–07:00 (otherwise the 6-h uptime mark lands ~09:25,
  mid-peak); R8 live gate-2 (weekday ≥2 h incl. peak, fresh app, parity A/B) is now
  actionable — recompute the centre/out multipliers on the day aggregate before start.

### Applied (engine)

**Nothing — no gate passed** (TOD h3/h4 neutral verdicts on double-gate; R8 is an
observation round for a new population; R11 entry marginal + level clean). Tooling only:
`--fleet` flag ×4 scripts (regression bit-exact), deep-recovery definition pinned.
jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### Open items after round 26

- **WEEKDAY PEAKS (priority #1) — GO for h7–h9**: host awake, pipeline healthy; restart
  app ~06:45–07:00; morning 07:00 cap-boundary check pre-registered (round-24 note).
- **R8 live gate-2 — actionable today** (weekday ≥2 h incl. peak, fresh app, parity A/B,
  centre x1.30 / out x0.90 recomputed on day aggregate).
- **R11**: entry re-registration proposal above (weekday night did NOT deliver fast
  feed; night-fleet mechanism repro in the bag). Decisive test dormant until a genuinely
  fast feed window.
- **Steady follow-up**: day-fleet third reading still pending (first day window);
  night-fleet baseline −43 (n=103) recorded separately, watch next night round.
- Carried: TOD h23/h0 weekday finals (tonight, host must stay awake); h5/h6 Monday
  first readings (data accumulating now); centre upward-release watch (day-fleet floor
  19.2–19.3 — night-fleet centre reads 19.3–19.6, consistent); %ahead-vs-device (device
  ride); 19:00/07:00 cap-boundary methodology caveat.

## Round 27 (Mon 05:11 → 05:50; dawn transition, pre-peak: night h4-tail/h5-remnant = 1.0
## (weekday mirrors Sunday's truncation structure), FIRST weekday day-fleet readings —
## prior norm 0.660/x1.065 reproduces Sunday, dawn feed +1.4–1.8 km/h FASTER than Sunday
## (outskirts-led; centre floor holds), R8 day dawn x1.31 first read ABOVE band,
## steady day-fleet third read POWERED −24.0 (n=165), mechanism 10th repro +4.54)

### Pipeline health (runbook step 1)

- Harvester alive (PID 31497), `harvest.log` ticking every 10 min through 05:31.
- App PID 24460 (started 03:25, uptime ~2.2 h at analysis — inside cap), Metro 8081 up.
  Nothing relaunched.
- **Host stayed awake**: pmset shows NO `Entering Sleep` after the 03:24:15 DarkWake —
  the whole 03:24→05:35 window is continuous (lid still closed; session activity holding
  it up). Gap-scan of `sim-2026-07-13.jsonl` since 04:35: zero gaps >30 s; file streams
  through 05:30:51 (32.2 MB, 125,240 records).

### Window & fleet

`--since 2026-07-13 04:40 --until 05:31`: 50.8 min. Day fleet ramp 69 → 159 trams
(57,162 records, 167 trams total); night fleet 46 → 0 (16,482 records, 46 trams),
**last night-line record 05:28:06 — weekday night service ends at the same ~05:28 as
Sunday**. Segmented: night frame (h4-tail 04:40–05:00, h5 remnant 05:00–05:28) vs day
fleet (full window, first weekday day readings — observations only, per the
pre-registration that a dawn tail is unfit for day finals).

### (а) Night frame: h4-tail and h5 remnant — both 1.0, weekday reproduces Sunday's structure

- **h4-tail (night fleet, 45 trams)**: norm vs 0.62 **x1.016** FULL (23/22 — coin flip),
  subsamples straddle x0.871–x1.097 (even-parity below, odd above). Confirms round-26's
  **h4 weekday-night = 1.0** on the closing 20 min of the hour.
- **h5 night remnant (05:00–05:28, 27 trams)**: **x1.065** FULL (17/10), subsamples
  straddle x0.968–x1.242 with 4-way cells down to n=5 — and the bucket is structurally
  truncated at 28 min (night service end), exactly Sunday's verdict. **No night-h5 TOD
  entry; the h5 owner is the day fleet.** Weekday independently reproduces the Sunday
  finding.
- **TOD_DWELL night h4/h5 = 1.0**: uncensored dwell/stop h04 [10.5, 21.1], h05
  [7.4, 18.1] s — 18 inside/at edge. R8 on the night window: centre/out MID 88/79 =
  x1.11 (n=48/87) — below band on a small truncated tail; noted only, no band update.

### (б) FIRST weekday day-fleet readings (h4-tail + h5; observations, no finals)

- **Prior norm: 0.660 = x1.065 vs 0.62** (155 trams, 98/57 above/below), **all 8 disjoint
  subsamples above** (x1.048–x1.081), no coin flip. Models: 15t 0.675, kt8d5 0.660,
  t3rplf 0.630, t3rp/14t 0.610. **Reproduces Sunday's day norm 0.650–0.660 on a weekday**
  — the R5 candidate-prior 0.66 case gains weekday independence; ship condition
  unchanged (needs all-day sample incl. peak + midday, then replay + live gates).
- **Paired h4-tail→h5 (87 trams)**: Δbias **−0.030** (26/55), Δspeed **−1.36** km/h,
  centre-led (centre −0.070/−2.46 vs out −0.010/−0.75) — mild DOWN, contradicting the
  static up-norm → no separable h5 TOD signal on the double gate. h5 day stays an
  observation (as pre-registered).
- **At-fix (day)**: all p50 **−2.0** (n=4354), fresh(seed) −2.0, %ahead 39.5 (37–40 band
  holds). New model-skew note: **15t reads −11.0 / %ahead 32.4** (n=2177) vs −1…−2 for
  the other four families — watch, first time split this way on a weekday.
- delayS weekday-morning cleanliness: fleet p50 20–40 s per 5-min bucket, no pulse —
  morning ramp is clean (mirrors round-26's night cleanliness).

### (в) Pre-registered question — are weekday dawn speeds faster than Sunday? YES, outskirts-led

- True feed speed (Δobs/ΔobsAt, moving p50): **h4 24.1 vs Sunday 22.3 (+1.8, +8%)**;
  **h5 23.0 vs Sunday 21.6 (+1.4, +6.5%)**; p90 42–44 vs Sunday's 38–40.
- Zonal split h5: centre **19.6** vs outskirts **24.2** — the centre sits ON the
  established day-fleet floor (19.2–19.6 across 23+ h now, no dawn release); **the
  weekday speed-up lives in the outskirts**.
- Implication for the peak block: weekday feed ≠ Sunday feed already at dawn; h7–h9 TOD
  deviations must be measured against weekday norms (0.66 fleet prior level), not
  Sunday-derived expectations.

### (г) R8 — day-fleet dawn read x1.31: first read ABOVE the x1.20–x1.27 band

- Censored per-dwell MID p50 centre/outskirts **102/78 s = x1.31** (n=181/568) — 19th
  consecutive zonal-direction reproduction, first ABOVE the band top (dawn window;
  fix cadence p50 44 s, same censoring floor). Uncensored budgets centre **[13.0, 32.9]**
  vs out **[8.9, 22.2]** s/stop. Fleet budget [9.5, 23.9] brackets 18 → TOD_DWELL day
  h4/h5 no change.
- Supports the gate-2 centre candidate **x1.30** (the pre-start recompute on the weekday
  day aggregate stands). Centre dwell watch: 102 < 115 — zonal entry still not triggered.

### (д) Steady −24/−25 follow-up — day-fleet THIRD consecutive reproduction, now POWERED

- **Steady ΣΔdelayS∈[−5,+5) day fleet = −24.0 (n=165)** (by level: [0,60) −37.0 n=91,
  [60,∞) −31.0 n=52) — vs r22 −25.0 (n=252) and r24 −24.0 (n=384). Three consecutive
  windows (Sunday evening ×2, weekday dawn) at −24…−25, below the −10…−15 designed-drag
  prediction. Escalation trigger (probe all p50 < −20) **NOT hit** — all p50 −2.0, no
  5-min bucket below −6.0. **Reclassified: stable day-fleet characteristic (~−24), passive
  watch**; escalate only on the standing trigger.
- Night-fleet steady this window **−12.0** (n=48) vs round-26's −43.0 (n=103) — the night
  baseline is NOT stable across windows; keep separate, do not average, watch next night.

### (е) R11 — entry not met on the day fleet either; mechanism 10th reproduction

- Entry: day feed h4 24.1 (touches ≥24) but h5 23.0 (not sustained); deep-recovery share
  (pinned def) day **46.0%** (902/1959), night **47.0%** (239/508) — under r24's 47.4%
  but not "well under". **Entry NOT met; R11 stays FILED/dormant** per the round-26
  re-registration proposal.
- Level test anyway: all p50 −2.0 full window, no plateau — **5th consecutive healthy-app
  read at target level**.
- **Mechanism 10th reproduction, first on a weekday day-fleet dawn**: OLS **+4.54**
  m/ΔdelayS-s (band +4.07…+4.64 holds), bands monotonic (−108/−15.5/+2/+61/+101),
  at_stop control −67 under recovery. Night-fleet OLS this window +3.98 (n=409, truncated
  tail) — first point below the band's low edge; small-n, band unchanged, noted.

### Applied (engine)

**Nothing — no gate passed** (night h4/h5 neutral on straddle + truncation; day h5 fails
the paired leg of the double gate and is pre-registered as observation-only; R8/steady/R11
are watches). No code changes at all this round (existing scripts covered every cut).
jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### PRE-REGISTERED PEAK BLOCK PLAN (next round, priority #1 — h7–h9)

1. **Prophylactic app restart 06:45–07:00** (window boundary; otherwise the 6-h uptime
   cap lands ~09:25 mid-peak). Note the new PID in the round log; do not restart again
   inside the peak.
2. **07:00 cap-boundary MIRROR check** (methodology caveat, round-19/24): the daytime
   centre cap engages at 07:00 and the bias basis shifts the OPPOSITE way vs the 19:00
   release. Read at-fix + norm on the same fleet 06:40–07:00 vs 07:00–07:20; a step
   across the boundary = artifact confirmation. **No TOD bookings from the two
   boundary-crossing 20-min windows.**
3. **R8 live gate-2** (pre-registered DAY experiment): weekday ≥2 h incl. peak, fresh
   app (see item 1), parity A/B; recompute centre/out candidates on the weekday day
   aggregate before start (dawn x1.31 supports centre x1.30 / out x0.90).
4. **h7–h9 TOD**: measure deviations against the weekday day-fleet norm (~0.66), not the
   0.62 shipped prior; double gate as always; expect the first genuinely non-neutral
   candidates of the program (peak boarding + traffic).
5. R11 dormant unless feed ≥24 sustained AND deep-recovery well under 47%.

### Open items after round 27

- **WEEKDAY PEAK BLOCK — GO** (plan above; host awake, pipeline healthy, app fresh).
- Steady day-fleet ~−24: passive watch (trigger: probe all p50 < −20). Night steady
  baseline unstable (−43 → −12): watch next night round.
- 15t model at-fix skew (−11.0 vs −1…−2 others): new watch, recheck on peak data.
- R8 dawn top-edge breach x1.31: fold into the gate-2 aggregate recompute.
- Carried: TOD h23/h0 weekday finals (tonight); h6 weekday first reading (accumulating
  now — peak round covers it); centre upward-release watch (floor 19.2–19.6, 23+ h);
  %ahead-vs-device (device ride); R5 prior-0.66 (now weekday-corroborated, needs all-day
  sample); 19:00/07:00 cap-boundary caveat (mirror check scheduled above).

## Round 28 (Mon 06:34 → ~06:55; PEAK-BLOCK PREP: h6-partial = 1.0 on the double gate
## (paired leg near-null; static leg reads BELOW the 0.66 weekday frame — centre-led),
## first morning-traffic signal: centre feed 18.9 = FIRST read below the 19.2–19.6 floor,
## outskirts flat 24.1; R8 x1.28 (20th); 07:00-mirror pre-boundary basis BOOKED
## (centre bias 0.560 / out 0.660); prophylactic app restart executed pre-peak —
## DOUBLED with an orchestrator restart, combined re-seed exclusion 06:44:28–06:47:15)

### Pipeline health (runbook step 1)

- Harvester alive (PID 31497), ticking every 10 min through 06:31; one MANUAL harvest
  cycle run ~06:39 (same merge-dedupe-drain as harvest.sh) to pull data to the restart.
- App PID 24460 (03:25, uptime ~3.3 h) alive through the whole analysis window; Metro
  8081 up. **pmset: NO `Entering Sleep` after the 03:24:15 DarkWake** — host awake
  continuously (last sleep entries 02:34/03:07, all pre-03:24).
- **Gap-scan since 05:31 (streamed): ZERO gaps >20 s** over 143,775 records
  (05:31:01 → 06:31:11). Poll cadence continuous.
- Host-load flags for successor rounds: jest+tsc ~06:41–06:45 (light, ~10 s of load);
  analysis streams 06:35–06:45 (light). No exclusion claimed (round-26 precedent).

### Window & fleet

`--since 05:31 --until 06:31` split at 06:00: h5-tail (05:31–06:00, 250 day trams,
56,797 records) vs h6-partial (06:00–06:31, 317 trams, 86,208 records). Morning ramp
continues: 250 → 317 day trams in an hour; night fleet gone (last 91–99 record 05:28,
round 27). h6 is PARTIAL (31 of 60 min) — full-hour verdict belongs to the peak round.

### (а) h6 vs the weekday day norm 0.66 — double gate: NO TOD entry (1.0), but the static leg is the first consistent-side sub-frame read

- **Static norm h6-partial: 0.640 = x1.032 vs 0.62** (301 trams, 167/134), all 8
  subsamples ≥x1.016 — same "prior runs ~0.65" signature as every day window.
  **In the 0.66 weekday frame: x0.955 FULL (121/180 below), ALL subsamples below**
  (halves x0.955/x0.970, parity x0.970/x0.939, 4-way x0.955–x0.970) — first
  consistent-side reading vs 0.66 of the program (magnitude −3…−6%). h5-tail control:
  0.650 = x1.048 vs 0.62 (its 4-way straddles x1.016–x1.065).
- **Paired h5-tail→h6 (220 trams): Δbias −0.005 (98/110), Δspeed −0.43 km/h — NEAR-NULL
  fleet-level** → the paired leg FAILS; no TOD booking. **Verdict: TOD h6 weekday = 1.0
  provisional (partial hour)** — reproduces Sunday h6 = 1.0 at fleet level.
- BUT the zonal split inside the pair is the strongest yet: **centre-majority Δbias
  −0.090 (5/42!), Δspeed −2.79** vs outskirts +0.030/+0.50 (n=54/166). The static-leg
  0.66-frame dip and the paired centre leg agree: **morning traffic is arriving,
  centre-led, and paceBias is absorbing it** (as designed — TOD stays clean while the
  bias field carries the zonal residual). Watch for h7: if the CENTRE effect grows while
  fleet stays null, the peak signal may be a zone-cap/R8 story, not a TOD_PACE story.

### (б) Morning traffic growth — h6 vs dawn (pre-registered question)

- True feed speed (Δobs/ΔobsAt, moving p50): fleet h4 24.1 (r27) → h5 22.8 → **h6 22.6**;
  **centre 19.6/20.0 → 18.9 — FIRST reading below the 23-h floor band 19.2–19.6**;
  outskirts flat 24.2 → 24.1 → 24.1. The slowdown is 100% centre-led; the dawn
  outskirts speed-up (r27) persists into h6.
- Dwells creep up: dwell/stop h05 [9.9, 24.7] → h06 [10.7, 25.7] s (18 still inside →
  TOD_DWELL h5/h6 = 1.0); dwell MID p50 84 → 85 s; fix cadence p50 45 s (same floor).
- delayS morning cleanliness: fleet p50 35 → 50 (06:00) → 38–40, no pulse — clean ramp.
- At-fix trajectory: all p50 −2.0 flat 05:30–06:20, then **−13/−22 at 06:25–06:30 with
  probe ΔdelayS −4** (recovery episode; −4 × slope 4.78 ≈ −19 — mechanism-consistent,
  NOT a plateau alarm). Peak-round note: condition at-fix reads on ΔdelayS as usual.

### (в) R8 band — x1.28 (20th zonal reproduction), gate-2 candidates hold

- Censored per-dwell MID p50 centre/outskirts **102/80 s = x1.28** (n=515/1319) — at/just
  above the x1.20–x1.27 band top, between r24's x1.20 and the dawn x1.31. Uncensored
  budgets centre [12.8, 32.7] vs out [9.6, 23.1] s/stop. Supports gate-2 candidates
  centre x1.30 / out x0.90 (weekday day aggregate so far: x1.31 dawn, x1.28 morning).
- Centre dwell watch: 102 < 115 — zonal entry still not triggered.

### (г) 07:00 cap-boundary MIRROR check — pre-boundary basis BOOKED (this round's key deliverable)

Estimator (pre-registered for the post-07:00 comparison): `day_prior.py --zone centre`
(NEW record-level CENTER-bbox flag, default `all` bit-exact regression vs the round-27
path verified by md5 on the h6 window) + plateau_diag zone rows, window 06:15–06:40
(ends 5 min before the app restart; re-seed exclusion honored):

- **Centre bias basis: p50 0.560 = x0.903 vs 0.62** (146 trams, 41/105 below-heavy;
  halves 0.550/0.560). **Outskirts control: 0.660 = x1.065** (294 trams, 193/100).
- **At-fix basis: centre −9.5 / %ahead 34.1 (n=1476); outskirts −2.0 / 41.1 (n=3516).**
- Mirror prediction: at 07:00 the daytime centre cap ENGAGES → if the 19:00 step is an
  artifact mirror, the centre bias basis jumps (opposite sign vs the 19:00 release) while
  the outskirts control stays put. Read 07:00–07:20 vs this basis; NO TOD bookings from
  06:40–07:00/07:00–07:20 boundary windows (pre-registration, round 27 item 2).
- 15t at-fix skew watch: **reproduced** — 15t −14.0 / %ahead 34.6 (n=2993) vs −2.0 /
  44.6–47.6 for all other families (2nd consecutive weekday read; r27: −11.0/32.4).
  Recheck on peak data stands.
- Steady day-fleet: −30.0 (n=355; by level [0,60) −27.5 n=172) — a notch below the
  −24…−25 characteristic; escalation trigger (probe all p50 < −20) NOT hit (all −2.0
  through 06:20). Passive watch unchanged. Mechanism: OLS **+4.78** (n=3623) — NEW top
  edge (band now +4.07…+4.78, 11th reproduction), bands monotonic
  (−110/−27/−1/+50/+109), at_stop control −74.

### (д) PROPHYLACTIC APP RESTART — executed, but DOUBLED (ops incident, minor)

Two restarts fired back-to-back — the orchestrator restarted manually while this
round's scheduled 06:45:00 restart was already armed in the background:

1. Orchestrator: terminate **06:44:28**, launch **06:44:31** (PID 46300), v2 records
   confirmed from 06:44:48.
2. Scheduled (this round): terminate **06:45:02**, launch **06:45:04** → **final app
   PID 46428**.

Verification on PID 46428: motionlog recreated 06:45, v2 lines flowing (obsAt present,
last record 06:45:37 at check time, file growing 11.4k → 12.2k lines between probes),
fresh 0.62 bias seeds visible. Container kept the 06:41–06:44 pre-restart lines
(harvest merge-dedupe will fold them in — no data hole). Old app 24460 retired at
3.3 h uptime; next 6-h mark ~12:45, clear of the morning peak.

**COMBINED RE-SEED EXCLUSION WINDOW for peak rounds: 06:44:28 → 06:47:15**
(two consecutive full-fleet re-seeds from the 0.62 prior; ±2 min rule applied from the
second launch). Coordinate future boundary restarts explicitly to avoid doubles —
double re-seed costs ~3 min of peak-adjacent data instead of ~2.

### Applied (engine)

**Nothing — no gate passed** (h6 fails the paired leg → 1.0 provisional; everything
else is watches/prep). Tooling only: `--zone centre|out|all` flag in day_prior.py
(default-path regression bit-exact on the h6 window, md5-verified). jest 22 suites /
359 tests green; `npx tsc --noEmit` clean.

### Open items after round 28 (handoff to the PEAK round)

- **PEAK ROUND IS GO**: app fresh (PID 46428, 06:45:04), host awake, harvester ticking,
  gap-scan clean. Exclude 06:44:28–06:47:15 (double re-seed) from all probe-level and
  bias-norm reads.
- **07:00 mirror check armed**: pre-boundary basis (06:15–06:40) = centre bias 0.560
  (x0.903) / out 0.660 (x1.065); at-fix centre −9.5/34.1 vs out −2.0/41.1. Read
  07:00–07:20 with `day_prior.py --zone centre` + plateau_diag zone rows against this.
  No TOD bookings from 06:40–07:00/07:00–07:20.
- **h6 completion**: 06:31–07:00 tail (minus re-seed window) → full-hour h6 verdict;
  partial-hour read = 1.0 (paired leg null), 0.66-frame dip −3…−6% centre-led.
- **h7–h9 TOD double gate vs weekday norm 0.66** (round-27 plan item 4); watch whether
  the centre-led slowdown (paired centre Δbias −0.090, centre feed 18.9 < floor band)
  grows into a fleet-level signal or stays zonal (→ R8/cap story, not TOD_PACE).
- **R8 live gate-2 actionable** (fresh app satisfied; weekday ≥2 h incl. peak; centre
  x1.30 / out x0.90 — weekday aggregate now x1.31 dawn + x1.28 morning).
- Watches carried: 15t at-fix skew (2nd reproduction −14.0/34.6 — recheck on peak);
  steady day-fleet −24…−30 passive (trigger all p50 < −20); centre feed floor BROKEN
  DOWNWARD 18.9 (first sub-19.2 read — morning traffic, expect deeper in peak);
  mechanism band extended +4.07…+4.78; R11 dormant (entry unmet); TOD h23/h0 weekday
  finals (tonight); %ahead-vs-device (device ride); R5 prior-0.66 (needs all-day incl.
  peak+midday — today completes it).

## Round 29 (Mon 07:11 → 07:55; THE WEEKDAY-PEAK HEADLINE ROUND: 07:00 cap-boundary
## MIRROR CONFIRMED (centre basis steps +0.070 UP as the cap engages — reading calibration
## for ALL 07:00–19:00 interpretation shipped), h6 weekday = 1.0 FINAL, h7 weekday peak
## through the double gate: fleet TOD gate FAILS on the paired leg — the peak is REAL
## (feed −8.2% vs Sunday h7) but ZONAL (paired centre −1.97 km/h 17/60 vs outskirts −0.39
## near-null) → NO TOD shipment, zonal findings to the R8/zonal dossier; engine rides the
## peak at all p50 −2.0; R8 x1.21/x1.26 (21st/22nd); R11 entry decisively unmet (53.6%);
## steady −24.0 POWERED (n=116, 4th repro); new model family 52t appears)

### Pipeline health (runbook step 1)

- Harvester alive (PID 31497), ticking every 10 min through 07:51 (677 MB total dir).
- App PID 46428 (launched 06:45:04 per round-28 restart) alive the whole round — uptime
  ~1.1 h at close, next 6-h mark ~12:45, clear of the peak. Metro 8081 up. Nothing
  relaunched; no restarts inside the peak (per pre-registration).
- **pmset: NO `Entering Sleep` after the 03:24:15 DarkWake** (last sleep entries
  02:34/03:07) — host awake continuously; `caffeinate` assertions live.
- **Gap-scan 06:31 → 07:51 (streamed, 194k+ records): exactly ONE gap >20 s —
  06:44:48 → 06:45:12 (24 s), the double app restart itself**, fully inside the
  pre-registered re-seed exclusion 06:44:28–06:47:15. Poll cadence otherwise continuous.
- Exclusions honored: re-seed 06:44:28–06:47:15 (no probe/bias reads); boundary windows
  06:40–07:00 / 07:00–07:20 quarantined from TOD bookings (mirror check only).
  Host-load flags for successors: jest+tsc ~07:45–07:46 (~15 s, light), analysis streams
  07:36–07:55 (light, 127 MB file passes). No exclusion claimed (round-26 precedent).

### Window & fleet

`--since 06:31` → 07:51:32. Cuts: h6-tail in two clean segments (06:31–06:44:28
pre-restart, 06:47:15–07:00 post-restart); 07:00–07:20 boundary window (mirror check
ONLY); **h7 usable window 07:20–07:51:32 (31.5 min, 359 trams, 103,761 records)**.
Morning ramp: 317 (h6-partial, r28) → 359 day trams. delayS p50 climbs 33 → 42 through
07:20–07:50 — clean peak delay build-up, no pulse.

### (а) 07:00 cap-boundary MIRROR — CONFIRMED (the round-19 artifact mirrored in reverse)

Pre-registered read, `day_prior.py --zone` + plateau_diag zone rows, 07:00–07:20 vs the
round-28 pre-boundary basis (06:15–06:40):

- **Centre bias basis: 0.560 → 0.630 (x0.903 → x1.016), step +0.070 UP**; above/below
  split FLIPS 41/105 → 75/68. Halves 0.610/0.650 — the transition is visible converging
  inside the first 20 min (bias half-life 150 s vs the 07:00 cap flip).
- **Outskirts control: 0.660 → 0.640 (x1.065 → x1.032)** — −0.020, 3.5× smaller than the
  centre step and OPPOSITE-signless (drift, not step); halves stable 0.640/0.640. Paired
  evidence below attributes this drift to peak-influx composition (new trams converging
  from the 0.62 seed), not physics: paired within-tram outskirts Δbias reads +0.000.
- At-fix: centre −9.5/%ahead 34.1 → **−18.0/34.6** full, decaying to −13.5 second half
  (bias re-learning through the cap flip + real morning traffic); outskirts −2.0/41.1 →
  −2.0/44.2 — untouched.
- Mechanism (as predicted round 19/24/28): `updatePaceBias` learns v/cap; at 07:00 the
  centre cap drops V_MAX 13.9 → V_CENTER 8.6 m/s (`zoneCapAt`, daytime 07:00–19:00), so
  the same physical speed reads as a HIGHER ratio → centre bias equilibrium jumps up.
  Mirror of the 19:00 release step, opposite sign, as pre-registered.
- **READING CALIBRATION SHIPPED (applies to ALL daytime rounds until 19:00):**
  post-boundary zone bases are **centre 0.630 / outskirts 0.640** (basis window
  07:00–07:20; h7 re-reads 07:20–07:51 confirm 0.630/0.640 — stable). The weekday 0.66
  norm was established in the CAPLESS dawn frame; fleet-level comparisons across the
  07:00 boundary carry a ~+0.07 × centre-record-share frame shift and MUST be zone-
  decomposed. Post-boundary-to-post-boundary comparisons (e.g. vs Sunday h7/h8) remain
  frame-consistent.

### (б) h6 weekday = 1.0 FINAL (full hour assembled from r28 partial + two clean tails)

- Tail statics: 06:31–06:44 **0.610/0.615 = x0.984 vs 0.62 (135/142 coin flip),
  x0.932 vs 0.66 (98/179, ALL subsamples below x0.909–x0.939)**; 06:48–07:00 **0.600 =
  x0.968 vs 0.62 (124/176), x0.909 vs 0.66 (75/226, ALL below)**. With r28's partial
  (x1.032/x0.955) the static sag deepens monotonically through the hour
  (x0.955 → x0.932 → x0.909 in the 0.66 frame) — but this is the capless-frame morning
  sag with the centre crowding in (zone-mix), and the paired instrument (r28 h5→h6:
  Δbias −0.005, Δspeed −0.43, near-null) already ruled fleet-level.
- **TOD_PACE h6 weekday = 1.0 FINAL; TOD_DWELL h6 = 1.0 FINAL** (full-hour dwell/stop
  uncensored **[10.8, 26.0] s**, 18 comfortably inside; dwell MID p50 88 s).
- R8 h6 full-hour: censored MID centre/outskirts **99/82 s = x1.21** (n=736/1576) —
  21st zonal reproduction, inside band x1.20–x1.27.

### (в) h7 WEEKDAY PEAK — the moment of truth: double gate says ZONAL, not TOD

Usable window 07:20–07:51 (boundary quarantine honored; partial hour — full-hour verdict
completes next round with the 07:52–08:00 tail):

- **Static leg vs 0.66: 0.630 = x0.955 FULL (128/229), ALL 8 disjoint subsamples below
  (x0.939–x0.970)** — second consecutive consistent-side hour. BUT in the (а)-corrected
  post-boundary frame the deviation evaporates: **h7 centre 0.630 vs basis 0.630, out
  0.640 vs 0.640 — the bias field shows NO h7-specific deviation at all.** The 0.66-frame
  dip is predominantly the dawn-capless→day-capped frame shift, exactly what the mirror
  check was pre-registered to calibrate out.
- **Paired within-tram h6(06:00–06:44) → h7(07:20–07:51), n=320 — THE DECIDER:**
  fleet Δbias **+0.010** (165/146), Δspeed **−0.90 km/h** (127/193);
  **centre-majority Δspeed −1.97 (17/60!)**, Δbias +0.020 (artifact-inflated by the
  +0.070 cap step → underlying centre bias sagged ~−0.05, absorbing the slowdown as
  designed); **outskirts-majority Δbias +0.000 (121/114), Δspeed −0.39 (110/133)** —
  inside the program's established near-null band (r28 fleet −0.43 = "near-null").
  The fleet −0.90 is carried by the centre minority. **Paired leg FAILS fleet-level.**
- Feed (true feed speed, Δobs/ΔobsAt moving p50): **h7 21.3 / centre 18.4 / out 23.1**
  vs h6 22.1/19.0/23.7 and vs **Sunday h7 23.2 → −8.2% fleet** (Sunday's own peak read
  only −2.3% poll-frame). The weekday peak is REAL and big — and the paired test says
  its within-tram component is centre-only; the fleet/outskirt hourly drift is
  composition (peak influx). **Centre feed 18.4 — third consecutive read below the
  19.2–19.6 floor band (18.9 → 18.7 → 18.4), deepening as predicted.**
- Dwells: h7 uncensored **[11.4, 26.6] s/stop** (vs h6 [10.8, 26.0]; vs SUNDAY h7
  [9.0, 22.1] — weekday boarding runs ~+20% heavier midband) — but 18 s stays inside
  the band → **TOD_DWELL h7 = 1.0 provisional**, no booking. Zone split: centre
  [14.5, 33.7] vs out [10.0, 23.5].
- Engine health at peak: at-fix **all p50 −2.0 flat** across every 5-min bucket
  07:20–07:50 (last partial bucket −5.0 with probe ΔdelayS −5 — mechanism-consistent),
  prev=on_track −44 (designed drag), fresh(seed) −2.0, %ahead 41.6. The sim rides its
  first weekday peak at target level — nothing to fix live.
- **VERDICT: TOD_PACE h7 weekday = 1.0 provisional (partial hour). No first TOD
  shipment — the double gate did its job: the weekday peak signal is ZONAL
  (centre-led), i.e. an R8/zone-cap story, not a fleet-wide TOD_PACE story.** Replay
  leg unnecessary (gate already failed 1 of 2 evidence legs; round-27 precedent).
  Pre-registration r28 ("watch whether the centre effect grows into fleet-level or
  stays zonal") answered: STAYS ZONAL on n=320 pairs.

### (г) R8 band + gate-2 status

- h7 censored MID centre/outskirts **106/84 s = x1.26** (n=745/1277) — **22nd
  consecutive zonal reproduction**, inside x1.20–x1.27. Weekday day aggregate:
  x1.31 dawn / x1.28 morning / x1.21 h6 / x1.26 h7 → gate-2 candidates
  **centre x1.30 / out x0.90 hold**. Centre dwell watch: 106 < 115 — closest read yet,
  still not triggered.
- Zonal dossier (for gate-2 / a future zonal pace model) accumulated this round:
  paired centre Δspeed −1.97…−2.77 across the three morning pairs; centre feed
  18.4–18.9 sub-floor; centre dwell/stop [14.5, 33.7] vs out [10.0, 23.5]; centre
  at-fix −15.5 vs out −2.0. Everything points the same way: the weekday peak lives in
  the centre, and the bias field + R8 zonal dwell are where it must be modeled.

### (д) Watches

- **15t at-fix skew — 3rd weekday reproduction**: −7.0/%ahead 37.0 (n=3551) vs −1…−2.5 /
  45–48 for t3rp/t3rplf/kt8d5/14t; amplitude wanders (r27 −11, r28 −14, now −7 with a
  −19 sub-window at 07:30–07:41) but the sign never flips. Converged bias 15t 0.610 =
  lowest family (14t 0.680 highest). Still a watch, no action.
- **NEW: model family `52t` first appears** (n=256 at-fix probes, −2.5/38.3, healthy) —
  first non-legacy family addition since the program started; watch only.
- **Steady day-fleet: −24.0 (n=116, [0,60) band) — 4th consecutive reproduction at
  −24…−30, now peak-POWERED.** Passive-watch classification (r27) confirmed; escalation
  trigger (probe all p50 < −20) not hit (all −2.0).
- Mechanism: OLS **+4.65** (n=2574; also +4.42/n=1651 on the boundary window) — 12th/13th
  reproductions, inside band +4.07…+4.78; bands monotonic (−106/−25/+1/+44/+112.5);
  at_stop control −75.

### (е) R11 — entry decisively UNMET at weekday peak (as pre-registered: peak ≠ fast feed)

Deep-recovery share (pinned def) **53.6%** (1843/3440) — back ABOVE the 52% constant
(peak = delay-accumulation regime, opposite of the fast-feed entry condition); fleet
feed 21.3 ≪ 23–24. **R11 stays FILED/dormant**; 6th consecutive healthy-app level read
at target (all p50 −2.0, no plateau).

### Applied (engine)

**Nothing — no gate passed.** The headline weekday-peak test resolved AGAINST a TOD_PACE
booking (paired leg zonal), TOD_DWELL bands still bracket 18 s, and the live engine
reads −2.0 through its first real peak. TOD_PACE_TABLE / TOD_DWELL_TABLE untouched
(all 1.0). No tooling changes (existing scripts covered every cut). Replay not run
(no candidate passed the evidence legs). jest 22 suites / 359 tests green;
`npx tsc --noEmit` clean.

### Open items after round 29 (handoff)

- **h7 full-hour completion** (07:52–08:00 tail) + **h8 deep peak** next round: same
  double gate, same zone bases (centre 0.630 / out 0.640); expect the zonal verdict to
  reproduce — if outskirts-paired ever leaves the near-null band, THAT is the fleet TOD
  signal. h9 closes the morning block.
- **R8 live gate-2 now fully armed**: weekday ≥2 h incl. peak satisfied by today's data;
  fresh app; candidates centre x1.30 / out x0.90 (aggregate x1.21–x1.31). The zonal
  dossier (в)/(г) makes this the program's top actionable experiment.
- **19:00 boundary re-read tonight** with the (а) bases (0.630/0.640 → expect reverse
  step at cap release; 18:40–19:20).
- Watches carried: centre feed floor break deepening (18.9 → 18.7 → 18.4 — track through
  h8–h9 peak and midday recovery); 15t skew (3rd repro); 52t new family; steady −24
  passive; mechanism band +4.07…+4.78; R11 dormant (entry: sustained ≥24 feed AND
  deep-recovery well under 47%); TOD h23/h0 weekday finals (tonight, keep host awake);
  %ahead-vs-device (device ride); R5 prior-0.66 — weekday peak reads 0.63–0.64
  post-boundary (zone-decomposed: ON basis) — the R5 case now needs the (а) zone-resolved
  framing before any midday conclusion.

## Round 30 (Mon 08:35 → ~08:55; DEEP-PEAK ROUND: h7 weekday = 1.0 FINAL (full hour ON the
## post-boundary zone bases 0.630/0.640), h8 deep peak through the double gate = 1.0
## provisional — THE ZONAL VERDICT HOLDS AND DEEPENS AT THE DEEPEST HOUR (paired h7→h8
## n=356: fleet Δbias +0.000 / Δspeed −0.15 = the most-null paired read of the program;
## centre −3.05 km/h (15/76!) = deepest centre slowdown yet; outskirts +1.05 near-null-UP);
## centre feed 18.0 = 4th consecutive sub-floor read, fleet peak floor flat 21.1–21.2;
## R8 x1.27/x1.22 (23rd/24th repro), centre-watch 109 = closest approach to 115;
## steady −16/−14 FIRST read back at the designed band edge; R8 GATE-2 SPEC SHIPPED)

### Pipeline health (runbook step 1)

- Harvester alive (PID 31497), ticking every 10 min through 08:31 (677 → 708 MB dir).
- App PID 46428 alive (launched 06:45:04, uptime 1:50 at round start; 6-h mark ~12:45,
  clear of the peak); Metro 8081 up (node 94557); sim 2AB8E802 Booted. Nothing relaunched.
- **pmset: NO `Entering Sleep` after the 03:24:15 DarkWake** (last sleep entries
  02:34/03:07); the 08:33:25 entry is a DarkWake→FullWake promotion from UserActivity,
  not a wake-from-sleep — host awake continuously since 03:24.
- **Gap-scan 07:51 → 08:31:54 (streamed): 136,684 records, ZERO gaps >20 s.** Poll
  cadence continuous. Working extract for the round taken once via `tail -c` to /tmp
  (the live file is 170 MB — one tail pass, all scripts reran against the extract).
- Host-load flags for successors: analysis streams ~08:36–08:50 (light), jest+tsc
  ~08:50 (~5 s). No exclusion claimed (round-26 precedent).

### Window & fleet

`--since 07:51` → 08:31:54. Cuts: h7-tail 07:52–08:00 folded into **h7 FULL 07:20–08:00**
(39.9 min usable, 359 trams, 131,788 records; boundary quarantine 07:00–07:20 stays out);
**h8 08:00–08:32** (31.8 min partial, 360 trams, 106,725 records). delayS p50 climbs
40 → 48 through the window — the peak delay build-up continues clean, no pulse.

### (а) h7 FINAL + h8 deep peak — double gate: the zonal verdict reproduces at the deepest hour

- **h7 full-hour static: fleet 0.640 = x1.032 vs 0.62** (210/149), all 8 subsamples
  x1.016–x1.048. Zone-decomposed on the (r29-а) post-boundary bases: **centre 0.633 vs
  basis 0.630, out 0.640 vs basis 0.640 — ON basis**, no h7-specific bias deviation on
  the full hour (confirms r29's partial-hour read). Paired leg failed fleet-level in r29
  (h6→h7 n=320, centre-only). **TOD_PACE h7 weekday = 1.0 FINAL. TOD_DWELL h7 = 1.0
  FINAL** (full-hour dwell/stop uncensored **[11.3, 26.2] s**, 18 inside; MID p50 94 s).
- **h8 static: fleet 0.640 = x1.032** (207/150); **centre 0.630 / out 0.640 — EXACTLY on
  the bases**; subsamples straddle x0.984–x1.065. Zero h8-specific deviation in the bias
  field at the deepest weekday hour. Bases 0.630/0.640 confirmed stable through 08:32.
- **Paired h7(07:20–08:00) → h8(08:00–08:32), n=356 — THE DECIDER: fleet Δbias +0.000
  (168/170), Δspeed −0.15 km/h (169/187) — the most-null paired read of the program.
  Centre-majority: Δbias −0.030 (30/59), Δspeed −3.05 (15/76) — the deepest centre
  slowdown yet (r29 h6→h7: −1.97). Outskirts-majority: Δbias +0.010 (138/111), Δspeed
  +1.05 (154/111)** — near-null, slightly UP: the outskirts already recover while the
  centre still deepens. Paired leg FAILS fleet-level → no booking; replay unnecessary.
- Feed (true feed speed): **h8 fleet 21.2 / centre 18.0 / out 22.9** (h7 full:
  21.1/18.3/22.9). Answer to the r29 question: the fleet peak floor is **flat at
  21.1–21.2** h7→h8 (vs Sunday h7 23.2), not deepening further — all remaining deepening
  is centre-only: **18.9 → 18.7 → 18.4 → 18.0, 4th consecutive read below the 19.2–19.6
  floor band**.
- Dwells (deep-peak build-up, honest gate): h8 uncensored **[12.1, 28.4] s/stop** — the
  weekday climb continues (h6 [10.8, 26.0] → h7 [11.3, 26.2] → h8 [12.1, 28.4]) but 18 s
  remains inside the bracket → **TOD_DWELL h8 = 1.0 provisional, no booking**. Zone
  split: centre [14.9, 35.4] vs out [10.8, 25.2].
- **VERDICT: TOD_PACE h8 weekday = 1.0 provisional (partial hour; completion with the
  08:32–09:00 tail + h9 next round). The r29 zonal verdict HOLDS at the deepest hour on
  the strongest evidence yet: fleet-level dead null, centre-only deepening. The weekday
  peak remains an R8/zone story, not a TOD_PACE story — TOD tables stay honestly
  neutral.**

### (б) R8 bands + zonal dossier (deep-peak additions)

- **h7 full censored MID centre/outskirts 109/86 s = x1.27** (n=553/966) — 23rd zonal
  reproduction, at band top. **h8: 106/87 = x1.22** (n=491/829) — 24th, inside band.
  Weekday day aggregate now: x1.31 dawn / x1.28 morning / x1.21 h6 / x1.27 h7 / x1.22 h8.
- **Centre dwell watch: h7 reads 109 — the CLOSEST approach yet to the 115 trigger**
  (h8: 106). Not triggered; if the PM peak crosses 115, zonal entry review fires.
- Zonal dossier additions (all same-direction): paired centre Δspeed **−3.05** (deepest);
  centre at-fix **−22.0** vs out −2.0 (r29: −15.5 — deepens); centre %ahead 33.7 vs out
  43.5; centre feed 18.0 sub-floor; centre dwell/stop [14.9, 35.4] vs out [10.8, 25.2].

### (в) Watches (peak readings)

- **Engine flatness on the deep peak: at-fix all p50 −2.0 FULL window (n=9538)**, second
  half −4.0; 5-min buckets −2…−9 with probe ΔdelayS −2…−4 (mechanism-consistent, no
  plateau) — **7th consecutive healthy-app read at target level**; prev=at_stop +0.0,
  prev=on_track −46 (designed drag), fresh(seed) −2.0, %ahead 40.5. The engine rides the
  deepest weekday hour with nothing to fix.
- **Mechanism 14th reproduction**: OLS **+4.35** m/ΔdelayS-s (n=3222; band +4.07…+4.78
  holds), bands monotonic (−113.5/−26/−2/+39/+108), at_stop control −79.
- **Steady day-fleet: −16.0 (n=333); by level [0,60) −14.0 (n=141), [60,∞) −11.0
  (n=131) — FIRST read back at the designed −10…−15 band edge** after four windows at
  −24…−30. The r27 "stable characteristic ~−24" classification weakens to
  window-dependent; passive watch unchanged (trigger all p50 < −20 never hit — all −2.0).
- **15t at-fix skew — 4th weekday reproduction**: −8.0/%ahead 37.0 full (n=4470), −12.0
  second half; sign persists, amplitude wanders −7…−14. Converged 15t bias 0.640 this
  window (t3rp/t3rplf now lowest at 0.610). Watch stands, no action.
- **52t (2nd sighting)**: 13 trams, at-fix −2.0/42.1 (n=385) healthy; static bias 0.680
  (h7) → 0.640 (h8).
- **R11: entry decisively unmet at the deep peak** — deep-recovery share (pinned def)
  **51.7%** (2262/4371; r29 53.6%), fleet feed 21.2 ≪ 23–24. Stays FILED/dormant.

### Applied (engine)

**Nothing — no gate passed** (h7/h8 both neutral on the double gate: static legs ON the
zone bases, paired leg fleet-null; TOD_DWELL brackets hold 18 s; the engine reads −2.0
through the deepest hour). TOD_PACE_TABLE / TOD_DWELL_TABLE untouched (all 1.0). No code
or tooling changes (existing scripts covered every cut; no replay — no candidate reached
it). jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### Open items after round 30 (handoff)

- **h8 completion** (08:32–09:00 tail) + **h9 peak-tail** next round → closes the morning
  block; same zone bases 0.630/0.640 (confirmed stable through 08:32). Watch: does the
  centre feed (18.0) turn back up as the peak releases, and does the outskirts paired
  +1.05 grow into an early-recovery signal?
- **R8 live gate-2: implementation spec SHIPPED below** — ready to hand to a separate
  implementation agent; targets recomputed this round (centre x1.30 / out x0.90 hold on
  the 5-window weekday aggregate).
- **19:00 boundary re-read tonight** (bases 0.630/0.640 → expect the reverse step at cap
  release; window 18:40–19:20).
- Watches carried: centre feed floor 18.0 (deepest; track h9 + midday recovery); 15t skew
  (4th repro); 52t; steady band-edge return (−16/−14 — window-dependence noted);
  mechanism band +4.07…+4.78; R11 dormant; TOD h23/h0 weekday finals (tonight, keep the
  host awake); %ahead-vs-device (device ride); R5 prior-0.66 (midday next — use the
  zone-resolved framing, fleet statics cross the 07:00 frame otherwise).

### R8 gate-2 implementation spec (live zonal-dwell A/B — for a separate implementation agent)

**Scope: calibration constants + dev-flag plumbing ONLY. No other engine behavior may
change. Flag default OFF; with the flag off the engine must be bit-identical (all 359
existing tests green untouched).**

**Purpose.** 24 consecutive zonal reproductions say centre stops cost ~x1.3 of the 18-s
default dwell and outskirts ~x0.9, while TOD (fleet-level) is honestly neutral. Today the
zonal residual is absorbed by paceBias and shows up as centre at-fix −22 m vs out −2.
Hypothesis: zonal DEFAULT dwell moves centre at-fix toward 0 with no fleet-level cost
(neutrality: 0.30×1.30 + 0.70×0.90 ≈ 1.02 at the measured centre stop-share 30.3–30.7%).

**Engine changes.**

1. `src/lib/engine/speedProfile.ts`
   - `export const ZONAL_DWELL_CENTRE = 1.30;` `export const ZONAL_DWELL_OUT = 0.90;`
     (place next to the TOD tables, ~lines 108–167). `CENTER_BBOX` is already exported
     (line 41).
   - `export function zonalDwellFactor(coord: [number, number]): number` — same bbox
     test as `zoneCapAt` (line 207); returns CENTRE inside, OUT outside.
   - Flag: `export const ZONAL_DWELL_AB = __DEV__ &&
     process.env.EXPO_PUBLIC_ZONAL_DWELL_AB === '1';` — `__DEV__` guard makes release/
     device builds structurally OFF; `EXPO_PUBLIC_*` precedent:
     `src/lib/golemio/client.ts:211`.
2. `src/lib/engine/tramSim.ts`
   - `dwellDurationMs(stop, nowMs?, zonalFactor?: number)` (line 292): apply the factor
     ONLY in the default branch — line 293 early-returns scheduled `stop.dwellSeconds`,
     which is NEVER scaled (same rule as TOD_DWELL). Default branch becomes
     `baseMs × todDwellFactor(nowMs) × (zonalFactor ?? 1)`.
   - All three call sites pass the treatment factor: line 373 (`applySnapshot` at_stop
     dwell), line 489 (schedule seeding), line 707 (tick stop arrival).
   - Treatment gating (helper in tramSim.ts): treated iff `ZONAL_DWELL_AB &&
     parseInt(sim.snapshot.key, 10) % 2 === 0` — **EVEN fleet numbers = treatment, ODD =
     control**, matching the analysis scripts' established parity split (`int(key) % 2`).
     Keys that don't parse to an integer (trip-id fallback keys) → control. Factor for a
     treated tram = `zonalDwellFactor(stop.coordinates)` (`RouteStop.coordinates` exists,
     `src/lib/types.ts:73`); untreated → 1.
3. Observability: log the flag state once at engine start (console). No motionlog schema
   change needed — parity is derivable from `key` downstream (established method).
4. Tests (new): bbox edges of `zonalDwellFactor`; `dwellDurationMs` scales the default
   branch only (scheduled dwell never scaled); parity gating incl. non-numeric keys;
   **flag OFF ⇒ bit-identical dwell durations**.

**Enabling (sim only).** Restart Metro as `EXPO_PUBLIC_ZONAL_DWELL_AB=1 npx expo start
--dev-client`, reload the app on the sim; verify the startup log line. Disable = unset
env + reload (no code revert). Never enable for a device build (double-guarded anyway).

**Targets (recomputed this round, weekday day aggregate, uncensored dwell/stop midpoints
÷ 18 s default).** centre measured x1.32 (dawn x1.275 / h6 x1.264 / h7 x1.339 / h8
x1.397) → **ship x1.30** (conservative); out measured x0.92 (x0.864–x1.000) → **ship
x0.90**. Shipped ratio 1.44 vs measured 1.43; fleet-neutrality 1.02.

**Protocol.** Duration **≥2 h weekday including ≥1 full peak hour** (pre-registered),
recommended peak + adjacent midday for contrast. Fresh app at window start (restart at a
boundary, honor the ±2-min re-seed exclusion; coordinate to avoid r28's double-restart);
NO restart inside the window. Analysis via the existing scripts — day_prior/tod_paired
already split by parity; plateau_diag needs a `--parity even|odd` filter added at
experiment time (regression must be bit-exact on default path).

**Success metrics (treated EVEN vs control ODD, same window).**
- Primary: centre-zone at-fix signed p50 — control expected ≈ −22 (this round's level);
  success = treated improves **≥ +5 m toward 0**, AND treated outskirts within ±3 m of
  control, AND treated fleet-level at-fix within −2 ± 3 m.
- Secondary: treated centre %ahead rises (33.7 → toward 40); treated centre dwell MID
  p50 up ~+15…25 s vs control (mechanical confirmation the treatment binds); teleport
  rate and hard-brake/crawl share within +20% of control.

**Rollback triggers (any one).** Treated centre at-fix WORSE than control by >5 m over 2
consecutive 30-min sub-windows; treated outskirts degrades >5 m vs control; teleports
+20% vs control; jest/tsc failure. Rollback = unset env + reload.

**After the experiment.** This supplies the live-evidence leg ONLY. Shipping zonal dwell
permanently (constants on by default, flag removed) still requires the standard double
gate + replay + full test suite — no shortcut.

## R8 gate-2 IMPLEMENTED + ENABLED (Mon 08:55 → 08:59; implementation agent)

**A/B ACTIVE (sim only): treatment=even regs, центр x1.30/out x0.90.**

### What shipped (code, flag default OFF — prod/device untouched)

- `src/lib/engine/speedProfile.ts`: `ZONAL_DWELL_CENTRE = 1.3` / `ZONAL_DWELL_OUT = 0.9`
  (next to the TOD tables), `zonalDwellFactor(coord)` (same inclusive bbox test as
  `zoneCapAt`), `ZONAL_DWELL_AB = __DEV__ && process.env.EXPO_PUBLIC_ZONAL_DWELL_AB
  === '1'` (double guard: release/device builds structurally OFF).
- `src/lib/engine/tramSim.ts`: `dwellDurationMs(stop, nowMs?, zonalFactor?)` — factor
  multiplies the DEFAULT branch only (scheduled `computed_dwell` early-return NEVER
  scaled, same rule as TOD_DWELL; composes `baseMs × todDwellFactor × factor`);
  `zonalDwellTreatmentFactor(sim, stop)` parity gate; all three call sites pass it
  (at_stop seeding, paceBias dwell deduction, tick stop arrival).
- **Parity gate detail (one deliberate deviation from the spec's literal formula):**
  treated iff the flag is ON and `key` is a STRICT integer string that is even
  (`String(parseInt(key)) === key`), not bare `parseInt(key) % 2` — Prague trip-id
  fallback keys are digit-prefixed (`991_…`), so bare parseInt would classify some of
  them by their numeric prefix while the analysis scripts' `int(key)` rejects them.
  Strict parse = engine treatment group ≡ scripts' parity split exactly; non-numeric
  keys → control, as the spec's intent states.
- `src/lib/engine/engine.ts`: one startup console line when (and only when) the flag is
  ON — the OFF path logs nothing and stays untouched. No motionlog schema change
  (parity derivable from `key` downstream, established method).
- Tests: `__tests__/zonal-dwell-ab.test.ts`, 23 new tests — bbox edges (+ sweep
  equivalence vs `zoneCapAt` zone classification); default-branch-only scaling;
  scheduled dwell untouched; parity gating incl. digit-prefixed fallback keys; **flag
  OFF ⇒ bit-identical 1400-tick [sM, vMs] trace** (tod-pace pattern: OFF(unset) ≡
  OFF('0') ≡ ON-control-odd `Object.is`-exact, ON-treated differs ⇒ hook live); ON
  end-to-end x1.30 centre / x0.90 outskirts through tick(); adaptive-dwell composition
  (extend caps at ZONAL base + 75 s; shorten trims the zonal base — treated/control
  ratio exactly x1.30; skip still skips); paceBias deduction uses the treated factor.
  ON variants load fresh module instances via `jest.isolateModules` + env var
  (babel-preset-expo does NOT inline `EXPO_PUBLIC_*` under jest — verified,
  metro-production-only).
- **jest 23 suites / 382 tests green (359 pre-existing untouched + 23 new);
  `npx tsc --noEmit` clean.**

### Enabling timeline (sim 2AB8E802 only; EXACT times, Mon 2026-07-13)

- 08:56:40 — old Metro (node 94557, unflagged) killed.
- 08:56:50 — Metro restarted with `EXPO_PUBLIC_ZONAL_DWELL_AB=1` (log `/tmp/metro-ab.log`,
  http 200 on :8081).
- 08:57:10 — previous app instance terminated (PID 46428, up since 06:45:04).
- **08:57:12 — fresh app launched (PID 63346) ← A/B ACTIVATION POINT. Re-seed exclusion
  ±2 min around the restart (pure-read exemption): quarantine 08:55–08:59.**
- 08:57:33 — bundle served (2015 modules); 08:57:35 — startup line verified in the Metro
  log: `[engine] R8 gate-2 zonal-dwell A/B ACTIVE (dev flag): treatment = even
  registration numbers, centre x1.3 / outskirts x0.9 (DEFAULT dwells only;
  odd/non-numeric keys = control)`.
- 08:57:53–08:58:44 — container motionlog confirmed alive and growing, v2 records
  (`obsAt`/`statePos`/`delayS`/`nextSeq` present), fresh `t`.
- Pipeline continuity: harvester PID 31497 alive (08:52 drain = the small live file,
  by design); NO restart planned inside the experiment window.

### For the next calibration rounds (read the A/B by parity split)

- Window: **≥2 h weekday incl. ≥1 full peak hour** from 08:57:12; recommended peak +
  adjacent midday for contrast. No app restart inside the window.
- **Success (treated EVEN vs control ODD, same window):** primary — treated centre
  at-fix improves **≥ +5 m toward 0** vs control (control expected ≈ −22), AND treated
  outskirts within ±3 m of control, AND treated fleet-level at-fix within −2 ± 3 m.
  Secondary — treated centre %ahead 33.7 → toward 40; treated centre dwell MID p50
  +15…25 s vs control (treatment binds); teleports and hard-brake/crawl within +20% of
  control.
- **Rollback (any one):** treated centre at-fix WORSE than control by >5 m over 2
  consecutive 30-min sub-windows; treated outskirts degrades >5 m vs control; teleports
  +20% vs control; jest/tsc failure. Rollback = unset env + reload (no code revert).
- Tooling note: day_prior/tod_paired already split by parity; plateau_diag still needs
  its `--parity even|odd` filter (add at experiment time, regression bit-exact on the
  default path).
- This is the live-evidence leg ONLY — permanent shipping still requires the standard
  double gate + replay + full suite.

### Program paused (orchestrator, ~09:2x)
Hourly rounds + harvester stopped by user request. A/B experiment left ACTIVE on the sim
(flag is sim-only; device builds structurally OFF) — measurement resumes with the next
round series. Next data source: USER LIVE RIDE RECORDINGS (device, ground truth) — see
ride-record v2 enrichment below.
