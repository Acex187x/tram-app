# Calibration analysis — 2026-07-13 (MONDAY — first weekday!)

Continues [analysis-2026-07-12.md](analysis-2026-07-12.md) (Sunday: rounds 0–23, full
résumé at its end). Record schema v2 (see plan.md). Sessions: sim-2026-07-13.jsonl
(rolled over automatically at midnight).

## Monday priorities (carried over + new)

1. **WEEKDAY PEAKS — the headline test of the whole TOD hypothesis.** Sunday measured all
   TOD buckets = 1.0, but Sunday peaks are weak by nature. Monday h7–h9 and h15–h18 with
   real commuter load + car traffic are the first true test: run the full double gate
   (norms vs 0.62-frame day-caps as established, paired within-tram tests, replay) —
   if weekday peaks read non-1.0 robustly, that's the program's first real TOD shipment.
   Morning boundary check 07:00 (mirror of the 19:00 cap-boundary artifact) pre-registered.
2. **R8 live gate-2** (zonal dwell, evidence complete 16×): parity A/B behind a dev flag
   per round-22 pre-registration — needs a weekday window ≥2 h incl. a peak, fresh app.
3. **R11 decisive**: entry condition (fleet ≥23–24 km/h or deep-recovery <52%) most likely
   fires in the weekday early night; steady −25 follow-up rides along.
4. **TOD h22/h23 finals** (carried from Sunday round 23).
5. **Device ride recording** (user, when possible) — ground truth outranks sim data.

## Ops rules (accumulated)

- Stream everything with --since; never whole-file passes over closed sessions (542 MB!).
- Exclude windows overlapping host load (local builds, heavy analysis) from probe-level readings.
- Prophylactic app restart at ~6 h uptime (uptime CPU-degradation class, see performance.md);
  restart at a window boundary and note the time.
- Post-19:00 / pre-07:00 norms live in the capless frame (vs 0.62), day norms vs 0.65–0.66.
- **HOST SLEEP (round 24): check `pmset -g log` at round start.** A closed-lid MacBook
  Maintenance-sleeps regardless of `caffeinate -s` (verified: machine re-slept 45 s after a
  live PreventSystemSleep assertion). During host sleep the sim+app are suspended; the file
  only gets 45-s DarkWake fragments every ~15 min. Windows overlapping host sleep: feed-side
  observables (delayS, obsDist) sampled inside a fragment are valid but sparse; ALL sim-side
  probes are contaminated — exclude. The harvester's `sleep 600` is suspended too (one cycle
  ran 2 h 11 m); a stale sim file with a live harvester PID = suspected host sleep, not a
  harvester crash.

## Rounds

## Round 24 (Mon 02:16 → 03:35, executed in wake-bursts; host-sleep incident round)

**Ops incident — clamshell sleep killed the night program.** Lid closed at 23:16:39 Sunday
(`Clamshell Sleep`, pmset log); from then until this round woke the machine at 02:16:40 the
host only DarkWoke for ~45 s every ~15 min. Consequences: h23 Sunday has 16 clean minutes
(23:00–23:16); h0–h2 Monday effectively don't exist (fragments); round 23's 23:49 reading was
itself a fragment. Recovery actions: harvest loop kicked (02:17, file was stale 2 h with a
suspended sleep-timer); `caffeinate -s -w <harvester>` started — **proved ineffective against
clamshell sleep** (machine re-slept 02:18:05), left running as it still prevents idle sleep
once the lid opens; manual drain + prophylactic app restart **03:25:26** (new PID 24460; old
app wall-uptime 6.8 h of which ~3 h suspended mid-run — restart also resets post-suspension
state). **USER ACTION REQUIRED: open the lid (or attach an external display) before ~06:30,
otherwise the headline weekday-peak program (h7–h9, Monday priority #1) collects nothing.**

Analysis salvaged from the Sunday tail (streamed via `tail -c 64M` extract, 21:18→23:49; h22
hour is 100% clean — sleep began 23:16):

- **TOD_PACE h22 = 1.0 FINAL (full hour 22:00–23:00, capless frame).** (1) norm vs 0.62
  **x1.016** FULL (130/115, 245 trams), halves x1.016/x1.032, parity x1.024/x1.016, 4-way
  x1.000–x1.048 — first hour reading at-or-above in every subsample, BUT round-22's
  first-33-min halves read x0.968/x0.984 below → straddle within the hour, magnitude ≤3.2%
  typical; likely coupled to the end-of-service delayS pulse (bias drifts up as the schedule
  slips, half B p50 0.640–0.650). (2) paired h21p→h22 (231 trams) Δbias **+0.010** (119/97),
  Δspeed **+0.36** — near-null, familiar zonal split (centre −0.020/−1.72 vs out
  +0.020/+0.47). (3) replay D52/D53 on the full-hour extract: both flat-to-worse (frs p50
  122/122 vs NEW 121) — gate record in replay.py. Triple gate unanimous: neutral.
- **TOD_DWELL h22 = 1.0 FINAL**: dwell/stop uncensored **[9.6, 24.2]** s (5664 stops), 18
  inside; stationary 11–29% of time.
- **R8 band RESTORED on the full hour — round-22 x1.17 caveat resolved as a half-window
  artifact.** Censored per-dwell MID p50 centre/outskirts **98/82 s = x1.20** (n=504/888),
  back inside x1.20–x1.27 (17th reproduction). Uncensored budgets centre [12.4, 31.5] vs out
  [8.4, 21.3] s/stop. Centre dwell watch: 98 < 115, zonal entry still not triggered.
- **TOD h23: NOT FINALIZABLE** (16 clean min of 60; host sleep). Clean-window reading
  (23:00–23:16, 163 trams, at-fix n=2132): norm vs 0.62 **x1.032** (94/69), halves
  x1.016/x1.032 — weak-up, direction consistent with the h22 half-B drift; day-fleet moving
  p50 21.2. Recorded as first reading; **stays 1.0**; full re-measure needs a lid-open night.
- **TOD h0 Monday: NO measurement** (≈5.5 clean min + fragments). First weekday-night h0
  remains unmeasured.
- **Late-night delayS slippage — RESOLVED (Sunday side): transient end-of-service pulse, not
  a night-transition ramp.** Day-fleet per-fix delayS p50 by 10-min bucket: 41 (22:00) → 51 →
  60 → **65 (22:30 peak)** → 64 → 59 → 55 → 46 (23:10) → 37 (23:40, sparse fragments). Round
  22 caught the rising edge only. Monday-night cleanliness check still open (blocked, no
  data).
- **R11 entry — threshold nominally crossed, decisive test still unpowered.** Window
  22:00–23:16 clean, fresh app: deep-recovery share ΔdelayS<−10 **47.4%** (2283/4814) — first
  reading below the 52% constant (round 22: 49.4%), BUT fleet true feed speed h22 **22.3**
  km/h (< 23–24) and the fast population is tiny: night lines h23 = **12 trams, p50 27.3
  km/h, bias p50 0.69, at-fix n=64 ≪ 300**. Mechanism 8th reproduction: OLS slope **+4.58**
  m/ΔdelayS-s (band +4.07…+4.59, top edge), bands monotonic (−107/−23/+6/+46/+105), at_stop
  control −69. Honest note: steady ΣΔdelayS∈[−5,+5) **−24.0** (n=384) — second consecutive
  reading below the predicted −10…−15 band (round 22: −25.0, n=252); probe-level all p50
  −2.0, review trigger (all < −20) not hit; now a named follow-up. Weekday-night R11 window
  = first lid-open night, target the 91–99 segment.
- Health/context: at-fix signed p50 **−2.0** across h22 + h23-clean (app fresh ~2 h) — night
  levels healthy; centre true-feed floor **19.2/19.3** (h22/h23) — no upward release through
  23:16 (23+ h); night-route share ~1% of records through 23:16. Replay gate re-validation
  on h22 full extract: OLD 118.8 → NEW 115.5 (**−2.8%, twenty-fifth**), frs p90 377.6 → 362.7
  (−4.0%).

**Applied (engine): nothing — no gate passed** (h22 finals are neutral verdicts; everything
else blocked on data). replay.py: comment-only gate-record note for the D52/D53 h22-final
rerun. jest + tsc green.

### Open items after round 24

- **BLOCKED ON LID: everything Monday.** Weekday peaks (priority #1, needs h7–h9), R8 live
  gate-2 (needs ≥2 h weekday incl. peak, fresh app), R11 weekday-night decisive, h0/h23
  weekday re-measures, Monday slippage cleanliness. Harvester + caffeinate + fresh app (PID
  24460) are standing by; pipeline resumes by itself once the lid opens.
- **Steady −24…−25 follow-up** (2 consecutive reads below the −10…−15 designed-drag band,
  n=384/252) — re-read on the first healthy weekday window; escalate only if probe-level
  all p50 also sinks (trigger < −20 stands).
- **R11 deep-recovery watch**: 52% constant broke (49.4% → 47.4% late-Sunday) — decide
  whether the entry condition means "weekday-night fleet" (original intent) explicitly when
  re-registering; don't fire on Sunday wind-down data.
- Carried: TOD h23 final + h0 first measure (weekday night, lid open); centre upward-release
  watch (floor 18.8–19.3, 23+ h); %ahead-vs-device (device ride); 19:00/07:00 cap-boundary
  methodology caveat; night fleet 91–99 first real measurement (still n≪gate).
