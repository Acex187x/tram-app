# Calibration analysis — night rounds, 2026-07-12

Continues `analysis-2026-07-11.md` (round 0 methodology/baselines + round 1
R1–R3 application). Night-focused: buckets 22–05, sim-collected data from the
continuously-running simulator build.

**Reproduce:**

```
python3 docs/calibration/night_profile.py docs/calibration/sim-sessions/sim-2026-07-12.jsonl \
    --since "2026-07-12 00:30" [--lines 91,...,99]
python3 docs/calibration/replay.py /tmp/night-window-0030.jsonl   # gate + TOD candidates
```

---

## Night round 0 — 2026-07-12 00:29 (`c918bf0`), recap

Fixed the harvester (cp-overwrite lost per-launch data → merge-dedupe),
authored `night_profile.py`, pinned the **pre-R1 baselines** used below (in
`night_profile.py`'s printed footers): device at-fix smooth-sim
signed p50 −38 / |e| p50 86 / p90 260 / %ahead 27.1; **fresh-sim (seed-bias)
subset: signed p50 +25.5, |e| p90 385, %ahead 56** (n=96). No engine change.
A dedicated round-0 doc was never written — this file is the running night
log from round 1 on.

## Night round 1 — 2026-07-12 01:11–01:55

### Pipeline health (runbook step 1)

- `harvest.sh` (merge-dedupe) alive since round 0; `harvest.log` ticking every
  10 min (23:33 → 01:35, monotone growth), `sim-2026-07-12.jsonl` 13.6 MB.
- App `cz.zabolotny.tramspotter` foregrounded on sim `2AB8E802`, Metro 8081
  `packager-status:running`. Nothing needed relaunching.
- Build verification from the data itself: since 00:30 the seed-bias value is
  0.62 on 6,312 records vs 15 at 1.00 → the sim runs the **R1–R3 build**
  (`0751de5`). Confirmed live, not assumed.

### Window & fleet (analysis only on data NEW since round 0)

`--since 2026-07-12 00:30` (round 0 committed 00:29): **00:30 → 01:35**,
65.8 min, 39,331 records, 107 trams; night routes 91–99 = 27,277 records
(69%) across **57 trams** (the rest are day lines finishing runs — excluded
from night conclusions). Buckets covered tonight: **h0, h1 only** (h22–23
predate the window; h2–5 not yet collected).

### Real night behavior (lines 91–99 only)

| h | trams | n_mov | p50 | p75 | p90 km/h | centre p50 | out p50 | at-fix n | ≥300 gate |
|---|---|---|---|---|---|---|---|---|---|
| 0 | 51 | 713 | 20.3 | 31.9 | 45.1 | 16.1 | 23.5 | 777 | yes |
| 1 | 56 | 1271 | 19.5 | 28.3 | 38.2 | 16.0 | 22.3 | 1341 | yes |

- **The "night running ~1.1–1.25" expectation is refuted for h0–h1.** Moving
  pace p50 ≈ 19.5–20.3 km/h — same league as the 23 h fleet (21.3 device),
  not faster. Centre p50 16 km/h < outside 22–24 (night network converges on
  the centre transfer hub with timed holds; geography split consistent with
  round 0).
- **Per-tram median converged paceBias = 0.62 in BOTH hours** (n=50/56 trams
  with ≥20 records; 50% of trams above, 50% below). That is EXACTLY the
  `PACE_BIAS_PRIOR` — the hour-of-day deviation the TOD entry would encode is
  **1.00**. The record-weighted p50 (0.59–0.60) sits lower only because slow
  trams contribute more records — a weighting artifact, not physics.
- Dwell: unobservable in this log (fixes only refresh on movement; 0% of
  scored intervals advance <10 m; no `obsAtMs`/stop table — round-1 §7 caveat
  stands). **No evidence basis for TOD_DWELL night entries.**
- paceBias clamp floor: ≤0.45 share is 4.1%/7.5% (h0/h1) — the 0.4 floor still
  binds mildly for the slowest tail (R4 remains deferred, unchanged evidence).

### R1–R3 build vs pre-R1 baseline (at-fix probe, live engine)

| | signed p50 | \|e\| p50 | \|e\| p90 | %ahead |
|---|---|---|---|---|
| pre-R1 device baseline (round 0) | −38 | 86 | 260 | 27.1 |
| pre-R1 device **fresh-sim** subset | **+25.5** | 101 | **385** | **56** |
| R1–R3 tonight, all (n=2744) | **−2.0** | **72** | **208** | 39.7 |
| R1–R3 tonight, **fresh (seed 0.62)** (n=162) | **−2.0** | 82.5 | **194** | **45.7** |
| R1–R3 tonight, converged (n=2582) | −2.0 | 71 | 209 | 39.3 |

**R1 is confirmed working in production:** the fresh-sim sprint signature is
gone (signed +25.5 → −2.0, |e| p90 385 → 194, %ahead 56 → 45.7); fresh sims
now perform at converged level from birth. Overall median |e| 86 → 72 and the
sim is median-unbiased (−2 m). Caveat: baseline is a device session, tonight
is sim data — but round 1 showed the two agree within ~1% on these metrics.

### Replay gate with TOD modeling (runbook step 5)

`replay.py` extended (this round): `tod` config key models `TOD_PACE_TABLE`
**with the updatePaceBias composition fix** — vTarget × tod(t) hour-blended
AND learning ratio ÷ tod(t) — so any future non-1.0 entry is gated by a
replay that actually models it. On the new window (86 trams):

| config | \|err\| p50 | \|err\| p90 | signed p50 | %ahead |
|---|---|---|---|---|
| OLD gate (pre-round-1) | 134.1 | 369 | +36 | 57.8% |
| NEW gate (R1+R3 shipped) | 128.6 | 357 | +8 | 51.8% |
| N1 tod h0 0.97 / h1 0.95 (record-wtd) | 127.1 | 355 | +6 | 51.2% |
| N2 tod h0=h1 = 1.15 (plan's night guess) | 128.2 | 361 | +18 | 53.7% |
| N3 tod h0=h1 = 0.95 | 127.0 | 355 | +6 | 51.2% |

- Shipped R1–R3 constants re-validate on tonight's data: OLD → NEW −4.1%
  median, fresh-sim p90 −1.6%, signed +36 → +8.
- **N2 (the planned ~1.15 night boost) is WORSE than neutral** (signed +18,
  %ahead 53.7) — the replay agrees with the live data: no night speed-up
  exists at h0–h1.
- N1/N3 shave ~1.2% off the coarse-replay median — **not shipped**, verdict:
  model artifact. The coarse replay is ahead-biased (+8 signed; no dwell
  model) where the live engine is unbiased (−2.0 signed, 39.7% ahead), so a
  global 5% slow-down flatters the replay while the live probe says it would
  push the median-unbiased engine behind reality. The honest estimator for a
  TOD entry (per-tram median bias vs prior) reads exactly **1.00** for both
  buckets. The ≥300-sample gate was met (777/1341); the evidence gate was not.

### Applied (engine)

**TOD tables: unchanged (all 1.0)** — for h0–h1 this is now a *measured*
value (deviation 1.00, n=50/56 trams, ≥300 at-fix samples each), no longer a
neutral placeholder. Table comments' "expect ~1.2 night" shape is refuted for
h0–h1; h22–23, h2–5 remain unmeasured.

**updatePaceBias × TOD composition fix (tramSim.ts)** — the prerequisite
flagged at round 1 for ever shipping a non-1.0 entry, landed now while the
tables are neutral (provably behavior-preserving: neutral factor is exactly
1.0, `expected × 1.0` bit-identical; full-suite green, replay identical by
construction). Expected speed is now `meanCruiseCapOver(..., V_CRUISE_REF_MS)
× todPaceFactor(interval midpoint)` — the same product `tick()` multiplies
`paceBias` into — so the bias learns only the per-vehicle residual and a
future TOD entry cannot double-apply. Unit tests (tod-pace.test.ts ×3):
residual learning under a uniform 0.5 table (ratio 0.9, not 0.45), midpoint
hour-blend evaluation (07:10–07:20 span → factor at 07:15), neutral-table
regression guard. Jest 22 suites / **354 tests** green; `npx tsc --noEmit`
clean.

### Open items for the next night round

- **h2–5 buckets**: harvester keeps collecting; re-run
  `night_profile.py --since "2026-07-12 01:35"` in the morning — each bucket
  needs its own ≥300 at-fix samples AND a per-tram bias deviation ≠1.00
  before a TOD entry moves. *(h2 closed by round 2 below; h3–5 still open.)*
- **h22–23**: only pre-round-0 data exists (pre-R1 mixture); needs a fresh
  evening collection on the R1–R3 build.
- %ahead 39.7 vs the 27% device design norm — sim-feed difference or drift?
  Re-check on the next device ride (device sessions outrank sim data).
- Prior 0.62 was learned under the old 13.9 reference; under the 11.7
  reference the equivalent fleet median should re-normalize to ~0.69 for the
  DAY fleet. Night fleet measures 0.62 exactly, so no change is warranted
  from tonight's data — but the first daytime round should re-measure the
  prior before trusting it.
- R7 (log `obsAtMs`/`target`/`teleport`) still open — dwell remains
  unobservable, at-fix probe still poll-quantized.

---

## Night round 2 — 2026-07-12 02:40–02:55

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079, merge-dedupe), `harvest.log` ticking every
  10 min through 02:35, `sim-2026-07-12.jsonl` 18.9 MB and growing.
- App `cz.zabolotny.tramspotter` foregrounded on sim `2AB8E802` (launchd PID
  56659), Metro 8081 `packager-status:running`. Nothing relaunched.

### Window (analysis only on data NEW since round 1)

`--since 2026-07-12 01:35`: **01:35 → 02:35**, 60.6 min, 28,336 records,
62 trams; night routes 91–99 = 27,594 records (**97%** — day lines have
finished; the cleanest night fleet so far). Buckets: h1 tail + **h2 (first
coverage)**.

### Real night behavior (lines 91–99)

| h | trams | n_mov | p50 | p75 | p90 km/h | centre p50 | out p50 | at-fix n | ≥300 gate |
|---|---|---|---|---|---|---|---|---|---|
| 1 (tail) | 60 | 822 | 20.5 | 28.7 | 40.2 | 16.1 | 23.9 | 859 | yes |
| 2 | 56 | 1184 | 19.9 | 27.3 | 38.0 | 16.5 | 22.3 | 1265 | **yes** |

- **h2 refutes the night speed-up too**: moving p50 19.9 km/h — identical
  league to h0–h1 (19.5–20.3) and the day fleet. Centre 16.5 < outside 22.3,
  same geography split as rounds 0–1. Three consecutive night buckets now
  measure flat ~20 km/h; the plan's "night 1.1–1.25" shape is dead for h0–h2.
- **Honest TOD estimator (per-tram median converged bias vs 0.62 prior)**:
  h1 = 0.630 (n=52 trams ≥20 rec; 28 above / 24 below), h2 = 0.635 (n=56;
  29/27) → implied deviations **1.016 / 1.024** — a coin flip around 1.00,
  well inside noise. Record-weighted p50 0.59–0.60 again sits lower (same
  slow-tram weighting artifact as round 1). clamp-floor share ≤0.45:
  5.3%/5.8% — R4 evidence unchanged, still deferred.
- Dwell still unobservable (0% dwell-ish intervals; R7 open) — no basis for
  TOD_DWELL entries.

### R1 continued validation + bias stability (at-fix probe, live engine)

| | signed p50 | \|e\| p50 | \|e\| p90 | %ahead |
|---|---|---|---|---|
| round-1 window, all (n=2744) | −2.0 | 72 | 208 | 39.7 |
| **round-2 window, all (n=2124)** | **−2.5** | **72** | **207** | 37.3 |
| round-1 fresh (seed 0.62) (n=162) | −2.0 | 82.5 | 194 | 45.7 |
| **round-2 fresh (n=116)** | **−2.0** | 60 | 182.5 | 47.4 |

- R1 holds on a fully disjoint window: fresh sims stay at converged level
  from birth (no sprint signature), overall metrics reproduce round 1 within
  ~1 m. The engine remains median-unbiased at night.
- Bias converged and stable: record-weighted p50 0.59–0.60 both hours, same
  as round 1 — no drift over 2 h of running.
- %ahead 37.3 (vs 39.7 round 1) — still above the 27% device norm; open item
  unchanged, needs a device ride.

### Replay gate (runbook step 5) — h2 candidates

`replay.py` night candidates updated for this window (N4–N6; 57 trams,
n_frs=142):

| config | \|err\| p50 | \|err\| p90 | signed p50 | %ahead |
|---|---|---|---|---|
| OLD gate (pre-round-1) | 123.0 | 335 | +36 | 58.1% |
| NEW gate (R1+R3 shipped) | 117.8 | 324 | +19 | 54.3% |
| N4 tod h1=1.016 h2=1.024 (measured) | 118 | 325 | +20 | 54.7% |
| N5 tod h1=h2=0.95 (control) | 117 | 323 | +16 | 53.6% |
| N6 tod h1=h2=1.15 (plan's guess) | 119 | 327 | +27 | 56.1% |

- Shipped constants re-validate a second time: OLD → NEW **−4.3%** median,
  fresh p90 **−19.1%**, signed +36 → +19.
- N4 (the measured deviations) is indistinguishable from neutral — confirms
  1.016/1.024 is noise, not signal.
- N5 shaves 0.8% — the same coarse-replay ahead-bias artifact as round-1
  N1/N3 (replay signed +19 where the live engine reads −2.5); NOT shipped,
  live at-fix metrics stay primary.
- N6 worse again — night boost refuted for h2 by both estimators.

### Applied (engine)

**Nothing.** h2 met the ≥300-sample gate (1265) but the evidence gate reads
1.00 (deviation 1.024, per-tram split 29/27); the live engine is already
median-unbiased at h2 (signed −2.0, |e| p50 69). TOD entries h0–h2 are now
all *measured* 1.00, not placeholders. Only `docs/calibration/replay.py`
candidates were updated (N1–N3 → N4–N6). Day buckets untouched.

Full suite anyway: jest 22 suites / 354 tests green; `npx tsc --noEmit`
clean.

### Open items after round 2

- **h3–5**: next window (`--since "2026-07-12 02:35"`) should cover h3 by
  ~04:00; same double gate (≥300 at-fix AND per-tram deviation ≠1.00).
  *(h3 closed by round 3 below; h4–5 still open.)*
- h22–23 fresh-build collection, %ahead-vs-device check, day-fleet prior
  re-measurement, R7 — all carried over unchanged from round 1.

---

## Night round 3 — 2026-07-12 03:11–03:45

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079), `harvest.log` ticking every 10 min through
  03:36, `sim-2026-07-12.jsonl` 23.7 MB and growing.
- App `cz.zabolotny.tramspotter` foregrounded on sim `2AB8E802` (same launchd
  PID 56659 as round 2 — no restarts all night), Metro 8081
  `packager-status:running`. Nothing relaunched.

### Window (analysis only on data NEW since round 2)

`--since 2026-07-12 02:35`: **02:35 → 03:35**, 60.9 min, 25,519 records,
57 trams; night routes 91–99 = **100%** of records. Buckets: h2 tail +
**h3 (first coverage)** — h3 matured earlier than the round-2 ~04:00
estimate (1092 at-fix samples by 03:35).

### Real night behavior (lines 91–99)

| h | trams | n_mov | p50 | p75 | p90 km/h | centre p50 | out p50 | at-fix n | ≥300 gate |
|---|---|---|---|---|---|---|---|---|---|
| 2 (tail) | 54 | 873 | 19.6 | 29.1 | 39.8 | 16.3 | 22.7 | 922 | yes |
| 3 | 57 | 1030 | 20.2 | 28.6 | 38.8 | 16.8 | 22.5 | 1092 | **yes** |

- **h3 refutes the night speed-up too**: moving p50 20.2 km/h — the fourth
  consecutive night bucket measuring flat ~20 km/h (h0 20.3, h1 19.5–20.5,
  h2 19.6–19.9, h3 20.2). Centre 16.8 < outside 22.5, same geography split
  as rounds 0–2. The plan's "night 1.1–1.25" shape is dead for h0–h3.
- **Honest TOD estimator (per-tram median converged bias vs 0.62 prior,
  trams ≥20 rec)**: h2 tail = 0.610 (n=51; 24 above / 26 below) → deviation
  **0.984**; h3 = 0.640 (n=56; 31/25) → deviation **1.032**. Both coin flips
  around 1.00, same noise band as rounds 1–2 (0.984–1.032 across all
  measurements). Note h2 measured 1.024 in round 2 and 0.984 tonight —
  the estimator itself brackets 1.00 across disjoint windows, which is
  exactly what "no TOD signal" looks like.
- Record-weighted bias p50 0.60 both hours (same slow-tram weighting
  artifact); clamp-floor share ≤0.45: 5.1%/5.2% — R4 evidence unchanged,
  still deferred.
- Dwell still unobservable (0% dwell-ish intervals; R7 open) — no basis for
  TOD_DWELL entries.

### R1 continued validation + bias stability (at-fix probe, live engine)

| | signed p50 | \|e\| p50 | \|e\| p90 | %ahead |
|---|---|---|---|---|
| round-1 window, all (n=2744) | −2.0 | 72 | 208 | 39.7 |
| round-2 window, all (n=2124) | −2.5 | 72 | 207 | 37.3 |
| **round-3 window, all (n=2014)** | **−2.0** | **67** | **205** | 38.7 |
| round-1 fresh (seed 0.62) (n=162) | −2.0 | 82.5 | 194 | 45.7 |
| round-2 fresh (n=116) | −2.0 | 60 | 182.5 | 47.4 |
| **round-3 fresh (n=118)** | −5.5 | **57.5** | **170** | 36.4 |

- R1 holds on a third disjoint window: fresh sims at converged level from
  birth (best fresh numbers of the night: |e| p50 57.5, p90 170 vs pre-R1
  device 101/385). Engine remains median-unbiased (−2.0).
- Bias converged and stable: record-weighted p50 0.60 both hours, unchanged
  through 3 h of running — no drift.
- %ahead 38.7 — in the 37–40 band of rounds 1–2, still above the 27% device
  norm; open item unchanged, needs a device ride.

### Replay gate (runbook step 5) — h3 candidates

`replay.py` night candidates updated for this window (N4–N6 → N7–N9;
57 trams, n_frs=140):

| config | \|err\| p50 | \|err\| p90 | signed p50 | %ahead |
|---|---|---|---|---|
| OLD gate (pre-round-1) | 123.4 | 353 | +26 | 56.5% |
| NEW gate (R1+R3 shipped) | 117.9 | 341 | +9 | 52.1% |
| N7 tod h2=0.984 h3=1.032 (measured) | 118 | 340 | +9 | 52.6% |
| N8 tod h2=h3=0.95 (control) | 117 | 340 | +5 | 51.4% |
| N9 tod h2=h3=1.15 (plan's guess) | 119 | 341 | +16 | 54.0% |

- Shipped constants re-validate a **third** time: OLD → NEW **−4.5%**
  median, fresh p90 **−8.7%**, signed +26 → +9.
- N7 (measured deviations) indistinguishable from neutral — 0.984/1.032 is
  noise, not signal.
- N8 shaves 0.8% — the same coarse-replay ahead-bias artifact as rounds 1–2
  (replay signed +9 where the live engine reads −2.0); NOT shipped, live
  at-fix metrics stay primary.
- N9 worse again — night boost refuted for h3 by both estimators.

### Applied (engine)

**Nothing.** h3 met the ≥300-sample gate (1092) but the evidence gate reads
1.00 (deviation 1.032, per-tram split 31/25); h2-tail re-measures 0.984
(24/26), re-confirming round 2's neutral verdict from a disjoint window.
The live engine is already median-unbiased at both hours (signed −2.0).
TOD entries **h0–h3 are now all *measured* 1.00**, not placeholders. Only
`docs/calibration/replay.py` candidates were updated (N4–N6 → N7–N9).
Day buckets untouched.

Full suite anyway: jest 22 suites / 354 tests green; `npx tsc --noEmit`
clean.

### Open items after round 3

- **h4–5**: next window (`--since "2026-07-12 03:35"`) should cover h4 by
  ~04:45–05:00; same double gate. Given four consecutive flat buckets, treat
  any non-1.00 h4–5 reading with extra suspicion (end-of-night depot runs
  may skew speeds).
- h22–23 fresh-build collection, %ahead-vs-device check, day-fleet prior
  re-measurement, R7 — all carried over unchanged from round 1.

---

## Night round 4 — 2026-07-12 04:39–04:55

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079), `harvest.log` ticking every 10 min through
  04:36, `sim-2026-07-12.jsonl` 29.9 MB and growing.
- App `cz.zabolotny.tramspotter` foregrounded on sim `2AB8E802` (same PID
  56659 since round 2 — no restarts all night), Metro 8081
  `packager-status:running`. Nothing relaunched.

### Window (analysis only on data NEW since round 3)

`--since 2026-07-12 03:35`: **03:35 → 04:36**, 61.1 min, 33,071 records,
89 trams; night routes 91–99 = 30,001 records (**91%** — the first day-line
trams appear late in the window, excluded from night conclusions as usual).
Buckets: h3 tail + **h4 (first coverage)** — h4 matured earlier than the
round-3 ~04:45–05:00 estimate (1415 night-line at-fix samples by 04:36).

### Real night behavior (lines 91–99)

| h | trams | n_mov | p50 | p75 | p90 km/h | centre p50 | out p50 | at-fix n | ≥300 gate |
|---|---|---|---|---|---|---|---|---|---|
| 3 (tail) | 52 | 839 | 19.7 | 29.3 | 39.3 | 16.5 | 22.9 | 886 | yes |
| 4 | 55 | 1336 | 20.5 | 28.9 | 39.4 | 16.7 | 23.4 | 1415 | **yes** |

- **h4 refutes the night speed-up too**: moving p50 20.5 km/h — the fifth
  consecutive night bucket measuring flat ~20 km/h (h0 20.3, h1 19.5–20.5,
  h2 19.6–19.9, h3 19.7–20.2, h4 20.5). Centre 16.7 < outside 23.4, same
  geography split as rounds 0–3. The plan's "night 1.1–1.25" shape is dead
  for the entire measured night h0–h4.
- **Honest TOD estimator (per-tram median converged bias vs 0.62 prior,
  trams ≥20 rec)**: h3 tail = 0.640 (n=51; 27 above / 24 below) → deviation
  **1.032** — reproduces round 3's h3 reading (1.032) exactly from a
  disjoint window; h4 = 0.640 (n=55; 30/25) → deviation **1.032**. Both
  inside the established noise band (0.984–1.040 across all rounds).
- **Depot filter (round-3 warning, h4 scrutiny)**: 9/56 night trams vanish
  from the feed >5 min before window end (trip terminating / depot run:
  8266, 8281, 8286, 8298, 8425, 8448, 8472, 8473, 8785). Excluding them,
  h4 reads deviation **1.016** (n=47; 25/23), moving p50 20.4 vs 20.5 full;
  the depot-suspect subset alone reads deviation 1.040, moving p50 20.7,
  at-fix signed −3.0. Full vs filtered vs suspects all agree within noise —
  **no depot effect detected at h4**; the non-warning reading needs no
  rescue, and the 1.032 is a coin flip either way.
- Record-weighted bias p50 0.59–0.61 (same slow-tram weighting artifact);
  clamp-floor share ≤0.45: 4.9%/5.0% — R4 evidence unchanged, still
  deferred.
- Dwell still unobservable (0% dwell-ish intervals; R7 open) — no basis for
  TOD_DWELL entries.

### R1 continued validation + bias stability (at-fix probe, live engine)

| | signed p50 | \|e\| p50 | \|e\| p90 | %ahead |
|---|---|---|---|---|
| round-1 window, all (n=2744) | −2.0 | 72 | 208 | 39.7 |
| round-2 window, all (n=2124) | −2.5 | 72 | 207 | 37.3 |
| round-3 window, all (n=2014) | −2.0 | 67 | 205 | 38.7 |
| **round-4 window, all (n=2301)** | **−2.0** | **73** | **204** | 38.7 |
| round-3 fresh (seed 0.62) (n=118) | −5.5 | 57.5 | 170 | 36.4 |
| **round-4 fresh (n=104)** | −2.0 | 68 | 196.3 | 41.3 |

- R1 holds on a fourth disjoint window: fresh sims at converged level from
  birth (|e| p50 68 / p90 196 vs pre-R1 device 101/385, no sprint
  signature). Engine remains median-unbiased (−2.0).
- Bias converged and stable: record-weighted p50 0.59–0.61, unchanged
  through 4 h of running — no drift.
- %ahead 38.7 — same 37–40 band as rounds 1–3, still above the 27% device
  norm; open item unchanged, needs a device ride.

### Replay gate (runbook step 5) — h4 candidates

`replay.py` night candidates updated for this window (N7–N9 → N10–N12;
82 trams, n_frs=206):

| config | \|err\| p50 | \|err\| p90 | signed p50 | %ahead |
|---|---|---|---|---|
| OLD gate (pre-round-1) | 123.7 | 338 | +32 | 57.4% |
| NEW gate (R1+R3 shipped) | 118.1 | 329 | +10 | 53.0% |
| N10 tod h3=1.032 h4=1.032 (measured) | 118 | 327 | +12 | 53.4% |
| N11 tod h3=h4=0.95 (control) | 117 | 328 | +7 | 52.2% |
| N12 tod h3=h4=1.15 (plan's guess) | 119 | 330 | +17 | 54.6% |

- Shipped constants re-validate a **fourth** time: OLD → NEW **−4.5%**
  median, fresh p90 −1.4%, signed +32 → +10.
- N10 (measured deviations) indistinguishable from neutral — 1.032/1.032 is
  noise, not signal.
- N11 shaves 0.8% — the same coarse-replay ahead-bias artifact as rounds
  1–3 (replay signed +10 where the live engine reads −2.0); NOT shipped,
  live at-fix metrics stay primary.
- N12 worse again — night boost refuted for h4 by both estimators.

### Applied (engine)

**Nothing.** h4 met the ≥300-sample gate (1415) but the evidence gate reads
1.00 (deviation 1.032 full / 1.016 depot-filtered, per-tram splits 30/25 and
25/23); h3-tail re-measures 1.032 (27/24), re-confirming round 3's neutral
verdict from a disjoint window. The depot-run concern from round 3 was
tested explicitly and found absent. The live engine is already
median-unbiased at both hours (signed −2.0). TOD entries **h0–h4 are now
all *measured* 1.00**, not placeholders. Only `docs/calibration/replay.py`
candidates were updated (N7–N9 → N10–N12). Day buckets untouched.

Full suite anyway: jest 22 suites / 354 tests green; `npx tsc --noEmit`
clean.

### Open items after round 4

- **h5**: next window (`--since "2026-07-12 04:36"`) should cover h5 by
  ~05:45–06:00; same double gate + depot/day-transition scrutiny (day lines
  ramp up through h5, so keep night conclusions on lines 91–99 only and
  rerun the depot filter).
- h22–23 fresh-build collection, %ahead-vs-device check, day-fleet prior
  re-measurement (day lines reappeared at h4 tail — a morning round could
  finally re-measure the prior under the 11.7 reference), R7 — all carried
  over unchanged from round 1.

---

## Night round 5 — 2026-07-12 05:39–05:55 (dawn transition)

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079), `harvest.log` ticking every 10 min through
  05:36, `sim-2026-07-12.jsonl` 43.9 MB and growing.
- App `cz.zabolotny.tramspotter` foregrounded on sim `2AB8E802` (same PID
  56659 since round 2 — no restarts all night), Metro 8081
  `packager-status:running`. Nothing relaunched.

### Window (analysis only on data NEW since round 4) — MIXED fleet

`--since 2026-07-12 04:36`: **04:36 → 05:36**, 59.8 min, 74,003 records,
**190 trams**. Night routes 91–99 = 17,323 records (**23%**, 47 trams, last
night record 05:28); day lines = 56,680 records (143 trams) — the morning
ramp-up. All analysis segmented night vs day from here on; 1 tram (8567)
appears on both (kept in the night segment).

### NIGHT segment (lines 91–99): h4 tail + early h5

| h | trams | n_mov | p50 | p75 | p90 km/h | centre p50 | out p50 | at-fix n | ≥300 gate |
|---|---|---|---|---|---|---|---|---|---|
| 4 (tail) | 47 | 822 | 20.6 | 29.7 | 40.8 | 18.5 | 22.3 | 844 | yes |
| 5 (early, →05:28 only) | 32 | 358 | 21.6 | 30.8 | 41.0 | 18.0 | 22.0 | 379 | **yes (barely)** |

- Moving p50 20.6 / 21.6 km/h — the flat ~20 km/h night band extends through
  the dawn tail (h0 20.3 … h4 20.5–20.6, early h5 21.6). No night speed-up
  at any measured hour, final.
- **Honest TOD estimator (per-tram median converged bias vs 0.62 prior,
  ≥20 rec)**: h4 tail = 0.655 (n=46; 27/19) → deviation **1.056**; early
  h5 = 0.670 (n=31; 21/10) → deviation **1.081** — the first readings
  outside the 0.984–1.040 noise band. NOT trusted, for four reasons:
  1. **Depot instability**: 34/47 night trams vanish >5 min before segment
     end (end of night service — expected at dawn). Subsets swing wildly:
     h4 filtered (13 trams) 0.952, suspects (33) 1.081; h5 filtered (13)
     1.145, suspects (18) 1.048. A real TOD signal would survive the split;
     this flips sign with it.
  2. **Truncated bucket**: night h5 is structurally a half-bucket — night
     service ends ~05:28, so 379 samples from 28 min of final legs/depot
     pull-ins is all a night h5 can ever contain. The h5 TOD entry belongs
     to the day fleet that dominates the hour.
  3. **Live engine already unbiased**: at-fix signed p50 −3.5 (h4) / −2.0
     (h5) on night lines — nothing to correct.
  4. **Replay gate**: N13 (measured 1.056/1.081) indistinguishable from
     neutral on both windows (night-only: |e| p50 127 vs 126, signed +10 vs
     +8; full: p50 identical, signed +7 vs +4); N15 (plan's 1.15) worse
     again (signed +14/+11). Round-3's dawn-scrutiny warning did its job.
- Record-weighted bias p50 0.63–0.66; clamp-floor ≤0.45 share 2.0–2.6% —
  R4 evidence unchanged, still deferred. Dwell still unobservable (R7 open).

### R1 continued validation (at-fix probe, live engine) — now on mixed fleet

| | signed p50 | \|e\| p50 | \|e\| p90 | %ahead |
|---|---|---|---|---|
| round-4 window, all night (n=2301) | −2.0 | 73 | 204 | 38.7 |
| **round-5 window, all lines (n=5559)** | **−5.0** | **72** | **214** | 37.0 |
| round-5 night only (n=1223) | −2.0 | 78 | 224 | 38.4 |
| round-5 day only (n=4326) | −5.0 | 71 | 209 | 36.2 |
| round-5 fresh (seed 0.62) (n=309) | −2.0 | 69 | 199 | 36.2 |

- **First R1 validation on day-line data**: fresh sims at converged level
  from birth on the morning ramp-up fleet too (signed −2.0, |e| p90 199 vs
  pre-R1 device fresh 385, no sprint signature). |e| p50 72 reproduces
  rounds 1–4 exactly on a 2.4× larger, day-dominated sample.
- Replay gate re-validates shipped constants a **fifth** time, first time
  day-dominated (full window, 187 trams: signed +22 → +4, %ahead 55.5 →
  50.9, |e| p90 334 → 327). Honest note: the replay *median* is flat this
  window (−0.1% full, +1.3% night-only) — the OLD-config advantage R1 eats
  was a night-fleet fresh-sim phenomenon; live at-fix stays primary and
  unambiguous.
- %ahead 37.0 — same 37–40 band; device-ride check still open.

### Morning observations, DAY fleet (observations ONLY — no day-bucket edits)

First morning measurement, for the day rounds to build on:

- **Convergence vs prior**: per-tram median converged bias (≥20 rec) =
  **0.680 at h4** (n=86 trams; 67 above prior / 19 below) and **0.660 at
  h5** (n=140; 91/49) → deviations vs the 0.62 prior **1.097 / 1.065**.
  The day fleet systematically converges ABOVE the prior — consistent with
  round-1's prediction that the fleet median re-normalizes to ~0.69 under
  the 11.7 reference (prior was learned as 0.62 under the old 13.9 ref).
  Reading: this looks like a **prior miscalibration for the day fleet**
  (~0.66–0.68 measured), not necessarily a dawn TOD effect — the two are
  not separable from a dawn tail. Day rounds must first re-measure the
  all-day prior, then TOD deviations against THAT.
- **Real speeds**: day h4 moving p50 22.3 km/h (per-tram median 22.8,
  n=62 trams), h5 21.6 (per-tram 21.6, n=124); p90 38–40. Implied pace vs
  the 42 km/h reference: 0.51–0.53. Early-morning caveat: first runs on
  empty streets — do NOT extrapolate to midday/peak.
- **At-fix (day only)**: h4 signed −10.0 / |e| p50 70 / %ahead 35.6;
  h5 signed −4.5 / 71 / 36.7 — engine mildly behind the morning fleet,
  consistent with the above-prior convergence; bias absorbs it within a
  few fixes (still-at-seed trams: 2 at h4, 0 at h5).
- Fleet size doubled within the hour (137 → 176 trams by h5); expect full
  day coverage by ~07:00 for a proper h6–h7 peak round.

### Applied (engine)

**Nothing.** Early h5 met the ≥300-sample gate (379, barely) but fails the
evidence gate on all four counts above (depot-unstable 1.081, truncated
bucket, live engine unbiased, replay-indistinguishable). TOD entries
**h0–h4 measured 1.00; h5 recorded as a structurally-truncated night
half-bucket** — its real owner is the day fleet. Day buckets untouched per
plan. Only `docs/calibration/replay.py` candidates were updated
(N10–N12 → N13–N15).

Full suite anyway: jest 22 suites / 354 tests green; `npx tsc --noEmit`
clean.

### Open items after round 5

- **Day rounds start**: first task = re-measure the all-day fleet prior
  under the 11.7 reference (dawn reading 0.66–0.68 vs 0.62 shipped; round-1
  prediction ~0.69). Only after that, per-hour day TOD deviations against
  the re-measured norm. Consider R5 (prior bump) as a candidate WITH full
  replay + live gates once a full-day sample exists.
- **h6–h7 peak round** ~07:00+ with the fleet at full strength.
- **h22–23**: still needs a fresh evening collection on the R1–R3 build
  (night service restarts ~00:00; day fleet covers 22–23).
- %ahead-vs-device check (37–40 sim band vs 27 device norm) — needs a
  device ride; R7 (obsAtMs/target/teleport logging) — dwell still
  unobservable. Both carried over.

---

## Day round 6 — 2026-07-12 06:11–06:45 (first full day round)

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079), `harvest.log` ticking every 10 min through
  06:26, `sim-2026-07-12.jsonl` 56 MB and growing.
- App `cz.zabolotny.tramspotter` foregrounded on sim `2AB8E802` (same PID
  56659 since round 2 — no restarts), Metro 8081 up. Nothing relaunched.

### Window (analysis only on data NEW since round 5) — pure day fleet

`--since 2026-07-12 05:36`: **05:36 → 06:36**, 60.0 min, 68,392 records,
**164 trams**, night routes = **0%** (night service ended 05:28; the "night
remnant" at the window start is empty — segmentation is trivially all-day).
New day-fleet artifact: 5 trams (1,083 records, 1.6%) log with
`simDist/lng/bias = null` (no sim spawned — no shape match); `night_profile.py`
at-fix/zone probes hardened against nulls (analysis-only change).

### MAIN TASK — all-day PACE_BIAS_PRIOR re-measurement (day fleet)

New rerunnable estimator `docs/calibration/day_prior.py`: per-tram MEDIAN
converged bias (bias ∉ {1.0, 0.62 seed}), tram filters **≥3 AVL fixes in
window AND sim age >5 min** (birth = first record in file; half-life 150 s ⇒
>2 half-lives), night lines excluded, then fleet median of per-tram medians.
Disjoint subsamples: window halves × fleet-number parity.

| subsample | trams | fleet p50 | above/below 0.62 | deviation |
|---|---|---|---|---|
| FULL window | 163 | 0.650 | 114/49 | x1.048 |
| half A (05:36–06:06) | 145 | 0.650 | 91/54 | x1.048 |
| half B (06:06–06:36) | 155 | 0.660 | 103/52 | x1.065 |
| even fleet numbers | 74 | 0.660 | 53/21 | x1.065 |
| odd fleet numbers | 89 | 0.650 | 61/28 | x1.048 |
| even × A / even × B | 66 / 72 | 0.655 / 0.660 | 45/21, 46/26 | x1.056 / x1.065 |
| odd × A / odd × B | 79 / 83 | 0.650 / 0.660 | 46/33, 57/26 | x1.048 / x1.065 |

- **The reading is stable**: all 8 disjoint subsamples on the same side of
  the prior (0.650–0.660, deviations x1.048–1.065, per-tram splits ~70/30 —
  no coin flip), and all 4 model families above too (15t 0.650, kt8d5 0.655,
  t3rplf 0.670, 14t 0.635). This confirms round-5's dawn read (0.66–0.68)
  at the low end and round-1's renormalization prediction directionally.
- **Convergence-drag check** (EWMA memory pulls young sims toward the seed,
  biasing the estimator DOWN): age-bucketed cohorts — the saturated 90+ min
  cohort (n=109) reads **0.660** (81/28); final-record snapshot of mature
  sims (age >30 min, ≥30 rec, n=140) reads **0.670**. True fleet value
  0.66–0.67; candidate prior = **0.66**.

### Replay gate (runbook step 5) — prior candidates → FAIL

N13–N15 → P16–P19 in `replay.py`; run on the window extract (163 trams,
n_frs=510, first day-dominated gate at this scale):

| config | \|err\| p50 | fresh p50 | fresh p90 | signed p50 | %ahead |
|---|---|---|---|---|---|
| OLD gate (pre-round-1) | 124.2 | 141 | 438 | +26 | 55.6% |
| NEW gate (R1+R3 shipped) | 122.4 | 139 | 412 | +8 | 51.6% |
| P16 prior 0.65 | 123 | 141 | 416 | +9 | 51.9% |
| **P17 prior 0.66 (candidate)** | 123 | 142 | 415 | +10 | 51.9% |
| P18 prior 0.67 | 123 | 142 | 414 | +10 | 52.0% |
| P19 prior 0.58 (control, wrong way) | 122 | 137 | 422 | +7 | 51.4% |

- Shipped constants re-validate a **sixth** time, first on a pure-day window
  (OLD → NEW −1.4% median, fresh p90 −6.0%, signed +26 → +8).
- **P17 improves neither gate metric** (median 122.4 → 123, fresh p90
  412 → 415); the wrong-way control P19 reads marginally "better" on
  fresh p50/signed — the same coarse-replay ahead-bias artifact documented
  every round (replay signed +8 where the live engine reads −5). Verdict:
  the replay gate is **insensitive to the prior at this dilution**
  (`inherit_bias` means the prior only seeds each tram's first segment) and
  what little it says points the artifact way. Gate: FAIL.
- **Live at-fix (primary)**: fresh sims at seed 0.62 are already
  median-unbiased — signed p50 **−2.0** (n=269, |e| p50 67, p90 202); the
  converged fleet reads −5.0/75/210. The pace controller's error feedback
  (factor = 1 + e/120) absorbs the seed-level difference — the same
  mechanism that neutralized every TOD candidate in rounds 1–5. There is no
  live error the prior bump would fix today.

### R1 continued validation (at-fix, live engine, first pure-day window)

signed p50 −5.0 / |e| p50 75 / p90 210 / %ahead 36.9 (n=5085) — |e| p50
within 3 m of rounds 1–5 on a day fleet 3× the night's; fresh(seed) −2.0.
No sprint signature. %ahead 36.9 — same 37–40 band; device-ride check open.

### TOD h6 (early-peak bucket)

At-fix n=3380 ≥300 (sample gate met), but no evidence gate: record-weighted
bias p50 0.65 and the per-tram estimator read exactly the prior-level
0.65–0.66 — indistinguishable from the fleet-wide prior miscalibration
(round-5's non-separability point stands, now measured). Real moving p50
21.0 km/h (h5 tail 21.5). Live h6 at-fix signed −5.0. No TOD entry; peak
7–9 rounds should measure deviations against the re-measured fleet norm
(0.66), not against 0.62.

### Applied (engine)

**Nothing.** The measurement half of the round-5 mandate is done — the
day-fleet prior is measured, stable, and documented (0.65–0.66 windowed,
0.66–0.67 saturated; candidate 0.66) — but the shipped rule is
measurement-stable AND replay-gate-pass AND live-at-fix-supported, and the
last two read no-signal/no-need. `PACE_BIAS_PRIOR` stays 0.62.
**Ship condition for R5 (prior 0.62 → 0.66)**, for a later day round:
(a) the all-day sample (incl. peak 7–9 + midday) keeps the per-tram
estimator ≥0.66 on disjoint subsamples, AND (b) a live signal appears that
the seed is costing accuracy — fresh(seed) at-fix signed p50 ≤ −10 m or
fresh |e| p90 degrading vs converged — or a replay formulation that
actually exercises the prior (fresh-segment-only scoring) improves.
Without (b) the change is cosmetically principled but empirically free ≈
unfalsifiable; don't ship those.

Changes this round: `day_prior.py` (new estimator), `night_profile.py`
null-hardening, `replay.py` candidates N13–N15 → P16–P19. Full suite
anyway: jest 22 suites / 354 tests green; `npx tsc --noEmit` clean.

### Open items after round 6

- **Peak round h7–h8** (~08:00+): first TOD bucket measured against the
  0.66 fleet norm; expect boarding+traffic effects — this is where the TOD
  table earns its keep if anywhere. ≥300 at-fix samples per bucket as usual.
- **R5 prior bump 0.62 → 0.66**: ship condition above (all-day stability +
  live signal). Keep accumulating per-round day_prior.py readings:
  06:36 → 0.650–0.660 (x1.048–1.065).
- h22–23 fresh-evening collection, %ahead-vs-device (37–40 vs 27 norm,
  needs device ride), R7 (obsAtMs/target/teleport logging; dwell still
  unobservable — 0.0% dwell-ish this window too) — carried over.

---

## Day round 7 — 2026-07-12 07:11–07:40 (morning peak onset, h7 first half)

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079), `harvest.log` ticking every 10 min through
  07:26, `sim-2026-07-12.jsonl` 66 MB and growing.
- App `cz.zabolotny.tramspotter` foregrounded on sim `2AB8E802` (same PID
  56659 since round 2 — no restarts), Metro 8081 `packager-status:running`.
  Nothing relaunched.

### Window (analysis only on data NEW since round 6) — pure day fleet

`--since 2026-07-12 06:36`: **06:36 → 07:26** at profile time (07:36 by the
prior/replay runs), 184–185 trams, ~66–74 k records, night routes 0%.

### (а) Peak onset h7 vs h6 — real speeds and bias

| h | trams | n_mov | p50 | p75 | p90 km/h | centre p50 | out p50 | at-fix n | signed p50 | rec-wt bias p50 |
|---|---|---|---|---|---|---|---|---|---|---|
| 6 (tail) | 165 | 1942 | 21.3 | 29.5 | 39.8 | 18.3 | 22.3 | 2024 | −9.0 | 0.65 |
| 7 (first half) | 178 | 2665 | 20.8 | 29.4 | 39.7 | 17.8 | 22.2 | 2763 | −7.0 | 0.68 |

- Fleet-level peak slowdown is **visible but tiny so far**: moving p50
  21.3 → 20.8 (−2.3%), and it is **centre-led** — centre −0.5 km/h
  (18.3 → 17.8), outskirts flat (22.3 → 22.2) — matching the
  peak-hits-centre expectation (б) in direction, not yet in magnitude.
  Context vs earlier morning: h5 centre was 19.0, so centre has shed
  ~1.2 km/h since dawn while outskirts shed ~0.8.
- The record-weighted bias climb 0.65 → 0.68 is **NOT a TOD signal**:
  paired within-tram test (141 trams present in both h6-tail and h7,
  day_prior filters) reads bias delta p50 **+0.010** (75/58 up/down),
  centre-majority trams exactly **0.000** (14/14), outskirts +0.020
  (61/44); paired moving-speed delta −0.73 km/h (67/74 — coin flip).
  The climb is fleet composition — kt8d5 converges at 0.740 (x1.121 vs
  0.66) and its share/maturity grows into the peak — plus new-tram influx.

### TOD h7 vs the 0.66 fleet norm (per round-6 mandate)

`day_prior.py --prior 0.66`, h7-only (07:00–07:26, 159 trams): FULL 0.680
(**x1.030**, 93/66); 8 disjoint subsamples 0.650–0.690 = x0.985–x1.045 —
**one 4-way cell flips below 1.00** (even×A 0.650), so the same-side rule
fails. h6-tail reads 0.640 (x0.970) — h6 and h7 straddle the norm. Sample
gate met (at-fix h7 n=2763 ≥ 300); evidence gate **FAIL** (mixed-sign
subsamples + paired-test null + live at-fix already fine at −7.0).

### Replay gate (runbook step 5) — P16–P19 → D20–D23

Run on the window extract (182 trams, n_frs=532):

| config | \|err\| p50 | fresh p50 | fresh p90 | signed p50 | %ahead |
|---|---|---|---|---|---|
| OLD gate (pre-round-1) | 130.1 | 128 | 415 | +34 | 57.4% |
| NEW gate (R1+R3 shipped) | 126.3 | 130 | 399 | +14 | 52.7% |
| D20 tod h7=1.03 (measured) | 127 | 131 | 398 | +15 | 53.1% |
| D21 tod h7=0.95 (control) | 126 | 128 | 401 | +11 | 52.3% |
| D22 prior 0.66 (R5 candidate) | 127 | 132 | 400 | +15 | 53.0% |
| D23 prior 0.58 (control, wrong way) | 126 | 128 | 395 | +12 | 52.5% |

- Shipped constants re-validate a **seventh** time, first on a peak window
  (OLD → NEW −2.9% median, fresh p90 −4.0%, signed +34 → +14).
- D20 indistinguishable from neutral; both wrong-way controls (D21/D23)
  read marginally "better" on signed — the documented coarse-replay
  ahead-bias artifact (replay signed +14 where the live engine reads −7).
  Gates: FAIL for D20 and D22 alike.

### (г) R5 evidence accumulation (prior 0.62 → 0.66)

- **(a) all-day stability**: this window vs 0.62 reads FULL **0.670
  (x1.081, 140/42)** — strongest reading yet; all subsamples above
  (x1.048–x1.097; running log: 06:36 → 0.650–0.660, 07:36 → 0.670).
  Vs the 0.66 candidate itself the full window reads 0.670 (91/89 —
  a coin flip), i.e. 0.66 remains a well-centred candidate.
- **(b) live signal**: still absent — fresh(seed 0.62) at-fix signed p50
  **−2.0** (n=289), fresh |e| p90 **189.8 vs converged 215.0** (fresh is
  *better*, not degrading); replay branch flat (D22 above). Condition (b)
  unmet → **PACE_BIAS_PRIOR stays 0.62**.

### (в) Dwell — first live attempt: NOT observable, root cause confirmed

Schema audit (`src/lib/feed/calibration.ts`): the record's `kmh` field is
**`s.simSpeedKmh`** — the sim's own speed, not the feed's; there is no
`obsAtMs` and no feed-reported speed in the passive log. Standing trams
emit no obsDist change, so dwells hide inside long inter-fix intervals
(fixIvl p50 55–60 s > dwell scale): dwell-ish share reads 0.0% again this
window. Real dwell (base 18 s × TOD_DWELL) is **structurally unobservable**
until R7 (obsAtMs/feed-speed logging) ships — which is an engine/logging
change, out of scope for calibration-constants rounds. No TOD_DWELL basis.

### R1 continued validation (at-fix, live engine, first peak window)

signed p50 −8.0 / |e| p50 75 / p90 214 / %ahead 37.0 (n=4787) — |e| p50
within 3 m of rounds 1–6; fresh(seed) −2.0/71/190, no sprint signature.
%ahead 37.0 — same 37–40 band; device-ride check still open.

### Applied (engine)

**Nothing.** h7 met the ≥300 sample gate but failed the evidence gate
against the 0.66 norm on all three prongs (mixed-sign disjoint subsamples,
paired within-tram null, replay-indistinguishable), and the live engine is
already mildly-behind-but-fine at −7. R5 ship condition: (a) accumulating
and stable, (b) still absent. Analysis-only changes:
`replay.py` candidates P16–P19 → D20–D23 + null-sim tram hardening (the
round-6 no-shape artifact reached the replay's zone lookup; trams with no
sim samples are now skipped).

jest 22 suites / 354 tests green; `npx tsc --noEmit` clean.

### Open items after round 7

- **Deep-peak round h7 second half + h8** (~08:30+): the centre-led
  slowdown just started (centre −0.5 km/h in h7's first 26 min); measure
  full h7 and h8 against the 0.66 norm once the window covers the true
  peak. Watch kt8d5 composition when interpreting record-weighted bias.
- **R5**: keep the per-round day_prior log (06:36 → 0.650–0.660, 07:36 →
  0.670); ship only when (b) appears — fresh(seed) signed ≤ −10, fresh p90
  degrading vs converged, or a prior-sensitive replay formulation improves.
- Midday rounds (h10–h14) for the all-day prior curve; h22–23 evening
  collection; %ahead-vs-device (37–40 vs 27, needs device ride); R7
  (obsAtMs/feed-speed logging — now confirmed the only route to dwell) —
  carried over.

## Day round 8 — 2026-07-12 08:11–08:37 (deep AM peak: h7 tail + h8; first v2 round)

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079), log ticking through 08:26; manual merge at
  08:36 for freshness → `sim-2026-07-12.jsonl` 462 k lines / 88 MB.
- App **restarted ~07:47 under the schema-v2 build** (new PID 13197), Metro
  8081 running. First v2 line 07:46:51; 74 107 v2 poll-lines by 08:36. v1/v2
  mix in one file — detected per-line by `obsAt` presence (plan.md).
- Restart consequence: all sims re-seeded at 07:47 → this window carries an
  unusually large fresh(seed) population (good live probe of the 0.62 seed)
  and half-A bias reads lean conservative (partially-converged sims pull
  toward the seed; they read 0.680 anyway, same as half B).

### Window (analysis on data NEW since round 7) — pure day fleet

`--since 2026-07-12 07:36` → 08:36 (59.6 min), 204 trams, 87.9 k records,
night routes 0%.

### (а) Deep-peak TOD signal — h8 vs h6-base

| h | trams | n_mov | p50 | p75 | p90 km/h | centre p50 | out p50 | at-fix n | signed p50 | \|e\| p50 |
|---|---|---|---|---|---|---|---|---|---|---|
| 7 (tail) | 185 | 2536 | 21.0 | 29.1 | 40.5 | 17.9 | 22.7 | 2641 | −4.0 | 75 |
| 8 | 203 | 3793 | 20.5 | 28.6 | 37.9 | 17.4 | 22.0 | 4054 | −2.0 | 70 |

- vs h6-base (21.3 / centre 18.3 / out 22.3): fleet **−3.8%**, centre
  **−0.9 km/h (−4.9%)**, outskirts −0.3 (−1.3%). The round-7 centre-led
  slowdown deepened slightly, and stayed centre-led.
- **Paired within-tram, h6→h8** (163 trams in both): Δspeed fleet −0.34
  (77/86 — null); **centre-majority −1.87 km/h (14/28)** vs outskirts +0.09
  (63/58). The slowdown is real but **centre-only**. Δbias reads +0.020
  (94/66) — *up*, against the slowdown: zone-mix contamination (peak = more
  time in the centre bbox; `updatePaceBias` learns v/cap against the 8.6 m/s
  centre cap → higher ratio at equal speed). Bias is not a clean TOD
  instrument across zone shifts; the paired speed test is.
- **Paired h7→h8** (183 trams): Δbias exactly **+0.000 (86/91)**; centre
  Δspeed −1.44 (16/33), outskirts **+0.92 (75/59)**.
- h8 vs the 0.66 norm (`day_prior --prior 0.66`, window): 0.680 **x1.030
  (122/81)**, and for the first time ALL 8 disjoint subsamples sit above
  (x1.015–x1.053). But the direction (bias UP in the peak) contradicts the
  measured slowdown, the paired test is null, and the same stable-above
  signal is exactly the R5 all-day drift — not an h8-specific deviation.
- Replay gate (D24/D25 added to `replay.py`, window extract 204 trams,
  n_frs=596): OLD 123.0 → NEW 119.4 (−3.0%, **eighth** re-validation);
  D24 tod h8=1.03 → 119 (indistinguishable); D25 wrong-way 0.95 → 119,
  signed +12 vs +15 (the documented ahead-bias artifact). Gate: **FAIL**.
- Live at-fix h8: signed **−2.0**, |e| p50 70, p90 207 (n=4054) — the best
  hourly reading of the program. Nothing to fix → **TOD_PACE h8 stays 1.0**.

### (б) v2 first light — real dwells + true feed speed (`dwell_v2.py`, NEW)

- **Real fix cadence** Δ(obsAt): p50 **46 s** (p10 24, p90 79) — this is the
  dwell detectability floor.
- **True feed speed** Δobs/Δ(obsAt), moving p50: h7 **23.2** / h8 **22.6**
  km/h (h8 centre 19.5, out 24.2). The poll-quantized method reads 21.0/20.5
  on the same windows → **poll speeds are ~9–10% low** (systematic, timing
  noise in the denominator). Hour-over-hour TOD comparisons are unaffected
  (same method both sides); absolute pace ratios were conservative.
- **Per-dwell detection** (flat obsDist ≥2 fixes + majority `at_stop` +
  bounded by moving fixes): n=**958** dwells vs 51 on_track traffic stops;
  duration bounds [lower p50 31 s, upper p50 147 s], MID p50 88 s. Only 958
  of **4371 stop visits** (ΔnextSeq) produced a detectable run (22%) — with
  cadence ≈ dwell scale the per-dwell median is censored UP massively and is
  NOT a population estimate.
- **Time-budget estimator** (uncensored: Σ stationary feed-time / stops
  passed via ΔnextSeq — matches the engine, which dwells at every stop):
  mean dwell per stop **h7 [9.0, 22.1] s, h8 [9.7, 24.4] s** (overall
  [8.9, 22.2]). The engine's 18 s default sits **inside the band**. Sample
  gate PASS (4371 ≥ 100), evidence gate **FAIL** (no resolvable deviation
  from 18 s) → **TOD_DWELL stays 1.0**. Centre dwells run longer than
  outskirts on the censored subset (MID p50 102 vs 82 s) — any future
  TOD_DWELL work should consider a zone split first.
- `statePos` in the wild: only `at_stop` (63% of v2 poll-lines) and
  `on_track` (37%).

### (в) R5 evidence accumulation (prior 0.62 → 0.66)

- vs 0.62: FULL **0.680 (x1.097, 177/26)**, all 8 subsamples x1.097–x1.113 —
  strongest reading yet. Running log: 06:36 → 0.650–0.660, 07:36 → 0.670,
  **08:36 → 0.680**.
- vs 0.66: x1.030 (122/81), first all-subsamples-above reading — but per (а)
  partly peak zone-mix; must re-read at midday before concluding 0.66 also
  undershoots.
- (b) live signal **still absent**: fresh(seed) signed p50 **−2.0** (n=550,
  the restart-enlarged population) vs converged −2.0; fresh |e| p90 220 vs
  206 (+7%, not the degradation signature; nowhere near the ≤ −10 ship
  trigger). Replay D22 flat (119 vs 119). → **PACE_BIAS_PRIOR stays 0.62**.

### R1 continued validation (at-fix, live engine, deep-peak window)

Window overall: signed p50 −2.0 / |e| p50 72 / p90 207 / %ahead 38.9
(n=6695) — |e| p50 within 3 m of rounds 1–7, %ahead in the same 37–40 band;
device-ride check still open. The 07:47 re-seed produced no sprint
signature (fresh %ahead 43.3 vs converged 38.6, signed identical).

### Applied (engine)

**Nothing.** Both double gates evaluated and failed on evidence:
TOD_PACE h8 (sample PASS n=4054; paired null + wrong-direction bias signal +
replay-indistinguishable + live at-fix −2.0), TOD_DWELL h7/h8 (sample PASS
4371 stop-visits; 18 s inside the uncensored [8.9, 24.4] band). Analysis
tooling shipped: **`dwell_v2.py`** (NEW — v2 fix-cadence / feed-speed /
per-dwell / time-budget analyzer), **`tod_paired.py`** (NEW — the paired
within-tram test as a rerunnable script, was round-7 ad-hoc), `replay.py`
D24/D25.

### Open items after round 8

- v2 keeps accruing: by midday the time-budget bands narrow — revisit
  TOD_DWELL with a full-day per-hour curve + zone split.
- Peak tail h9–h10: watch centre speeds recover (a cleaner TOD contrast than
  the peak itself, since h8 live errors are already −2 m).
- R5: midday reading needed to separate the morning 0.65→0.68 climb from the
  peak zone-mix effect; ship condition (b) still unmet.
- Consider migrating norm/speed measurements to Δ(obsAt) (v2) as v1 windows
  age out — poll-quantized levels are ~9–10% low.
- Carried: %ahead-vs-device (needs device ride), evening/night rounds.

## Day round 9 — 2026-07-12 08:36–09:36 (peak decay: h8 tail + h9; first all-v2 round)

### Pipeline health (runbook step 1)

- `harvest.sh` alive (PID 65079), log ticking through 09:27; manual merge at
  09:36 → `sim-2026-07-12.jsonl` 547 k lines. App PID 13197 (v2 build) still
  up, Metro 8081 up. No app restart this round — the fresh(seed) population
  is back to normal size (n=411).
- Window `--since 2026-07-12 08:36` → 09:36 (60.2 min), 218 trams, 84.6 k
  records, **100% v2 lines** (first round with no v1 in the window), night
  routes 0%.

### (а) Peak decay TOD signal — h9 vs h8 / h6-base

| h | trams | n_mov | poll p50 | feed p50 | feed centre p50 | feed out p50 | at-fix n | signed p50 | \|e\| p50 | p90 |
|---|---|---|---|---|---|---|---|---|---|---|
| 8 (tail) | 202 | 2120 | 21.2 | 22.5 | 19.7 | 24.0 | 2333 | −3.0 | 73 | 210 |
| 9 | 219 | 3905 | 20.4 | 22.5 | 19.8 | 24.2 | 4157 | −3.0 | 77 | 207 |

- **Feed speeds (v2, Δobs/Δ(obsAt)) are now the primary level metric** per
  round-8 finding; poll speeds again read ~9–10% low on the same windows
  (20.4 vs 22.5 h9) — the systematic offset reproduces exactly.
- **The expected post-peak recovery has NOT arrived by 09:36.** Feed speeds
  h8→h9 are flat (centre 19.7→19.8, out 24.0→24.2). Paired within-tram
  h8→h9 (200 trams): fleet Δspeed −0.25 (93/107 — null), Δbias +0.000
  (97/87 — null); **centre-majority −2.17 km/h (15/42)** vs outskirts +0.29
  (78/65) — the centre slowdown persists, mildly deepened, zone-confined.
- Paired h6→h9 (157 trams): Δspeed fleet −0.83 (59/98), centre −1.16,
  outskirts −0.63; Δbias +0.040 (102/50) — vs base the slowdown is still on,
  h9 is not yet post-peak. 09:11 "спад пика" was optimistic; recovery must be
  in h10+.
- Live at-fix h9: signed **−3.0**, |e| p50 77, p90 207 (n=4157) — engine
  already tracks the slower centre fine; nothing to correct.
- Replay gate (D26/D27 added to `replay.py`, window 218 trams, n_frs=612):
  OLD 129.5 → NEW 125.2 (**−3.3%, ninth re-validation**); D26 tod h9=1.02 →
  125/356 (indistinguishable from NEW); D27 wrong-way 0.95 → 125/355,
  signed +14 vs +17 (the documented ahead-bias artifact again). Gate:
  **FAIL** → **TOD_PACE h9 stays 1.0**.

### (б) Dwell time-budget dynamics (dwell_v2, v2 window 08:33–09:36)

- Fix cadence Δ(obsAt): p50 45 s (p10 23, p90 78) — unchanged floor.
- Per-dwell (censored): n=1116 dwells vs 49 traffic stops; MID p50 89 s.
  **Centre 101 vs outskirts 82 s** — the round-8 zone split (102/82)
  reproduces exactly.
- **Time-budget (uncensored)**: mean dwell/stop **h8 [9.8, 23.3] s,
  h9 [10.6, 25.8] s** (5295 stop-visits total). The predicted post-peak
  shrink of central standing did **not** materialize — h9's band is
  marginally *higher* than h8's (stationary 12–30% of time vs 12–28%),
  consistent with (а): h9 is still peak-like. Engine 18 s remains inside
  every band → sample gate PASS (5295 ≥ 300), evidence gate **FAIL** →
  **TOD_DWELL stays 1.0**. The stable centre/outskirts dwell split (101/82,
  two rounds running) is the strongest emerging dwell structure — zone
  split first if it survives midday.

### (в) R5 evidence (prior 0.62 → 0.66)

- vs 0.62: FULL **0.670 (x1.081, 179/37)**, all 8 subsamples above
  (x1.065–x1.113). Running log: 06:36 → 0.650–0.660, 07:36 → 0.670, 08:36 →
  0.680, **09:36 → 0.670** — first *retreat*, right as the zone-mix should
  start unwinding. Supports the peak-contamination reading of the 08:36
  0.680 peak; the underlying floor still looks ≥ 0.65.
- vs 0.66: x1.015 (111/103) and the round-8 "all 8 subsamples above" did
  **not** survive — half A x0.985, odd-parity x0.985, odd×time cells below.
  0.66 is back to statistically indistinguishable outside the peak.
- (b) live signal **still absent**: fresh(seed) signed p50 −2.0 (n=411) vs
  converged −3.0; fresh |e| p90 196 vs converged 208 — fresh sims read
  *better* than converged, the opposite of the degradation signature (ship
  trigger ≤ −10 nowhere near). Replay D22 (prior 0.66) flat at 125.
  → **PACE_BIAS_PRIOR stays 0.62**; the midday reading (h11–h13, minimal
  centre-peak contamination) remains the decisive point — do not ship on
  the morning climb alone.

### R1 continued validation (at-fix, live engine)

Window overall: signed p50 −3.0 / |e| p50 76 / p90 208 / %ahead 39.1
(n=6490) — |e| p50 within 3 m of rounds 1–8, %ahead in the same 37–40 band;
projSim |e| p50 75/p90 203 (marginally better than smooth, as always).
Device-ride %ahead check still open.

### Applied (engine)

**Nothing.** Both double gates evaluated and failed on evidence:
TOD_PACE h9 (sample PASS n=4157; paired fleet null + replay-indistinguishable
+ live at-fix −3.0 already healthy), TOD_DWELL h8/h9 (sample PASS 5295
stop-visits; 18 s inside [10.6, 25.8]). R5 unchanged (condition (b) absent;
09:36 read retreated to 0.670). Analysis tooling: `replay.py` D26/D27.

jest 22 suites / 354 tests green; `npx tsc --noEmit` clean.

### Open items after round 9

- **h10–h11 recovery round**: the centre slowdown is still fully on at
  09:36 — the *end* of it is now the cleanest TOD contrast available. Watch
  feed centre p50 climb back toward 20.5+ and the paired centre Δspeed flip
  sign.
- **R5 midday point** (h11–h13): decisive for 0.62→0.66; running log
  0.655→0.670→0.680→0.670.
- Dwell: keep the h-by-h time-budget curve; if centre/outskirts 101/82
  survives midday, evaluate a zone-split dwell (centre ×1.2-ish) BEFORE any
  TOD_DWELL hour shaping.
- Feed-speed (Δ(obsAt)) is now primary for levels; keep poll-based only for
  hour-over-hour deltas and v1-era comparisons.
- Carried: %ahead-vs-device (needs device ride), evening/night rounds.

## Day round 10 — 2026-07-12 10:11–10:45 (the NOON point: h9 tail + h10)

### Pipeline health — data-loss root cause found and fixed (harvester)

- `harvest.sh` was alive (PID 65079), log ticking, app PID 13197 (v2 build,
  no restart since 07:46), Metro 8081 up, feed healthy (staleness p50 ~37 s
  all window). BUT the container motionlog was being **evicted by the app
  itself**: `DIR_CAP_BYTES = 8 MB` (`src/lib/motionlog/core.ts`) —
  `enforceDirCap()` protects only the active *ride* file, so once the daily
  log passes 8 MB it deletes the ACTIVE daily log; everything since the
  previous harvest is lost. Holes since 08:17 (v2 log rate ~0.5 MB/min hits
  the cap every ~15–20 min): rounds 8–9 lost 1–2 min per ~20 min (~5–8%,
  medians unaffected); this round the phases drifted and lost 10:07:30–10:16
  and 10:27:26–10:32:32 (**~12.5 of 61 min**).
- **Fix (loop-owned tooling, not engine): `harvest.sh` now DRAINS the
  container file after each successful merge** (`rm` after `awk`-dedupe
  merge; `fs.append` recreates it on the next ~5 s flush), so it never
  reaches the cap. Old harvester killed, new one started (PID 31497); drain
  cycle verified live (container recreated at 183 KB, repo merged through
  10:44). App-side fix (protect the active daily log in `enforceDirCap`) is
  an app-code change — out of scope for calibration rounds, left as **R9**
  proposal. Holes are irrecoverable; all round-10 stats below are computed
  on the surviving ~48.5 min and none of the conclusions lean on the missing
  slices (per-10-min at-fix slices are consistent across the holes).

### Window (analysis on data NEW since round 9)

`--since 2026-07-12 09:36` → 10:37 (61.4 min wall, ~48.5 min surviving),
246 trams, 81.3 k day-line records, 100% v2, night routes 0%.

### (а) Peak decay — the recovery has STILL not arrived; sim flips to +1

| h | trams | n_mov | feed p50 | feed centre p50 | feed out p50 | at-fix n | signed p50 | \|e\| p50 | p90 |
|---|---|---|---|---|---|---|---|---|---|
| 9 (tail) | — | 2606 | 22.4 | 18.9 | 24.4 | 2837 | +0.0 | 82 | 225 |
| 10 | — | 3088 | 22.3 | 19.4 | 24.0 | 3813 | +2.0 | 82 | 226 |

- Feed centre p50 19.4 at h10 vs 19.5–19.8 through the peak — **flat**; the
  morning centre slowdown persists into h10. Paired within-tram h9→h10
  (224 trams): fleet Δspeed +0.22 (115/109 — null), Δbias −0.010 (94/122);
  **centre-majority −1.64 km/h (17/36)** vs outskirts **+1.03 (98/73)** —
  outskirts are recovering, the centre is not. h8→h10 (205): centre −1.58,
  out +0.02. The clean "end-of-peak" TOD contrast is still ahead (h11+).
- **The live at-fix flipped sign**: window signed p50 **+1.0**, %ahead
  **50.2** (first departure from the 37–40 band of rounds 1–9), |e| p50 82
  (band was 70–77), p90 225; projSim 65/141 (better, as always). Fresh-slice
  scan shows the drift is genuine (clean slices read −2…+4, around-hole
  slices +17…+22) — consistent with stationary share rising (h10 13–31%,
  the day's highest) and the centre still slow while sims keep pace. The sim
  is now median-unbiased at noon; nothing to correct, direction worth
  watching (an *ahead* drift argues against any prior/TOD increase).
- Replay gate (D28/D29 added, window extract 245 trams, n_frs=708):
  OLD 143.8 → NEW 135.7 (**−5.6%, tenth re-validation, strongest yet**);
  D28 tod h10=0.99 → 136/409 (indistinguishable); D29 wrong-way 1.05 →
  136/412 (marginally worse). Sample gate PASS (n=3813); evidence gate
  **FAIL** (paired fleet null + replay-flat + live at-fix +2.0) →
  **TOD_PACE h10 stays 1.0**.

### (б) R5 — THE DECISIVE NOON POINT: condition (a) MET, (b) absent & opposed

- vs 0.62: FULL **0.660 (x1.065, 177/63)**, ALL 8 disjoint subsamples above
  (0.660–0.680, x1.065–x1.097). Running log: 06:36 → 0.655, 07:36 → 0.670,
  08:36 → 0.680, 09:36 → 0.670, **10:37 → 0.660** — the peak bump unwound,
  the floor held at ≥ 0.66.
- **Zone-mix explanation ELIMINATED** (the round-9 question): per-tram
  zone-majority split of the same estimator — **outskirts-majority trams
  read the HIGHEST: 0.670 (x1.081, 148/45, n=193)** vs centre-majority
  0.650 (x1.048, 37/17, n=54); pure-outskirts (<10% centre time) 0.640
  (x1.032, 35/22, n=57). If the centre-cap ratio inflation drove the
  elevation, centre trams would read high — they read *lowest*. The
  elevation above 0.62 is fleet-wide physics, not peak geography.
- vs 0.66: FULL 0.650 **x0.985 (106/134)**, subsamples mixed (x0.970–x1.015)
  — 0.66 is statistically well-centred at noon; with pure-outskirts at 0.64
  the honest point estimate is **0.65–0.66**.
- **Verdict (a): MET.** The deviation vs 0.62 is same-side in every subsample
  of every round since 06:36, survives outside the peak, and is not
  attributable to zone-mix.
- **Verdict (b): still ABSENT — and at noon directionally OPPOSED.**
  fresh(seed 0.62) n=333: signed p50 **+8.0** (ahead of the feed, not
  behind), |e| p50 63 / p90 194 — *better* than converged (83/226). Raising
  the seed makes fresh sims faster → further ahead. Replay D22 flat
  (136 vs NEW 136). Ship triggers (fresh signed ≤ −10 / fresh p90 degrading
  / replay gain) all unmet → **PACE_BIAS_PRIOR stays 0.62**. R5 now waits
  purely on (b); if (b) never appears, the seed is simply not costing
  anything and R5 dies as unnecessary — re-check at the evening peak and
  once on a device ride.

### (в) Dwell zone split — THIRD reproduction; R8 zonal proposal (no code)

- Censored per-dwell MID p50 centre/outskirts: **106/86 s** (rounds 8/9/10:
  102/82, 101/82, 106/86 — ratio **x1.23** three rounds running, peak and
  noon alike → the structure is ZONAL, not hourly).
- NEW: **uncensored zone-split time-budget** — centre **[13.7, 32.0]** vs
  outskirts **[9.9, 23.2]** s/stop (1505/3394 stop-visits); bands separated,
  midpoints 22.9 vs 16.6 (x1.38). The engine's flat 18 s sits inside both
  bands but near the *top* of outskirts' and the *bottom* of centre's.
- Fleet-level TOD_DWELL: h9 [10.5, 24.7], h10 [11.7, 27.0] s — 18 s inside;
  sample gate PASS (5788 stop-visits ≥ 300), evidence gate FAIL →
  **TOD_DWELL stays 1.0**.
- **R8 proposal (future dwell-model change, NOT implemented, NOT a TOD
  table)**: split the default dwell by zone in `tramSim.ts` — stops WITHOUT
  a scheduled `computed_dwell` pick ~**21–22 s inside CENTER bbox / ~16–17 s
  outside** (fleet mean preserved: ~30% of stop-visits are central,
  0.3×21.5 + 0.7×16.5 ≈ 18.0 s). Zone test per stop coordinate vs the
  existing CENTER bbox. Gates before shipping: (1) the uncensored zone bands
  stay separated in an evening AND a night window (only daytime evidence so
  far); (2) replay cannot gate dwell (no stop model) → gate on live at-fix
  centre-subset signed error and teleport rate before/after; (3) never
  defeat the braking envelope. Expected live effect: slows central sims —
  the right direction given the noon +ahead drift.

### R1 continued validation (at-fix, live engine)

Window: signed p50 +1.0 / |e| p50 82 / p90 225 / %ahead 50.2 (n=6650) —
first round outside the 37–40 %ahead band and 3–5 m above the 70–77 |e| p50
band; flip is toward *unbiased* (see (а)), no sprint signature
(fresh 63/194 better than converged). Watch next round; device-ride %ahead
check still open.

### Applied (engine)

**Nothing.** Both double gates evaluated and failed on evidence: TOD_PACE
h10 (sample PASS n=3813; paired null + replay flat + live +2.0), TOD_DWELL
h9/h10 (sample PASS 5788; 18 s inside every band). R5: (a) formally
declared MET this round; (b) absent and sign-opposed → no ship. Tooling
shipped: `replay.py` D28/D29, **`harvest.sh` container-drain fix** (data-loss
root cause `DIR_CAP_BYTES` eviction; harvester restarted, PID 31497).

### Open items after round 10

- **R9 (app code, out of calibration scope)**: `enforceDirCap()` must never
  evict the active daily log (protect it like the active ride file, or evict
  oldest-first excluding today's). Until then the harvester drain masks it.
- **h11+ round**: catch the actual end of the centre slowdown (paired centre
  Δspeed flipping positive) — still the cleanest TOD_PACE contrast available.
- **R8 zonal dwell**: needs an evening + night zone-band reproduction, then
  the live-gate experiment described above.
- **R5**: waits purely on (b); re-evaluate at the evening peak (h15–18) and
  on the next device ride.
- Watch whether %ahead ~50 / |e| p50 ~82 persists at h11+ (new noon regime
  vs transient).
- Carried: %ahead-vs-device (needs device ride), evening/night rounds.

## Day round 11 — 2026-07-12 11:11–11:36 (midday: h10 tail + h11; first post-R9 round)

### Pipeline health — R9 VERIFIED, container log holes gone

- Harvester PID 31497 (drain version) alive, log ticking every ~10 min
  through 11:34; app PID 33035 (restarted ~10:55 with the R9 build), Metro
  8081 up. `sim-2026-07-12.jsonl` 747 k records through 11:34:37.
- **Continuity scan of the whole day**: 22 gaps > 20 s, ALL of them ≤ 10:54:35
  (the biggest — 10:07/10:27 — are round 10's known `DIR_CAP_BYTES` holes;
  the three 23–27 s blips at 10:51–10:54 are the R9 app restart itself).
  **After 10:54:35 → 11:34:37: zero gaps > 20 s.** First post-R9 interval is
  hole-free; R9 (protect the active daily log in `enforceDirCap`) + harvester
  drain together close the data-loss story. R9 drops off the open-items list.

### Window (analysis on data NEW since round 10)

`--since 2026-07-12 10:37` → 11:34 (57.6 min), 253 trams, 120.5 k day-line
records, 100% v2, night routes 0%.

### (а) THE MAIN QUESTION: the round-10 "+1.0 / 50.2% ahead" regime did NOT persist

- At-fix by hour: h10-tail signed p50 **−7.0** / %ahead **38.3** (n=3396),
  h11 **−2.0** / **39.4** (n=5477); window overall −3.0 / 38.9, |e| p50 76,
  p90 209 — **back inside the historical −2…−7 / 37–40 / 70–77 bands**.
  The round-10 excursion (+1.0 / 50.2 / 82) evaporated the moment the data
  holes did — consistent with round 10's own fresh-slice scan (clean slices
  −2…+4, hole-adjacent +17…+22). Verdict: **transient hole artifact, not a
  noon TOD structure**. The sim is NOT rushing at midday.
- Feed (v2): fleet p50 21.8/22.2 km/h (h10/h11), centre 19.5/19.0, out
  23.6/24.1 — centre still flat at the peak-era level.
- **Pre-registered candidate TOD_PACE h10–h11 < 1.0 (0.97) — double gate**:
  sample gate PASS (h11 n_fix 5477 ≥ 300). Evidence gate **FAIL** on all
  three legs: (1) live at-fix is *behind* (−2…−7), a 0.97 entry pushes it
  further behind; (2) paired h10→h11 fleet null (Δspeed −0.19, Δbias +0.000,
  123/107); (3) replay D30 (tod h11=0.97) **flat vs NEW** (118/311, fresh
  137/394 — identical), D31 wrong-way control 1.03 marginally worse
  (118/312, signed +18) — even the ahead-biased replay, which would flatter
  a <1.0 entry, shows nothing. **TOD_PACE h10/h11 stays 1.0; candidate dead.**
- Replay gate on the window extract (253 trams, n_frs=750): OLD 122.6 →
  NEW 118.0 (**−3.7%, eleventh re-validation**); fresh p90 404.8 → 393.9.

### (б) Centre slowdown — NOT recovered at h11

- Paired within-tram h10→h11 (247 trams): fleet Δspeed −0.19 (121/126 —
  null), Δbias +0.000 (123/107); **centre-majority −1.99 km/h (18/45)** vs
  outskirts **+0.69 (103/81)**. Same signature as h8→h10: outskirts
  recovering/flat, centre still sinking. Six consecutive hours of flat-or-
  falling centre feed (19.0–19.8 p50 since h7) now read less like "peak
  congestion" and more like an **all-day daytime centre structure** —
  the release point to catch is the evening (h19+), not h12. Live at-fix
  stays near-unbiased, so the engine's centre cap already absorbs it;
  nothing to correct.

### (в) R5 / R8 копилки

- **R5 vs 0.62**: FULL **0.660 (x1.065, 187/66)**, ALL 8 disjoint subsamples
  above (x1.065–x1.081). Running log: 06:36 → 0.655, 07:36 → 0.670, 08:36 →
  0.680, 09:36 → 0.670, 10:37 → 0.660, **11:34 → 0.660** — condition (a)
  keeps holding effortlessly. vs 0.66: FULL 0.650 x0.985 (105/148), all
  subsamples x0.970–x0.985 (slightly below) — honest point estimate still
  0.65–0.66.
- **R5 condition (b)**: fresh(seed) n=875 signed p50 **−3.0**, |e| p50 82 /
  p90 202 vs converged −3.0 / 76 / 209 — the round-10 "+8 ahead" opposition
  has SOFTENED to neutral (fresh p90 actually better than converged), but
  none of the ship triggers fire (fresh signed ≤ −10 / fresh p90 degrading /
  replay gain — D22 flat again, 118 vs 118). **PACE_BIAS_PRIOR stays 0.62**;
  (b) now genuinely rides on the evening peak (h15–18) and the next device
  ride. If the evening also reads neutral, R5 dies as planned.
- **R8 zonal dwell — FOURTH reproduction**: censored per-dwell MID p50
  centre/outskirts **103/82 s** (rounds 8/9/10/11: 102/82, 101/82, 106/86,
  103/82 — ratio x1.23–x1.26 across peak AND noon). Uncensored zone
  time-budget: centre **[14.6, 34.7]** vs out **[9.8, 23.3]** s/stop
  (1914/4235 stop-visits) — bands separated again, midpoints 24.7 vs 16.6.
  Engine flat 18 s: near the top of the outskirts band, near the bottom of
  centre's. Daytime evidence now saturated; **what R8 still needs is the
  evening AND night zone bands** (gate 1), then the live at-fix experiment
  (gate 2). No code this round.
- Fleet-level TOD_DWELL: h10 [11.1, 26.3], h11 [11.3, 27.2] s — 18 s inside;
  sample gate PASS (6149 stop-visits), evidence gate FAIL → **stays 1.0**.

### R1 continued validation (at-fix, live engine)

Window: signed p50 −3.0 / |e| p50 76 / p90 209 / %ahead 38.9 (n=8873),
projSim 77/204 — all bands restored after the round-10 hole-driven
excursion. fresh(seed) 82/202 vs converged 76/209: no sprint signature.

### Applied (engine)

**Nothing.** Both double gates evaluated and failed on evidence: TOD_PACE
h10/h11 (sample PASS; live at-fix behind-not-ahead + paired null + replay
D30 flat), TOD_DWELL h10/h11 (sample PASS; 18 s inside every band). R5 (b)
still unmet. Tooling: `replay.py` D30/D31 added (round-11 gate record).

### Open items after round 11

- ~~R9~~ **verified and closed** (zero post-restart holes).
- **Evening rounds (h15–18)**: R5 (b) decisive re-check; R8 evening zone
  band; the end-of-day centre-slowdown release (h19+) is now the main
  TOD_PACE contrast candidate.
- **R8**: needs evening + night zone-band reproduction, then the live gate.
- Watch: centre feed p50 flat 19.0–19.8 since h7 — if it persists to h14–15
  it is structural daytime, and the TOD story is zonal, not hourly.
- Carried: %ahead-vs-device (needs device ride), night rounds for TOD_DWELL.

## Day round 12 — 2026-07-12 12:11–12:40 (routine noon: h11 tail + h12)

### Pipeline health

- Harvester PID 31497 (drain version) alive, ticking every ~10 min through
  12:35; app PID 33035 (R9 build, no restart since ~10:55), Metro 8081 up,
  simulator booted. Continuity scan of the window: **zero gaps > 20 s** —
  second consecutive hole-free round; R9 + harvester drain keep holding.

### Window (analysis on data NEW since round 11)

`--since 2026-07-12 11:34` → 12:35 (61.1 min), 254 trams, 130.5 k day-line
records, 100% v2, night routes 0%.

### (а) TOD h12 — the expected quiet noon point; double gate evaluated, FAIL

- At-fix by hour: h11-tail signed p50 **−2.0** / %ahead **38.7** (n=4048),
  h12 **−2.0** / **38.8** (n=5629); window overall −2.0 / 38.8, |e| p50 75,
  p90 202 — squarely inside the historical −2…−7 / 37–40 / 70–77 bands.
  Round 11's verdict stands: the round-10 "+1/50%" excursion was a hole
  artifact; with holes gone the noon regime is boringly stable.
- Feed (v2): fleet p50 22.2/22.1 km/h (h11/h12), centre **19.0/19.1**, out
  23.8/24.0 — centre flat at the peak-era level for the SEVENTH consecutive
  hour (19.0–19.8 since h7).
- **TOD_PACE h12 double gate**: sample gate PASS (n_fix 5629 ≥ 300).
  Evidence gate **FAIL** on all three legs: (1) h12 vs the 0.66 norm reads
  x0.985 with mixed subsamples (x0.955–x1.015); (2) paired within-tram
  h11→h12 (253 trams) fleet null — Δspeed +0.12 km/h (128/125), Δbias
  +0.000 (125/115); (3) replay D32 (tod h12=0.99) **flat vs NEW**
  (116/305/frs 344 — identical), D33 wrong-way control 1.05 marginally
  worse (signed +16→+19). **TOD_PACE h12 stays 1.0** — as pre-registered,
  neutral.
- Replay gate on the window extract (254 trams, n_frs=792): OLD 119.4 →
  NEW 116.0 (**−2.9%, twelfth re-validation**); fresh p90 365.3 → 343.3
  (−6.0%).

### (б) Centre slowdown — h12 update: stopped sinking, still not released

- Paired h11→h12 centre-majority Δspeed **−0.37 km/h (29/39)** vs outskirts
  **+0.28 (99/86)** — the first round since h8 where the centre is no longer
  actively losing speed (−1.6…−2.0 in rounds 10–11), but the LEVEL stays
  down at 19.1 vs 23–24 outside. Consistent with the round-11 read: an
  all-day daytime centre structure, release expected h19+, and the engine's
  centre cap already absorbs it (centre-hour at-fix stays −2). Nothing to
  correct; the evening release remains the main TOD_PACE contrast candidate.

### (в) R5 / R8 копилки

- **R5 vs 0.62**: FULL **0.660 (x1.065, 192/62)**, ALL 8 disjoint subsamples
  above (x1.065–x1.081). Running log: 06:36 → 0.655, 07:36 → 0.670, 08:36 →
  0.680, 09:36 → 0.670, 10:37 → 0.660, 11:34 → 0.660, **12:35 → 0.660** —
  condition (a) rock-steady. vs 0.66: FULL 0.645 x0.977 (105/149),
  subsamples x0.955–x1.015 — honest point estimate still 0.65–0.66.
- **R5 condition (b)**: fresh(seed) n=592 signed p50 **−2.0**, |e| p50 74 /
  p90 187 vs converged −2.0 / 75 / 203 — fresh sims again neutral-to-better;
  no ship trigger (replay D22-class gain absent — window gate flat). 
  **PACE_BIAS_PRIOR stays 0.62**; (b) rides on the evening peak (h15–18)
  and the next device ride, as scheduled.
- **R8 zonal dwell — FIFTH reproduction**: censored per-dwell MID p50
  centre/outskirts **103/82 s** (rounds 8–12: 102/82, 101/82, 106/86,
  103/82, 103/82 — ratio x1.23–x1.26, five rounds across peak and noon).
  Uncensored zone time-budget: centre **[14.3, 33.9]** vs out
  **[9.9, 23.3]** s/stop (2059/4511 stop-visits) — bands separated again,
  midpoints 24.1 vs 16.6. Daytime evidence saturated; **still waiting on
  the evening AND night zone bands** (gate 1) before the live experiment
  (gate 2). No code.
- Fleet-level TOD_DWELL: h11 [11.6, 27.0], h12 [11.0, 26.4] s — 18 s inside;
  sample gate PASS (6570 stop-visits), evidence gate FAIL → **stays 1.0**.

### R1 continued validation (at-fix, live engine)

Window: signed p50 −2.0 / |e| p50 75 / p90 202 / %ahead 38.8 (n=9677),
projSim 75/203 — all bands nominal. fresh(seed) 74/187 vs converged 75/203:
no sprint signature.

### Applied (engine)

**Nothing — a quiet round, exactly as expected at noon.** Both double gates
evaluated and failed on evidence: TOD_PACE h12 (sample PASS n=5629; norm-
deviation mixed + paired null + replay D32 flat), TOD_DWELL h11/h12 (sample
PASS; 18 s inside every band). R5 (b) still unmet. Tooling: `replay.py`
D32/D33 added (round-12 gate record).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### Open items after round 12

- **Evening rounds (h15–18)**: R5 (b) decisive re-check; R8 evening zone
  band; centre-slowdown release (h19+) — the main TOD_PACE contrast.
- **R8**: needs evening + night zone-band reproduction, then the live gate.
- Centre feed p50 flat 19.0–19.8 for 7 hours (h7–h12) — one or two more
  flat midday reads and the daytime-centre story is formally structural
  (zonal, not hourly); the engine's centre cap covers it either way.
- Carried: %ahead-vs-device (needs device ride), night rounds for TOD_DWELL.

## Day round 13 — 2026-07-12 13:11–13:36 (routine early afternoon: h12 tail + h13)

### Pipeline health

- Harvester PID 31497 (drain version) alive, ticking every ~10 min through
  13:25; app PID 33035 (R9 build, no restart since ~10:55), Metro 8081 up,
  simulator booted. Continuity scan of the window: **zero gaps > 20 s** —
  third consecutive hole-free round.

### Window (analysis on data NEW since round 12)

`--since 2026-07-12 12:35` → 13:35 (60.6 min), 254 trams, 129.7 k day-line
records, 100% v2, night routes 0%.

### (а) TOD h13 — pre-registered neutral afternoon point; double gate evaluated, FAIL

- At-fix by hour: h12-tail signed p50 **−2.0** / %ahead **39.0** (n=3852),
  h13 **−5.0** / **37.9** (n=5710); window overall −4.0 / 38.4, |e| p50 76,
  p90 207 — squarely inside the historical −2…−7 / 37–40 / 70–77 bands.
- Feed (v2): fleet p50 22.0/22.2 km/h (h12/h13), centre **18.9/19.2**, out
  23.8/23.8 — centre flat at the peak-era level for the EIGHTH consecutive
  hour (18.9–19.8 since h7). Per the round-12 pre-registration ("one or two
  more flat midday reads"), the daytime-centre story is now **formally
  structural: zonal, not hourly** — the engine's centre cap covers it
  (centre-hour at-fix stays −2…−5); the remaining TOD question for the
  centre is the evening release (h19+).
- **TOD_PACE h13 double gate**: sample gate PASS (n_fix 5710 ≥ 300).
  Evidence gate **FAIL** on all three legs: (1) h13 vs the 0.66 norm reads
  x0.985 with mixed subsamples (x0.970–x1.015); (2) paired within-tram
  h12→h13 (253 trams) fleet near-null — Δspeed +0.55 km/h (140/113), Δbias
  +0.010 (133/106); (3) replay D34 (tod h13=0.99) **flat vs NEW**
  (119/312/frs 369 — identical, signed 19 vs 20), D35 wrong-way control
  1.05 marginally worse (119/313, signed +21, frs p90 371).
  **TOD_PACE h13 stays 1.0** — as pre-registered, neutral.
- Replay gate on the window extract (254 trams, n_frs=780): OLD 122.8 →
  NEW 119.3 (**−2.9%, thirteenth re-validation**); fresh p90 384.4 → 369.1
  (−4.0%).

### (б) Centre slowdown — h13: level unchanged, zonal split persists

- Paired h12→h13 centre-majority Δspeed **−0.99 km/h (25/43)** vs outskirts
  **+1.70 (115/70)** — the outskirts tick up while the centre stays pinned
  at ~19; same signature as every round since h8. With the structural
  verdict above, this stops being a per-round question: nothing to correct
  hourly, the release point to catch is h19+.

### (в) R5 / R8 копилки

- **R5 vs 0.62**: FULL **0.660 (x1.065, 195/59)**, ALL 8 disjoint subsamples
  above (x1.048–x1.081). Running log: 06:36 → 0.655, 07:36 → 0.670, 08:36 →
  0.680, 09:36 → 0.670, 10:37 → 0.660, 11:34 → 0.660, 12:35 → 0.660,
  **13:35 → 0.660** — condition (a) rock-steady. vs 0.66: FULL 0.650 x0.985
  (110/144), subsamples x0.970–x1.015 — honest point estimate still
  0.65–0.66.
- **R5 condition (b)**: fresh(seed) n=566 signed p50 **−2.0**, |e| p50 74 /
  p90 195 vs converged −4.0 / 76 / 207 — fresh sims again neutral-to-better;
  no ship trigger. **PACE_BIAS_PRIOR stays 0.62**; (b) rides on the evening
  peak (h15–18) and the next device ride, as scheduled.
- **R8 zonal dwell — SIXTH reproduction**: censored per-dwell MID p50
  centre/outskirts **102/85 s** (rounds 8–13: 102/82, 101/82, 106/86,
  103/82, 103/82, 102/85 — ratio x1.20–x1.26, six rounds across peak, noon
  and early afternoon). Uncensored zone time-budget: centre
  **[14.7, 34.7]** vs out **[9.8, 23.3]** s/stop (2050/4450 stop-visits) —
  bands separated again, midpoints 24.7 vs 16.6. Daytime evidence saturated;
  **still waiting on the evening AND night zone bands** (gate 1) before the
  live experiment (gate 2). No code.
- Fleet-level TOD_DWELL: h12 [11.4, 26.8], h13 [11.3, 27.0] s — 18 s inside;
  sample gate PASS (6500 stop-visits), evidence gate FAIL → **stays 1.0**.

### R1 continued validation (at-fix, live engine)

Window: signed p50 −4.0 / |e| p50 76 / p90 207 / %ahead 38.4 (n=9562),
projSim 74/203 — all bands nominal. fresh(seed) 74/195 vs converged 76/207:
no sprint signature.

### Applied (engine)

**Nothing — a quiet round, exactly as expected at h13.** Both double gates
evaluated and failed on evidence: TOD_PACE h13 (sample PASS n=5710; norm-
deviation mixed + paired near-null + replay D34 flat), TOD_DWELL h12/h13
(sample PASS; 18 s inside every band). R5 (b) still unmet. Tooling:
`replay.py` D34/D35 added (round-13 gate record).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### Open items after round 13

- **Evening rounds (h15–18)**: R5 (b) decisive re-check; R8 evening zone
  band; centre-slowdown release (h19+) — the main TOD_PACE contrast.
- **R8**: needs evening + night zone-band reproduction, then the live gate.
- Daytime-centre slowdown now **formally structural** (8 flat hours h7–h13,
  18.9–19.8 p50); the per-round watch item retires — what remains is
  catching the h19+ release.
- Carried: %ahead-vs-device (needs device ride), night rounds for TOD_DWELL.

## Day round 14 — 2026-07-12 14:11–14:30 (routine pre-peak: h13 tail + h14; last before the evening peak)

### Pipeline health

- Harvester PID 31497 (drain version) alive, ticking every ~10 min through
  14:26 (260 M drained); app PID 33035 (R9 build, no restart since ~10:55),
  Metro 8081 up, simulator booted. Continuity scan of the window: **zero
  gaps > 20 s** — fourth consecutive hole-free round.

### Window (analysis on data NEW since round 13)

`--since 2026-07-12 13:35` → 14:26 (51.0 min), 254 trams, 109.6 k day-line
records, 100% v2, night routes 0%.

### (а) TOD h14 — pre-registered neutral point with pre-peak watch; double gate evaluated, FAIL

- At-fix by hour: h13-tail signed p50 **−4.0** / %ahead **38.9** (n=3924),
  h14 **−8.0** / **37.2** (n=4140); window overall −6.0 / 38.0, |e| p50 77,
  p90 210 — h14 sits at the *behind* edge of the historical −2…−7 band
  (most-behind hourly reading of the day), everything else inside.
- Feed (v2): fleet p50 22.0/22.2 km/h (h13/h14), centre **19.1/19.1**, out
  23.7/23.9 — centre flat at the peak-era level for the NINTH consecutive
  hour (18.9–19.8 since h7); structural verdict from round 13 unchanged.
- **TOD_PACE h14 double gate**: sample gate PASS (n_fix 4140 ≥ 300).
  Evidence gate **FAIL** on all three legs: (1) h14 vs the 0.66 norm reads
  x0.985 with all subsamples x0.970–x0.985 — the same level h12/h13 read,
  not an h14 deviation; (2) paired within-tram h13→h14 (242 trams) fleet
  near-null — Δspeed +0.28 km/h (130/112), Δbias −0.010 (104/125); (3)
  replay D36 (tod h14=0.99) **flat vs NEW** (118/318/frs 384 — signed 17
  vs 18), D37 wrong-way control 1.05 marginally worse (119 p90 319, signed
  +20). **TOD_PACE h14 stays 1.0** — as pre-registered, neutral.
- **Early pre-peak precursors (flagged separately, per plan — NOT gate
  evidence yet)**: (i) paired centre-majority Δspeed **−3.19 km/h (16/48)**
  vs outskirts **+1.59 (114/64)** — the strongest centre downtick of the
  day (rounds 11–13 read −0.4…−2.0); (ii) at-fix h14 signed −8.0 — band
  edge, behind side; (iii) per-record centre at-fix −23.5 vs −19/−20 in
  h13 and the round-13 window (NEW metric this round; the −19…−23 level is
  the normal daytime-centre baseline per the 12:35–13:35 re-read, so only
  the *tick* is the signal, not the level). All three point the same way:
  the evening slowdown may be starting in the centre. h15–16 decides.
- Replay gate on the window extract (254 trams, n_frs=716): OLD 122.1 →
  NEW 117.8 (**−3.5%, fourteenth re-validation**); fresh p90 388.8 → 383.2.

### (б) Centre slowdown — h14: level flat, paired downtick (see precursors above)

- Level pinned at 19.1 both hours; the paired −3.19 is the first
  acceleration of the centre *decline* since the morning peak decayed.
  Structural zonal verdict stands; what changes at h15+ is whether a
  *deepening* below the 18.9–19.8 corridor appears (that would be the
  TOD_PACE evening signal, distinct from the zonal cap story).

### (в) R5 / R8 копилки

- **R5 vs 0.62**: FULL **0.660 (x1.065, 184/70)**, ALL 8 disjoint subsamples
  above (x1.048–x1.081). Running log: 06:36 → 0.655, 07:36 → 0.670, 08:36 →
  0.680, 09:36 → 0.670, 10:37 → 0.660, 11:34 → 0.660, 12:35 → 0.660,
  13:35 → 0.660, **14:26 → 0.660** — condition (a) rock-steady, ninth
  reading. vs 0.66: FULL 0.650 x0.985 (105/148), subsamples x0.970–x0.985
  (all slightly below this round) — honest point estimate still 0.65–0.66.
- **R5 condition (b)**: fresh(seed) n=512 signed p50 **−2.0**, |e| p50 63 /
  p90 193 vs converged −7.0 / 78 / 210 — fresh sims clearly better than
  converged this round; replay D22 (prior 0.66) flat-to-worse (118/320 vs
  118/318). No ship trigger. **PACE_BIAS_PRIOR stays 0.62**; (b) decided
  by the evening peak (next rounds) and the next device ride.
- **R8 zonal dwell — SEVENTH reproduction**: censored per-dwell MID p50
  centre/outskirts **104/83 s** (rounds 8–14: 102/82, 101/82, 106/86,
  103/82, 103/82, 102/85, 104/83 — ratio x1.20–x1.26, seven rounds).
  Uncensored zone time-budget: centre **[13.1, 34.8]** vs out
  **[9.9, 24.2]** s/stop (2259/4679 stop-visits) — bands separated again,
  midpoints 24.0 vs 17.1. **Still waiting on the evening AND night zone
  bands** (gate 1) before the live experiment (gate 2). No code.
- Fleet-level TOD_DWELL: h13 [11.6, 26.8], h14 [11.4, 27.4] s — 18 s
  inside; sample gate PASS (6943 stop-visits), evidence gate FAIL →
  **stays 1.0**.

### R1 continued validation (at-fix, live engine)

Window: signed p50 −6.0 / |e| p50 77 / p90 210 / %ahead 38.0 (n=8064),
projSim 74/205 — |e| p50 at the top of the 70–77 band, %ahead nominal,
signed leaning behind (see h14 precursors). fresh(seed) 63/193 vs
converged 78/210: no sprint signature.

### Applied (engine)

**Nothing — neutral h14 as pre-registered, with pre-peak precursors
logged.** Both double gates evaluated and failed on evidence: TOD_PACE h14
(sample PASS n=4140; norm-deviation at the h12/h13 level + paired fleet
near-null + replay D36 flat), TOD_DWELL h13/h14 (sample PASS; 18 s inside
every band). R5 (b) still unmet. Tooling: `replay.py` D36/D37 added
(round-14 gate record).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ВЕЧЕРНИЙ ЧЕК-ЛИСТ (15:11–18:11) — что именно смотреть

**R5 (b) — решающая проверка на пике.** Ship-триггеры (любой из):
1. fresh(seed) at-fix signed p50 ≤ **−10 m** (fresh sims систематически
   отстают — сид 0.62 занижен для пикового флота);
2. fresh |e| p90 деградирует против converged (>+15–20% с n≥300 —
   исторически fresh 187–220 vs conv 203–210);
3. replay D22 (prior 0.66) даёт выигрыш на вечернем окне (до сих пор flat:
   118 vs 118 все раунды).
Контекст: (a) выполнено давно (девять чтений 0.655–0.680 vs 0.62); если
вечер тоже нейтрален по всем трём — **R5 закрывается как «не шипить»**,
как и было запланировано в раунде 11.

**R8 — вечерняя зональная полоса (gate 1, часть 2 из 3).** Нужно:
censored MID p50 centre/out в x1.20–x1.26 И разделённые uncensored
time-budget полосы (centre mid ~24–25 vs out ~17) на h15–h18 окнах. Если
вечер воспроизводит — остаётся ночная полоса, потом live-эксперимент
(gate 2). Дополнительно: следить, растёт ли centre dwell АБСОЛЮТНО на
пике (дневной диапазон MID p50 101–106 s) — рост >115 s был бы первым
входом для TOD_DWELL-зонального, не только зонального R8.

**TOD_PACE h15–h18 — главный контраст дня.** Пре-регистрация: если
пиковое замедление реально, ждём (в порядке силы): paired centre
Δspeed ≤ −2…−3 км/ч (продолжение сегодняшнего −3.19), centre feed p50
ПРОБИВАЕТ вниз коридор 18.9–19.8, fleet p50 падает с 22.0–22.2 к ≤21,
at-fix signed уходит за −10 (сим впереди реальности → нужен фактор <1.0).
Двойной гейт как всегда: sample n_fix ≥ 300/час + все три лега evidence
(норм-отклонение с подвыборками, paired, replay D-пара). ВАЖНО: зонально-
структурная часть (центр ~19 весь день) УЖЕ покрыта центр-капом — не
записывать её в TOD; TOD-сигнал только то, что МЕНЯЕТСЯ к вечеру.

**Вечерний release h19+ (после пика):** centre p50 выходит вверх из
18.9–19.8 — это второй конец структурной истории и потенциальный
TOD_PACE >1.0 вечерний вход; фиксировать час выхода.

### Open items after round 14

- **Evening rounds (h15–18)**: чек-лист выше — R5 (b) решающая, R8
  вечерняя полоса, TOD_PACE пиковый контраст; h19+ release.
- **R8**: needs evening + night zone-band reproduction, then the live gate.
- Pre-peak precursors logged this round (paired centre −3.19, at-fix h14
  −8.0) — confirm or retire at h15–16.
- Carried: %ahead-vs-device (needs device ride), night rounds for TOD_DWELL.

## Day round 15 — 2026-07-12 15:11–15:55 (EVENING PEAK ONSET: h14 tail + h15 first 26 min)

### Pipeline health

- Harvester PID 31497 alive, ticking through 15:26 (292 M) and 15:36
  mid-round; app PID 33035 (no restart), Metro 8081 up, simulator booted.
  Continuity scan: **zero gaps > 20 s** — fifth consecutive hole-free round.

### Window (analysis on data NEW since round 14)

`--since 2026-07-12 14:26` → 15:26 (60.5 min), 255 trams, 127.6 k day-line
records, 100% v2, night routes 0%.

### (а) TOD h15 — the peak decision point; double gate evaluated, FAIL — but
### the round's real finding is a 20-min SIM-SIDE TRANSIENT at 15:00–15:20

- At-fix by hour: h14-tail signed p50 **−10.0** / %ahead 36.9 (n=5276),
  h15 **−35.0** / 32.9 (n=4143) — nominally a huge "behind" reading, and
  the pre-registered "signed уходит за −10" line fires. Dissection says
  it is NOT a TOD signal:
  - **10-min slices**: 15:00–:10 −38, :10–:20 −48, then **:20–:30 −10.5**
    (day-file re-read after the 15:36 drain: :20 −12, :30 −11). A 20-min
    excursion with full recovery, not a regime.
  - **Fleet-wide, uniform body shift**: centre −44 / out −31; even −35.5 /
    odd −37; all models (15t worst −46); distribution p10/p25/p50/p75
    −183/−102/−10/+44 (h14) → −251/−150/−44/+20 (excursion) → back to
    −190/−109/−12/+37 after :20. prev=at_stop probes stayed −2 both
    hours; prev=on_track deepened −59 → −80 (moving trams only).
  - **EVERY feed observable is flat through the excursion** (per 10-min
    bucket): true feed speed 21.7–22.3 km/h; fix cadence p50 48/49 s; jump
    at fix p50 332/332 m; **delayS p50 38/39 s** (реальный флот не теряет
    график); delivery latency (t−obsAt) p50 11.1–11.4 s; re-anchors 34–43
    /bucket; bias-reseeds flat; trams 253→251.
  - **Sim-side chase signature**: simKmh p50 21.1 → 23.5, devM p50 175 →
    152 (controller closing a gap that opened ahead of it), tick Δ/exp
    0.97 → 0.947 (−2%, not a stall). Fleet bias p50 flat 0.65–0.67.
  - **Hypotheses killed**: (1) hourly-boundary artifact — minute-of-hour
    sweep h10–h13 reads −2…−6 at :00–:20, no sawtooth anywhere but 15:00
    (FALSIFIED as a general artifact); (2) fix-latency shift — flat
    (FALSIFIED); (3) trip-turnover/re-anchor burst at 15:00 — flat
    (FALSIFIED); (4) thermal/poll stall — poll p50 5.0 s throughout, tick
    −2% only (insufficient).
  - **Open candidate mechanism**: the engine target blends `sSched` into
    the projection (`target = blend(sObs, sSched) − TRAIL_M`,
    tramSim.ts:270) — a schedule-side step at the 15:00 service
    transition would move targets fleet-wide with the feed flat, and
    paceBias re-convergence (t½ 150 s, dwell-deducted) matches the
    ~20-min recovery. UNPROVEN — pre-registered test at 16:00 (below).
- **TOD_PACE h15 double gate**: sample gate PASS (n_fix 4143 ≥ 300).
  Evidence gate **FAIL on all three legs**: (1) h15-only vs the 0.66 norm
  x0.985 (114/128), halves x0.985/x0.985, even x0.977 — the same norm
  level as h12–h14, not an h15 deviation; (2) paired within-tram h14→h15
  (237 trams) fleet near-null — Δspeed **+0.07 km/h** (121/116), Δbias
  +0.000 (109/118); (3) replay D38 (tod h15=0.99) flat vs NEW (117/315,
  signed 18 vs 18, frs 125 vs 126), D39 wrong-way control 1.05 marginally
  worse (signed +20, devM 222/510). **TOD_PACE h15 stays 1.0.** Caveat:
  h15 sampled only :00–:26 (~40% of the hour, transient inside) — final
  h15 verdict at round 16 on the full hour.
- Pre-registered peak signals scorecard: paired centre Δspeed **−3.85
  (12/50)** — fires, deeper than r14's −3.19, but it is the ZONAL story
  (outskirts +1.49, fleet null; per pre-registration NOT bookable to
  TOD); centre corridor pierce — transient only (buckets 18.7/18.3 at
  :00–:20, back to **19.6** at :20; hourly 19.3/18.7); fleet p50 → ≤21 —
  NO (22.0/21.9); at-fix < −10 — fired but dissected as the transient +
  a residual −11/−12 plateau (см. R1 ниже). No true TOD evening signal yet.
- Replay gate on the window extract (255 trams, n_frs=778): OLD 120.5 →
  NEW 116.9 (**−3.0%, fifteenth re-validation**); fresh p90 390.5 → 369.2.

### (б) Centre slowdown — h15: paired downtick deepens, corridor holds (transient pierce only)

- Hourly centre feed p50 19.3 (h14) / 18.7 (h15, transient-weighted);
  bucket series 19.3, 19.3, 19.2, 19.3, 18.7, 18.3, **19.6** — the
  corridor floor 18.9 was pierced only during the excursion and the :20
  bucket came back INSIDE (19.6). Tenth consecutive hour of the 18.9–19.8
  structural regime; paired centre −3.85 is the second consecutive
  acceleration of the centre decline (−0.4…−2.0 → −3.19 → −3.85). A
  SUSTAINED (non-transient) break below 18.9 at h16–17 remains the
  pre-registered evening TOD entry condition, distinct from the zonal cap.

### (в) R5 / R8 копилки

- **R5 vs 0.62 — TENTH reading**: FULL **0.660 (x1.065, 187/68)**, halves
  x1.065/x1.065, parity x1.073/x1.065, 4-way x1.065–x1.081 — all above,
  rock-steady. Running log: … 13:35 → 0.660, 14:26 → 0.660, **15:26 →
  0.660**. vs 0.66: FULL 0.650 x0.985 (113/141), ALL subsamples x0.985 —
  0.66 remains the fleet's neutral point.
- **R5 condition (b) — the decisive peak check, trigger-by-trigger**:
  - Trigger 2 (fresh p90 degradation): fresh |e| p90 206.7 vs conv 229.0
    — fresh BETTER. **Not fired.**
  - Trigger 3 (replay D22 gain): 117/314 signed 19 vs NEW 117/315 signed
    18 — flat, sixth consecutive flat reading. **Not fired.**
  - Trigger 1 (fresh signed ≤ −10): full window fresh −25.0 (n=552) —
    fires by the letter, BUT converged reads −20.0 (the transient hits
    everything). Clean segments: pre-transient (14:26–15:00) fresh −14.0
    (n=317) vs conv −10.0; post-transient (15:20+) fresh −13.5 (n=152) vs
    conv −11.0. Still ≤ −10 by the letter — but the fresh−conv
    differential is −2.5…−4 m (r14 had fresh BETTER: −2 vs −7), i.e. the
    −10-level is a FLEET-WIDE h14+ shift that a seed change cannot fix
    (converged sims sit there too), and the peak-proper clean fresh
    sample is n=152 < 300. **Verdict: PROVISIONALLY FIRED — confirm at
    round 16 on ≥300 clean h15–16 fresh probes; ship only if the
    fresh−conv differential itself exceeds ~−10, otherwise it is
    not-fired-in-spirit (fleet-level story, not a seed story).**
  - **PACE_BIAS_PRIOR stays 0.62 this round.**
- **R8 zonal dwell — EIGHTH reproduction, evening band part 1**: censored
  per-dwell MID p50 centre/outskirts **103/84 s** (x1.23 — rounds 8–15
  band x1.20–x1.26 intact). Uncensored zone time-budget (now a permanent
  dwell_v2.py output): centre **[14.2, 34.3]** vs out **[10.4, 24.2]**
  s/stop (2033/4431 stop-visits), midpoints 24.3 vs 17.3 — matches the
  pre-registered ~24–25 vs ~17. The evening zone band (h15 onset)
  REPRODUCES; still need full h16–18 + the night band before gate 2.
  Centre dwell watch: MID p50 103 s < 115 s — TOD_DWELL-zonal entry NOT
  triggered.
- Fleet-level TOD_DWELL: h14 [11.5, 26.9], h15 [11.7, 27.9] s — 18 s
  inside both; sample gate PASS (6464 stop-visits), evidence gate FAIL →
  **stays 1.0**.

### R1 continued validation (at-fix, live engine)

Window: signed p50 −21.0 / |e| p50 81 / p90 226 / %ahead 35.1 (n=9419),
projSim 76/208 — |e| p50 above the 70–77 band and signed outside −2…−7,
both excursion-inflated. Post-transient level: −11/−12 — an elevated
"behind" plateau continuing the h14 slide (−4 → −8 → −10 → −16 → −11/−12
by minute-buckets). NOT the peak-slowdown signature (that would push the
sim AHEAD, positive signed); watch whether the plateau persists at h16 or
decays with the transient's tail. fresh(seed) 78/207 vs converged 81/229:
no sprint signature.

### Applied (engine)

**Nothing — no engine constant passed its gate.** TOD_PACE h15: sample
PASS, evidence FAIL ×3 (norm x0.985 + paired fleet null + replay D38
flat). TOD_DWELL h14/h15: evidence FAIL. R5(b): trigger 1 provisionally
fired (confounded by the transient + fleet-wide shift; clean peak sample
n=152 < 300), triggers 2/3 clean misses — prior held at 0.62 pending
round 16. Tooling: `replay.py` D38/D39 (round-15 gate record);
`dwell_v2.py` grew a permanent per-zone uncensored time-budget (R8 band
tracking, replaces the ad-hoc computation).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 16 (16:11)

1. **16:00-boundary watch (решает судьбу транзиента)**: если at-fix
   signed p50 в 16:00–16:20 снова уходит к −30…−50 с откатом к :20 при
   плоских feed-наблюдаемых — это ПОЧАСОВОЙ ГРАНИЧНЫЙ АРТЕФАКТ движка
   (кандидат: sSched-ступенька в target-бленде, tramSim.ts:270) —
   структурный баг-фикс, НЕ TOD-константа; если нет — 15:00 остаётся
   one-off (сервисный переход расписания). Проверять слайсами :00/:10/:20.
2. **R5(b) финал**: trigger 1 на ≥300 ЧИСТЫХ fresh-пробах h15–16
   (исключив граничные окна :00–:20, если артефакт подтвердится); ship
   только если fresh−conv дифференциал ≤ −10 м сам по себе. Triggers 2/3
   перечитать. Если все чисто-нейтральны — **R5 закрывается «не шипить»**.
3. **TOD_PACE h15 финал** на полном часе (данные 15:26–16:00 доберут
   хвост) + h16 по стандартному двойному гейту.
4. **R8**: h16-полоса (censored ratio x1.20–x1.26 И разделённые бюджеты);
   centre dwell watch >115 s.
5. **R1 плато**: держится ли −11/−12 «behind» уровень вне граничных окон.

### Open items after round 15

- Round-16 pre-registration above (boundary watch is the headline).
- **R8**: h16–18 + night band, then the live gate 2.
- Pre-peak precursors: paired centre CONFIRMED deepening (−3.19 → −3.85,
  zonal); at-fix "behind" — resolved into transient + plateau, see №1/№5.
- Carried: %ahead-vs-device (needs device ride), night rounds for
  TOD_DWELL, h19+ evening release watch (centre exits 18.9–19.8 upward).

## Day round 16 — 2026-07-12 16:35–16:55 (DECISIVE: 16:00-boundary watch + R5 close-out; h15 tail + h16 first 37 min)

### Pipeline health

- Harvester PID 31497 alive (16:07/16:17/16:27/16:37 ticks, 325 M drained);
  app PID 33035 alive, no restart (5 h 40 m uptime); Metro 8081 up,
  simulator booted. Continuity: **TWO poll gaps > 20 s** this round —
  25.2 s + 41.2 s back-to-back at 15:55:35 → 15:56:42 (a single ~66-s app
  stall; first gaps after five hole-free rounds). Everything else 5.0 s
  p50 / 5.2 s p90. The stall matters — see (а).

### Window (analysis on data NEW since round 15)

`--since 2026-07-12 15:26` → 16:37 (71.5 min), 253 trams, 151.1 k day-line
records, 100% v2, night routes 0%.

### (а) 16:00-boundary watch — NOT REPRODUCED; the 15:00 transient stays a one-off

Pre-registered question: does at-fix signed p50 dive to −30…−50 at
16:00–16:20 with recovery by :20 and flat feed observables (the 15:00
pattern)? **NO.**

- 10-min at-fix slices: 15:30 −15, 15:40 −15, 15:50 **−35.5**, 16:00 −25,
  16:10 −12, 16:20 −30, 16:30 −24. 5-min: 16:00 −27 → 16:05 −22.5 →
  16:10 −11 → 16:15 −13. The 16:00–16:20 window overall reads **−19.0**
  (n=3158) — at the window's own baseline, no excursion, nothing to
  recover from at :20.
- The one real excursion sits at **15:54–15:59** (per-minute p50: 15:53
  −6 → 15:54 −72 → 15:55 −46.5 → 15:56 **−195** → 15:57 −87 → 15:58
  −55.5 → 15:59 −26 → 16:00 −13.5) — i.e. BEFORE the boundary, and it has
  a mundane explanation the 15:00 event did not: the logged **66-s poll
  stall** at 15:55:35–15:56:42 (sim integration frozen ~66 s at ~6 m/s ≈
  the −195 spike, then controller catch-up over ~3 min — the exact stall
  signature round 15 falsified for 15:00, where poll cadence was clean).
  On top of it, feed observables were NOT flat this time: fleet delayS
  p50 rose 47 → 64 s across 15:45–15:57 and eased back to ~48–52 (round
  15's transient had delayS pinned at 38/39) and centre feed dipped to
  17.8 in the 15:50 bucket. Different event class.
- **Verdict (pre-registered)**: the 15:00–15:20 transient did NOT
  generalize to the 16:00 boundary → NOT an hourly-boundary engine
  artifact. It stays a **one-off** (most plausibly the 15:00 service
  transition in the timetable feed; the sSched-step-in-target-blend
  candidate, tramSim.ts:270, remains UNPROVEN and dormant). **No R10
  proposal filed** — nothing reproducible to fix. Cheap follow-up: watch
  tomorrow's 15:00 boundary if collection is still running; two
  same-time reproductions would reopen R10 with a service-transition
  (not hourly) trigger.

### (б) R5 FINAL — condition (b) trigger-by-trigger on the clean peak window; ALL NEUTRAL → R5 CLOSED

- **Trigger 1** (fresh signed ≤ −10 AND fresh−conv differential ≤ −10):
  clean fresh probes n=464 ≥ 300 (15:26–16:00: 304; 16:20+: 160; the
  stall/boundary 16:00–16:20 excluded). Readings: 15:26–16:00 fresh
  **−16.0** vs conv −19.0; 16:20+ fresh **−2.0** vs conv −28.0; full
  window fresh −16.5 vs conv −20.0. The fresh−conv differential is
  **positive everywhere** (fresh BETTER than converged, +3…+26 m) — the
  −10…−16 level is the fleet-wide h14+ "behind" plateau (see R1 below),
  which converged sims share and a seed change cannot address. **Not
  fired** (per the round-15 pre-registered differential criterion).
- **Trigger 2** (fresh p90 degradation > +15–20%): fresh |e| p90 227.6 vs
  conv 226.0 (+0.7%). **Not fired.**
- **Trigger 3** (replay D22 prior-0.66 gain): D22 118/317 signed 19 vs
  NEW 117/316 signed 18 — flat-to-marginally-worse, **seventh consecutive
  flat** reading. **Not fired.**
- Condition (a) context — ELEVENTH reading: window vs 0.62 FULL **0.650
  (x1.048, 186/67)**, ALL 8 disjoint subsamples above (x1.048–x1.081);
  running log: … 14:26 → 0.660, 15:26 → 0.660, **16:37 → 0.650**. vs
  0.66: FULL 0.640 x0.970 (101/152) — honest point estimate 0.65.
- **ВЕРДИКТ: R5 ЗАКРЫВАЕТСЯ как «не шипить»** (as scheduled in round 11).
  The controller eats the seed level: paceBias converges to the fleet's
  0.65–0.66 within ~2 half-lives regardless of the 0.62 seed, fresh sims
  are neutral-to-better than converged in live at-fix in EVERY round
  including the peak (the one scenario that could have punished a low
  seed), and the prior-0.66 replay bought nothing in seven windows across
  morning peak, noon, afternoon and evening peak. Live unbiasedness is
  achieved without touching the prior. **PACE_BIAS_PRIOR stays 0.62** —
  permanently, absent a device-ride contradiction.

### (в) TOD h15 FINAL + h16 first read — both stay 1.0

- **TOD_PACE h15 FINAL (full hour 15:00–16:00)**: sample PASS. Evidence
  FAIL ×3: (1) full-hour norm read vs 0.66 **x0.970** (104/148), halves
  x0.985/x0.985, parity x0.985/x0.970 — the h12–h14 level, not an h15
  deviation; (2) paired h14→h15 was fleet-null (round 15) and h15→h16 is
  too (below); (3) replay D38 flat (117/316 vs NEW 117/316). The round-15
  caveat (40% hour coverage) is discharged: **TOD_PACE h15 = 1.0 FINAL.**
- **TOD_PACE h16 (first read, :00–:37)**: sample gate PASS (n_fix 5944).
  Evidence gate FAIL ×3: (1) h16 vs 0.66 norm **x0.970** (110/142),
  halves x0.985/x0.985, parity x0.970/x0.985, 4-way x0.970–x0.985 — same
  fleet level since h12; (2) paired within-tram h15→h16 (241 trams)
  fleet near-null — Δspeed **+0.83 km/h** (138/103), Δbias −0.010
  (106/122); zonal split intact: centre-majority −1.81 (18/42) vs
  outskirts +1.50 (120/61) — shallower than r15's −3.85, the centre
  decline is NOT accelerating further; (3) replay D40 (tod h16=0.98)
  flat vs NEW (117/315 vs 117/316, signed 18/18), D41 wrong-way control
  marginally worse (118/317, signed 19). **TOD_PACE h16 stays 1.0** —
  final verdict at round 17 on the full hour.
- True feed speed by hour: fleet p50 21.8/21.9 km/h (h15 full / h16),
  centre 18.8/19.2, out 23.7/23.7 — no fleet slowdown to ≤21, centre
  corridor 18.9–19.8 holds (h15's 18.8 is transient/stall-weighted;
  bucket series 19.4, 19.2, 17.8*, 19.3, 18.9, 19.2, 19.6 — only the
  stall+delayS bucket pierces). Eleventh and twelfth consecutive
  structural-corridor hours. No evening TOD_PACE entry signal.
- Replay gate on the window extract (253 trams, n_frs=868): OLD 121.6 →
  NEW 117.5 (**−3.4%, sixteenth re-validation**); fresh p90 391.0 → 378.3.

### (г) R8 evening band part 2 (h16) — REPRODUCES (ninth); daytime+evening evidence complete

- Censored per-dwell MID p50 centre/outskirts **104/82 s = x1.27**
  (h16-only: 104/82; window: 104/82) — a hair above the historical
  x1.20–x1.26 top edge; band updated to **x1.20–x1.27** across rounds
  8–16 (peak, noon, afternoon, evening peak).
- Uncensored zone time-budget, h16-only: centre **[14.4, 34.1]** vs out
  **[10.0, 24.1]** s/stop (1231/2749 stop-visits), midpoints 24.3 vs
  17.1 — matches the pre-registered ~24–25 vs ~17. Bands separated.
- **Gate-1 status: daytime + evening COMPLETE. Remaining: the night band
  (after 22:00), then gate 2 (the live zonal-dwell experiment).**
- Centre dwell watch: MID p50 104 s < 115 s — TOD_DWELL-zonal entry NOT
  triggered (absolute centre dwell is flat through the peak: 101–106 all
  day).
- Fleet-level TOD_DWELL: h15 [11.7, 27.1], h16 [11.4, 27.5] s — 18 s
  inside both; sample gate PASS (7569 stop-visits), evidence gate FAIL →
  **stays 1.0**.

### R1 continued validation (at-fix, live engine) — the "behind" plateau persists and deepens late in h16

Window: signed p50 −20.0 / |e| p50 79 / p90 224 / %ahead 34.6 (n=11059,
stall included); h16 alone −22.0 / 77 / 219 / 34.2. The round-15 plateau
question (№5): outside the stall/boundary windows the level reads −11…−15
(15:20–15:40, 16:10–16:15) but the h16 second half sits at **−22…−30**
(16:20/:25/:30/:35 slices) with feed observables flat (22.0 km/h, delayS
back to 41–54, centre 19.2–19.6) — the plateau is NOT decaying with the
transient's tail; it is drifting deeper through the peak. Signature
unchanged: prev=at_stop probes −2.0 (%ahead 44.7) vs prev=on_track −69.0
(moving trams carry the whole offset). NOT the peak-slowdown signature
(that would be sim-AHEAD/positive). fresh(seed) 75/228 vs converged
80/226 — no sprint signature. **Watch item for round 17: does the
−22…−30 level persist into h17, and does it release after the peak?**

### Applied (engine)

**Nothing — no engine constant passed its gate**, and the headline
boundary hypothesis was falsified, so no R10 bug-fix proposal either.
TOD_PACE h15 FINAL 1.0, h16 stays 1.0 (evidence FAIL ×3 each), TOD_DWELL
h15/h16 stays 1.0 (18 s inside every band). **R5 formally CLOSED, prior
0.62 permanent.** Tooling: `replay.py` D40/D41 added (round-16 gate
record).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 17 (~17:40)

1. **TOD_PACE h16 финал** на полном часе + h17 первый замер — стандартный
   двойной гейт. Ожидание по одиннадцати часам ровного x0.97–x0.985:
   нейтрально.
2. **R1 плато −22…−30**: держится ли в h17 вне стол-окон; если к h18–19
   уровень уйдёт за −35 при плоских feed-наблюдаемых НА ПОЛНОМ ЧАСЕ (не
   транзиент) — это станет самостоятельным R11-кандидатом (систематическое
   отставание сима на пике; направление НЕ TOD_PACE-совместимое, т.к.
   норм-чтения x0.97 тянут в ДРУГУЮ сторону — сначала диагностика, не
   константа).
3. **R8**: h17/h18-полосы — опциональное подкрепление (gate-1 дневная+
   вечерняя части уже полны); решает НОЧНАЯ полоса после 22:00.
4. **h19+ release watch**: centre p50 выходит вверх из 18.9–19.8 —
   фиксировать час.
5. **15:00-boundary one-off**: dormant; перепроверить завтра на границе
   15:00, если сбор ещё идёт (двойное совпадение по времени суток →
   reopen R10 с триггером «сервисный переход», не «почасовой»).

### Open items after round 16

- Round-17 pre-registration above (h16 final + плато — главное).
- **R8**: night band (22:00+), then live gate 2. Band now x1.20–x1.27.
- **R5 CLOSED** («не шипить», prior 0.62 permanent).
- Carried: %ahead-vs-device (needs device ride), night rounds for
  TOD_DWELL, h19+ release watch, dormant 15:00-boundary recheck.

## Day round 17 — 2026-07-12 17:11–18:00 (HEADLINE: R11 diagnosis of the "behind" plateau; h16 final + h17 first read)

### Pipeline health

- Harvester PID 31497 alive (16:47…17:48 ticks, 372 M drained); app PID
  33035 alive, no restart (~7 h uptime); Metro 8081 up, simulator booted.
  Continuity: **zero poll gaps > 20 s** (746 polls, cadence p50 5.0 /
  p90 5.2 / max 10.4 s) — the 15:55 stall did not recur.

### Window (analysis on data NEW since round 16)

`--since 2026-07-12 16:37` → 17:48 (71.4 min), 256 trams, 153.2 k
day-line records, 100% v2, night routes 0%. Diagnostic comparison
windows re-cut from the day file: 09:36–10:36, 12:35–13:35, 14:26–15:26.

### (а) R11 DIAGNOSIS — the plateau dissected; mechanism identified and quantified (new tooling: `plateau_diag.py`)

**Trajectory**: h17 full-hour at-fix signed p50 **−22.0** (n=7695) — exactly
the h16 level (−21.0 this window); 5-min slices wobble −8.5…−36.5 with NO
trend; the pre-registered escalation (за −35 на полном часе) did NOT fire,
but no release either. Feed observables flat throughout (true feed speed
21.7/21.8 km/h h16/h17, delayS p50 39–49 s, delivery latency 10–12 s).

**Dissection** (all-day comparison: morn 09:36 / noon 12:35 / h15 / peak):

- **prev=at_stop probes sit at −2 m ALL DAY; prev=on_track carry the whole
  plateau**: −48 (morn) / −52 (noon) / −67 (h15) / **−69 (peak)**. The
  on_track share of probes is CONSTANT 46–47% all day → the deepening is
  not a probe-mix shift.
- **Not fresh sims** (fresh −10 vs converged −22; fresh |e| p90 199 <
  conv 218), **not sim age** (age 0–15/15–60/60–180 min → −22/−21/−22),
  **not one model** (15t deepest −33, but 15t is the centre-heavy fleet;
  kt8d5/t3rplf/14t sit −8…−12). **Zonal**: centre −38 vs outskirts −14
  (morning: −21 vs −2) — both ~2x deeper at peak, centre carries more.
- **МЕХАНИЗМ (подтверждён количественно): schedule-pace projection drag.**
  `targetDistAt` (tramSim.ts:267) projects the observation forward at
  SCHEDULE pace (`sObs = obsDist + max(0, sSched(now) − sSched(obsAt))`,
  blend 0.75·sObs + 0.25·sSched − TRAIL_M). When a tram runs FASTER than
  its delay-shifted schedule (fix-to-fix ΔdelayS < 0), even a perfect
  target-follower reads behind the next fix by ~0.75·v·|ΔdelayS|.
  Prediction: signed-vs-ΔdelayS slope ≈ +0.75·v̄ ≈ +4…5 m per second.
  **Measured: +4.26 m/s** (morn +4.55, noon +4.50, h15 +4.12 — stable all
  day); ΔdelayS bands perfectly monotonic: <−10 s → −130, −10…−3 → −49,
  −3…+3 → −6, +3…+10 → +15, >+10 → **+88** (sim AHEAD when trams lose
  time). The mechanism is STRUCTURAL, not peak-specific.
- **Что именно пиковое**: равномерный сдвиг −20…−30 м ВНУТРИ всех
  ΔdelayS-полос (состав полос не изменился), сконцентрированный в
  «steady»-пробах (|ΣΔdelayS| < 5 s за ~4 фикса: morn **−10** → h15 −25 →
  peak **−41**), при том что остаток одного интервала закрывается к
  следующему фиксу (quiet-current после рекавери-фикса: −2…−9 — контроллер
  успевает). Т.е. в пик движущиеся трамваи систематически опережают
  schedule-pace ВНУТРИ перегона даже при плоском end-to-end delayS —
  slack расписания съедается на остановках (реальные dwell короче
  заложенных), а проекция и 0.25·sSched-бленд наследуют внутриперегонную
  фазу расписания. Сим на перегоне отстаёт от реальности на ~69 м p50
  (≈11 s хода при 22 км/ч) и ресинхронизируется на каждой остановке
  (at_stop −2).
- **Кросс-чек**: replay.py проецирует наблюдение по РЕАЛЬНОМУ темпу
  (vProj EWMA) вместо расписания — и на том же окне его at-fix signed
  читает **+17** (%ahead 54), плато отсутствует. Прямое подтверждение,
  что источник — schedule-pace проекция, а не контроллер/bias/dwell.

**ВЕРДИКТ (пре-регистрированный)**: НЕ «правильное отражение реальности»
(рендер честно отстаёт от реальности на перегоне — свежий фикс уже
впереди сима, а истинная текущая позиция ещё дальше), но и НЕ
эскалирующий баг: уровень стабилен, ограничен, самосинхронизируется на
остановках, границу −35 не пробил. **→ R11-ПРЕДЛОЖЕНИЕ (механизм, БЕЗ
реализации)**: проецировать наблюдение вперёд не чистым темпом
расписания, а расписанием × выученным внутриперегонным фактором
(per-tram EWMA реализованной межфиксовой скорости к скорости расписания
на том же спане — та же величина, что paceBias уже учит против
V_CRUISE_REF, но применённая к ПРОЕКЦИИ, а не только к chase-скорости
сима). Ожидаемо: on_track p50 −69 → ~−TRAIL_M, общий уровень −22 →
−5…−10; наклон +4.3 м/с останется (неустранимый информационный предел
фида). Риски: overshoot перед остановками (braking envelope НЕ трогать),
взаимодействие с adaptive dwell (главный сток коррекции ошибки);
прототипировать конфигом projection-speed в replay.py ДО движка.
Условие имплементации: отдельный раунд с прототипом в replay + полный
двойной гейт; текущее плато НЕ горит (перцептивно ≤11 s хода).

### (б) TOD h16 FINAL + h17 first read — both stay 1.0

- **TOD_PACE h16 FINAL (full hour)**: sample PASS (n_fix ~9.6 k across
  rounds 16–17). Evidence FAIL ×3: (1) full-hour norm vs 0.66 **x0.985**
  (109/144), halves x0.985/x0.970; (2) paired h15→h16 fleet near-null
  (round 16); (3) replay D40 on the full-hour extract flat (117/312
  signed 16 vs NEW 117/312 signed 16; D41 wrong-way worse 117/313/18);
  full-hour gate re-validation OLD 120.5 → NEW 117.3 (−2.6%).
  **TOD_PACE h16 = 1.0 FINAL.**
- **TOD_PACE h17 (first read, :00–:48)**: sample PASS (n_fix 7695).
  Evidence FAIL ×3: (1) h17-only norm vs 0.66 **x0.985** (114/141), ALL
  8 disjoint subsamples x0.970–x0.985 — the h12–h16 fleet level; (2)
  paired within-tram h16→h17 (252 trams) fleet null — Δspeed **−0.13
  km/h** (122/130), Δbias +0.010 (131/111); centre-majority −0.90 (20/44)
  vs outskirts +0.31 — the centre decline keeps DECELERATING (−3.85 →
  −1.81 → −0.90), no evening TOD entry; (3) replay D42 (h17=0.98) flat
  (115/307 signed 17 vs NEW 116/307 signed 17), D43 wrong-way control
  worse (signed 19). **TOD_PACE h17 stays 1.0** — final at round 18.
- True feed speed: fleet p50 21.7/21.8 km/h (h16 tail / h17), centre
  19.0/**18.9** (corridor floor touched but not pierced — watch), out
  23.2/23.6. Thirteenth/fourteenth consecutive structural-corridor hours;
  h19+ upward release not yet.
- Replay gate on the window extract (256 trams, n_frs=870): OLD 119.7 →
  NEW 115.5 (**−3.5%, seventeenth re-validation**); fresh p90 396.6 →
  375.8.
- R5 копилка (closed; THIRTEENTH reading for the record): vs 0.62 FULL
  0.660 (x1.065, 193/63); vs 0.66 FULL 0.640 (x0.970, 104/152) — the
  fleet neutral point stays 0.65–0.66; running log: … 15:26 → 0.660,
  16:37 → 0.650, **17:48 → 0.660**.

### (в) R8 evening band part 3 (h17) — REPRODUCES (tenth)

- Censored per-dwell MID p50 centre/outskirts **103/83 s = x1.24** —
  mid-band (x1.20–x1.27 intact across rounds 8–17).
- Uncensored zone time-budget: centre **[13.8, 33.4]** vs out
  **[9.8, 23.3]** s/stop (2402/5199 stop-visits), midpoints 23.6 vs
  16.6 — the pre-registered ~24 vs ~17 shape holds.
- Fleet-level TOD_DWELL: h16 [10.9, 25.9], h17 [11.2, 26.7] s — 18 s
  inside both; evidence gate FAIL → **stays 1.0**. Centre dwell watch:
  MID p50 103 s < 115 s — zonal entry NOT triggered.
- Gate-1: остаётся только НОЧНАЯ полоса (22:00+), затем gate 2.

### Applied (engine)

**Nothing — no engine constant passed its gate; R11 filed as a PROPOSAL
(mechanism + replay prototype path), no implementation** per the round-17
pre-registration (диагностика прежде константы; уровень не эскалирует).
TOD_PACE h16 = 1.0 FINAL, h17 stays 1.0, TOD_DWELL h16/h17 stays 1.0.
Tooling: **`plateau_diag.py` NEW** (at-fix dissection: trajectory ×
statePos, class splits, ΔdelayS mechanism bands + OLS slope, delayS-level
control, accumulation/steady analysis); `replay.py` D42/D43 added.

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 18 (~19:00)

1. **TOD_PACE h17 финал** (хвост :48–:60 доберётся) + h18 первый замер —
   стандартный двойной гейт. Ожидание: нейтрально (x0.97–x0.985).
2. **R11 release-тест (решающий для механизма)**: механизм
   schedule-slack предсказывает, что ПОСЛЕ пика (h18–19) steady-пробы
   отмелеют обратно к −10…−15 и on_track p50 вернётся к −45…−55
   (утренне-полуденный уровень) при плоском фиде. Подтверждение релиза =
   сильное свидетельство за механизм; НЕ-релиз при спавшем трафике =
   пересмотр (запускать `plateau_diag.py` на каждом окне).
3. **h19+ release watch**: centre p50 выходит вверх из 18.9–19.8 —
   фиксировать час (сегодня h17 коснулся пола 18.9).
4. **R8**: ночная полоса после 22:00 (единственный недостающий кусок
   gate-1), затем gate 2.
5. **15:00-boundary one-off**: dormant; перепроверка завтра 15:00.

### Open items after round 17

- Round-18 pre-registration above (R11 release-тест — главное).
- **R11 PROPOSAL FILED** (schedule-pace projection drag; prototype in
  replay.py before touching the engine; not urgent — plateau stable).
- **R8**: night band (22:00+), then live gate 2. Band x1.20–x1.27.
- Carried: %ahead-vs-device (needs device ride), night TOD_DWELL rounds,
  h19+ release watch, dormant 15:00-boundary recheck (tomorrow).

## Day round 18 — 2026-07-12 18:11–18:55 (HEADLINE: R11 release test — INCONCLUSIVE, the release itself never arrived in the feed; plus a calendar discovery: today is SUNDAY)

### Pipeline health

- Harvester PID 31497 alive (ticks through 18:29, 391 M drained); app PID
  33035 alive, no restart (~7 h 41 m uptime); Metro 8081 up, simulator
  booted. Window-level continuity: **zero poll gaps > 20 s** (476 polls,
  cadence p50 5.0 / p90 6.0 / max 20.0 s) — BUT a **stall cluster at
  18:23–18:29**: poll gaps 13 / 20 / 13 s at 18:23:33 / 18:25:57 /
  18:28:44 plus a sim-tick integration collapse measured directly (fleet
  cruise ΔsimDist/expected p50 **0.563**, p25 0.082 at 18:26; 0.576 at
  the 18:29 drain edge; 0.93–0.99 everywhere else). Per-minute at-fix
  dived to −137…−205 with the at_stop invariant breaking (−48/−72/−124 at
  18:26/:27/:29) while EVERY feed observable stayed flat (delayS 44–51,
  fix lag flat, feed speed flat) and simKmh spiked to 25.7 (catch-up) —
  the **15:55 event class (app stall), second occurrence**; 18:23–18:29
  excluded from clean reads below. Watch: if the class recurs, restart
  the app BEFORE the night R8 band (uptime approaching 8 h).

### Window (analysis on data NEW since round 17)

`--since 2026-07-12 17:48` → 18:29 (41.1 min), 255 trams, 86.4 k day-line
records, 100% v2, night routes 0%. Clean cut for probe-level reads:
17:48–18:23 (stall cluster excluded).

### КАЛЕНДАРНОЕ ОТКРЫТИЕ: 2026-07-12 — ВОСКРЕСЕНЬЕ

Seventeen rounds (включая пре-регистрации «morning peak» / «evening
peak» / «post-peak release») использовали будничную рамку; никто не
проверил день недели. Следствия: (1) все сегодняшние TOD-вердикты — это
**воскресные** значения часов; к счастью, ни одна запись не ушла от 1.0,
таблица не контаминирована; (2) будничные пики НЕ ИЗМЕРЕНЫ — любая
будущая запись ≠1.0 требует буднего подтверждения; (3) «пик спадает к
h18» как посылка R11-release-теста была календарной, не измеренной —
и фид её не подтвердил (ниже). Ровный x0.97–x0.985 весь день и плоские
18.9–19.8 центра идеально согласуются с воскресным сервисом.

### (а) R11 release test — pre-registered discriminating moment DID NOT ARRIVE; plateau unchanged under an unchanged regime (mechanism-consistent, but not the decisive confirmation)

- **Feed regime check first** (the test's entry condition «пик спал»):
  fleet true feed speed h18 **21.7** km/h (h15–h17: 21.7–21.9), centre
  19.0 / out 23.6, fleet delayS p50 41–53 s (h17: 39–49), ΔdelayS
  deep-recovery composition **52% both windows** (r17 recut 2719/5207 vs
  r18 clean 1301/2526 probes in the <−10 s band). **Ничего не спало** —
  воскресный вечерний фид идентичен дневному.
- **Plateau under the unchanged regime — unchanged** (clean 17:48–18:23
  vs r17 recut 16:37–17:48): all −26.0 vs −21.5; prev=on_track **−70 vs
  −69**; prev=at_stop **−2 vs −2** (invariant intact outside the stall);
  steady ΣΔdelayS∈[−5,+5) **−41.0 vs −45.5**; steady-by-level [0,60)
  −34.0 vs −41.0, [60,∞) −21.5 vs −37.0 — mild shallowing in the steady
  rows, nowhere near the pre-registered −10…−15 release target.
- **Mechanism reproductions, 4th window**: OLS slope **+4.07** m/s clean
  (+4.09 full window; historical +4.12…+4.55); ΔdelayS bands perfectly
  monotonic (−129 / −42 / −19 / +6 / +88.5); replay cross-check
  (real-pace vProj projection) signed **+19**, %ahead 54.5 — no plateau,
  re-confirming the projection side as the source.
- **ВЕРДИКТ (честный): INCONCLUSIVE.** Механизм предсказывает release
  ПРИ смене режима фида; режим не сменился → предсказание не
  протестировано (наблюдённое «без изменений при неизменном входе» —
  слабое согласие, не решающее свидетельство). Это НЕ «плато держится
  вне пика» (пика в фиде не было — воскресенье), поэтому
  пересмотр-триггер НЕ активирован. **Решающий тест переносится на
  ночное окно (h20–22+, совместить с ночной R8-полосой)**: ночной флот
  исторически быстрый; если фид реально ускорится (fleet p50 ≥ 23–24,
  доля deep-recovery падает), механизм требует steady → −10…−15 и
  on_track → −45…−55; если фид сменится, а плато удержится — вот тогда
  пересмотр. **R11-предложение остаётся в силе; прототип в replay.py НЕ
  реализован в этом раунде (не дозрел — нет решающего окна).**

### (б) TOD h17 FINAL + h18 first read — both stay 1.0

- **TOD_PACE h17 FINAL (full hour 17:00–18:00, 256 trams, n_frs=782)**:
  replay D42 **flat** (115/308 signed 18 vs NEW 115/308 signed 18; frs
  p90 375 vs 378), D43 wrong-way control worse (116/309, signed 20);
  full-hour gate OLD 120.1 → NEW 115.5 (−3.8%). Norm + paired legs
  failed in round 17. **TOD_PACE h17 = 1.0 FINAL.**
- **TOD_PACE h18 (first read, :00–:29)**: sample gate PASS (n_fix 4596).
  Evidence gate FAIL ×3: (1) h18-only vs 0.66 norm **x0.985** (110/139),
  halves x0.985/x0.970, parity x0.985/x0.970, 4-way x0.962–x0.985 — the
  h12–h17 fleet level; (2) paired within-tram h17→h18 (250 trams) fleet
  null — Δspeed **+0.14 km/h** (127/123), Δbias +0.000 (115/119);
  centre-majority −1.92 (20/48) vs outskirts +0.83 — the zonal split
  (sequence −3.85 → −1.81 → −0.90 → −1.92), NOT bookable to TOD; (3)
  replay D44 (tod h18=0.99) flat (119/315 signed 18 vs NEW 119/315
  signed 19), D45 wrong-way worse (119/317, signed 22, devM 220).
  **TOD_PACE h18 stays 1.0** — final at the next round on the full hour.
- True feed speed: fleet p50 21.9/21.7 km/h (h17 tail / h18), centre
  18.4*/19.0 (*12-min tail slice 17:48–18:00, thin sample — the
  round-17 full-hour h17 read was 18.9; flagged as noise, not a pierce),
  out 23.8/23.6. **h18 = fifteenth consecutive structural-corridor hour;
  h19+ upward release NOT yet** — watch continues.
- Replay gate on the window extract (255 trams, n_frs=634): OLD 124.6 →
  NEW 119.1 (**−4.4%, eighteenth re-validation**); fresh p90 416.9 →
  375.1.
- R5 копилка (closed; FOURTEENTH reading for the record): vs 0.62 FULL
  **0.650 (x1.048, 178/76)**, ALL 8 disjoint subsamples above
  (x1.032–x1.081); vs 0.66 FULL 0.640 x0.970 (114/141). Running log: …
  16:37 → 0.650, 17:48 → 0.660, **18:29 → 0.650**.

### (в) R8 band part 4 (h18) — REPRODUCES (eleventh)

- Censored per-dwell MID p50 centre/outskirts **104/84 s = x1.24** —
  mid-band (x1.20–x1.27 intact across rounds 8–18).
- Uncensored zone time-budget: centre **[13.9, 33.4]** vs out
  **[9.6, 22.7]** s/stop (1380/2990 stop-visits), midpoints 23.7 vs
  16.2 — the ~24 vs ~17 shape holds.
- Fleet-level TOD_DWELL: h17 [10.8, 24.2], h18 [11.0, 26.8] s — 18 s
  inside both; evidence gate FAIL → **stays 1.0**. Centre dwell watch:
  MID p50 104 s < 115 s — zonal entry NOT triggered.
- Gate-1: остаётся ночная полоса — NB она будет **воскресно-ночной**
  (полоса зонально-структурная, воспроизведена во всех дневных режимах,
  так что для gate-1 приемлемо; пометить в записи).

### Applied (engine)

**Nothing — no engine constant passed its gate.** TOD_PACE h17 = 1.0
FINAL, h18 stays 1.0, TOD_DWELL h17/h18 stays 1.0. R11: no
implementation, no prototype this round (inconclusive window). Tooling:
`replay.py` D44/D45 added (round-18 gate record, with the Sunday flag).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 19 (ночное окно, ≥22:00)

1. **R8 ночная полоса** — последний кусок gate-1 (censored ratio
   x1.20–x1.27 И разделённые бюджеты ~24 vs ~17). Перед раундом
   проверить здоровье приложения (stall-класс, аптайм ~11 ч к 22:00);
   при повторении класса — перезапуск ДО набора полосы.
2. **R11 решающий release-тест на ночном окне**: `plateau_diag.py`;
   вход = фид реально сменился (fleet p50 ≥ 23–24 км/ч и/или доля
   ΔdelayS<−10 существенно ниже 52%). Предсказание механизма: steady →
   −10…−15, on_track → −45…−55. Фид сменился + плато удержалось =
   пересмотр; фид не сменился = тест снова не активирован (честно
   фиксировать).
3. **TOD h18 финал** (полный час) + h19/h20+ первые замеры — стандартный
   двойной гейт; все ночные значения помечать Sunday-night.
4. **h19+ centre upward release watch** (выход из 18.9–19.8 вверх).
5. **Буднее подтверждение (перенос на будни)**: paired-склон центра,
   «пиковые» уровни плато, 15:00-boundary recheck (dormant) — всё
   сегодняшнее промаркировано воскресеньем.

### Open items after round 18

- Round-19 pre-registration above (ночь: R8 полоса + R11 решающий тест).
- **R11 PROPOSAL остаётся FILED** (механизм: 4 окна воспроизводимости;
  решающее дискриминационное окно — ночь или будний спад пика).
- **DOW-каветка NEW**: все TOD-вердикты 2026-07-12 = Sunday; любая
  будущая запись ≠1.0 требует буднего подтверждения.
- **R8**: night band tonight (Sunday-night пометка), then live gate 2.
- Carried: %ahead-vs-device (needs device ride), h19+ release watch,
  15:00-boundary recheck (теперь: в будний день).
- Pipeline: stall-класс (15:55, 18:23–18:29) — 2 случая; следить,
  рестарт приложения перед ночной полосой при повторении.

### Post-round-18 note (orchestrator): sim-stall class root-caused as HOST LOAD
Both "app stall" events correlate exactly with heavy load on the host Mac, not with app
defects: 15:54–15:59 = the local EAS device build (xcodebuild, all cores; IPA timestamp
15:57); 18:23–18:29 = round-18's own analysis pass over the 391 MB session file. The
simulator's JS thread gets starved when the host is saturated — real devices never see
this class. Interpretation rules going forward: (1) exclude windows overlapping local
builds / heavy analysis from clean readings (as round 18 already did); (2) no app restart
needed before the night window unless a stall occurs WITHOUT host load; (3) run heavy
one-off jobs (builds) outside analysis windows when possible.

## Day round 19 — 2026-07-12 19:34–19:50 (HEADLINE: the h19 bias drop is the 19:00 daytime-cap boundary, not TOD; h18 final + h19 first read both 1.0; SUNDAY evening)

### Pipeline health

- Harvester PID 31497 alive (ticks through 19:30, 421 M drained); app PID
  33035 alive (~8 h 41 m uptime); Metro 8081 up, simulator booted. The
  harvest.log flat-size stretch (18:59 → 404 M, 19:09 → 404 M, 19:20 →
  405 M, then 19:30 → 421 M) looked like a stall but is a drain/reporting
  artifact: the DATA is continuous — 722 polls, cadence p50 5.0 / p90 5.5
  / max 15.8 s, **zero gaps > 20 s**, per-minute record counts flat
  2.0–2.4 k across the whole 18:59–19:20 stretch. No host-load stall this
  window; no app restart needed before the night band. NB the round-18
  stall recovery tail is visible at the window edge (18:29–18:35 at-fix
  −87/−61) and is excluded from clean reads.
- **Orchestrator-note compliance**: this round's own heavy analysis pass
  ran ~19:36–19:50 — round 20 must treat any sim stall overlapping that
  window as host-load class (exclude, don't restart).

### Window (analysis on data NEW since round 18)

`--since 2026-07-12 18:29` → 19:30 (61.1 min), 257 trams, 130.7 k
day-line records, 100% v2, night routes 0%. Cuts: h18 tail 18:29–19:00,
h19 19:00–19:30; h18 FULL hour re-cut 18:00–19:00 from the day file
(126.6 k records) for the final.

### (а) КАЛИБРОВОЧНОЕ ОТКРЫТИЕ: 19:00 — граница дневного центр-капа; h19-нормы структурно несравнимы с дневными

- **The raw read that triggered it**: h19-only vs the 0.66 norm
  **x0.939** (76/170) with ALL 8 disjoint subsamples BELOW
  (x0.924–x0.955) — the FIRST hour outside the h12–h18 x0.962–x0.985
  corridor. Taken alone this would look like the first real TOD_PACE
  candidate of the day.
- **But the feed moved NOWHERE**: true feed speed h18→h19 21.7→**21.9**
  km/h (centre 18.5→19.0, out 23.5→23.7), dwell MID 92→89 s, dwell/stop
  budgets flat, delayS p50 38–47 flat, deep-recovery share 52.7% ≈ 52%.
  Paired within-tram h18→h19 (248 trams): fleet Δspeed **+0.42** km/h
  (134/114, null-to-up) while fleet Δbias **−0.030** (86/152) — the two
  legs CONTRADICT, and the bias drop is entirely zonal: centre-majority
  Δbias **−0.110 (3/59!)** vs outskirts −0.010, with centre Δspeed only
  −1.52.
- **Механизм (подтверждён кодом)**: `zoneCapAt` (speedProfile.ts:207)
  applies the centre 8.6 m/s cap only while `daytime` = 07:00–19:00
  Prague (engine.ts:106); at 19:00 all profiles rebuild capless and
  `updatePaceBias` (tramSim.ts:504, via `meanCruiseCapOver`) starts
  measuring centre spans against ~11.7 m/s instead of 8.6 → at constant
  real speed the learned centre bias must fall by ~x0.74 (observed
  centre-majority x0.83 with partial-zone spans — matches). **Перекрёстная
  сверка**: h19 fleet bias lands exactly on **0.62** — the R5 prior
  learned from NIGHT (capless) data; h19-only vs 0.62 reads **x1.008**
  with mixed subsamples (x0.968–x1.032) = neutral in the capless frame.
  The daytime fleet neutral 0.65–0.66 and the night prior 0.62 are the
  SAME fleet in two cap regimes.
- **Следствия (методология)**: (1) post-19:00 bias-norm reads vs the
  0.66 daytime neutral are INVALID as TOD evidence — for h19+ use the
  capless frame (vs 0.62) + the paired feed-truth leg + replay; (2) the
  same discontinuity in reverse must exist at 07:00 — предсказание для
  будней/утра: centre-bias jump UP at 07:00 при плоском фиде (проверка =
  подтверждение механизма со второй стороны); (3) R5-копилка: записи
  после 19:00 в другом режиме — flag mixed/capless in the running log.

### (б) TOD h18 FINAL + h19 first read — both stay 1.0

- **TOD_PACE h18 FINAL (full hour 18:00–19:00, 255 trams)**: (1) norm vs
  0.66 **x0.985** (116/139), ALL subsamples x0.970–x0.985 — corridor
  level; (2) paired h17→h18 fleet null (round 18); (3) replay D44
  (h18=0.99) on the full hour **flat** (|err| 116/313 signed 17 vs NEW
  116/313 signed 17), D45 wrong-way control worse (signed 19, frs p90
  379 vs 370); full-hour gate OLD frs p50 127 → NEW 121 (−4.7%).
  **TOD_PACE h18 = 1.0 FINAL** (Sunday value).
- **TOD_PACE h19 (first read, :00–:30)**: sample gate PASS (n_fix 4802,
  247 trams). Evidence gate FAIL: (1) the norm leg's x0.939 is
  DISCREDITED as TOD evidence (cap boundary, §а); in the capless frame
  vs 0.62 it reads x1.008 mixed = neutral; (2) paired feed-truth leg
  null (Δspeed +0.42; Δbias −0.030 fully cap-explained); (3) replay D46
  (h19=0.95, norm-implied, cap-contaminated) marginal (115/308 signed 17
  vs NEW 116/308 signed 19 — within the historical flat band), D47
  wrong-way worse (116/310 signed 21). **TOD_PACE h19 stays 1.0** —
  final at the next round on the full hour, capless-frame legs only.
- True feed speed: fleet p50 21.7 (h18 full) / 21.9 (h19); centre
  **18.7** (h18 full) / **19.0** (h19), out 23.5/23.7. NB h18 full-hour
  centre 18.7 dips 0.2 BELOW the 18.9–19.8 corridor floor (h19 recovers
  to 19.0) — a downward wobble at the boundary hour, NOT the upward
  release the watch is armed for; sixteenth/seventeenth
  structural-corridor hours. **h19/h20 upward release NOT yet.**
- Replay gate on the window extract (257 trams, n_frs=786): OLD frs p50
  131 → NEW 119 (**−9%, nineteenth re-validation**); frs p90 376 → 366.
- R5 копилка (closed; FIFTEENTH reading, **mixed-regime — first spanning
  the 19:00 cap boundary**): vs 0.62 FULL 0.640 (x1.032, 164/93), halves
  x1.065 (capped) / x1.016 (capless); vs 0.66 FULL 0.630 x0.955
  (83/174). Running log: … 17:48 → 0.660, 18:29 → 0.650, **19:30 →
  0.640 (mixed)**. Post-19:00 the fleet neutral reverts toward the night
  prior 0.62 — R5's 0.62 validated for the capless regime from the day
  side.

### (в) R8 band parts 5–6 (h18 full, h19) — REPRODUCES (twelfth, thirteenth)

- Censored per-dwell MID p50 centre/outskirts: h18 full **104/84 =
  x1.24**, h19 **102/82 = x1.24** — mid-band (x1.20–x1.27 intact across
  rounds 8–19).
- Uncensored zone time-budget: h18 centre [14.4, 34.3] vs out
  [10.1, 23.6] s/stop (midpoints 24.4/16.9); h19 centre [14.2, 33.9] vs
  out [10.3, 23.8] (24.1/17.1) — the ~24 vs ~17 shape holds.
- Fleet-level TOD_DWELL: h18 [11.4, 27.0], h19 [11.5, 27.0] s — 18 s
  inside both; evidence gate FAIL → **stays 1.0**. Centre dwell watch:
  MID p50 104/102 < 115 s — zonal entry NOT triggered.
- Gate-1: остаётся ночная полоса (22:00+, Sunday-night пометка).

### (г) R11 watch — decisive window STILL not arrived; plateau unchanged; cap boundary adds a consistency point

- **Entry condition check**: fleet true feed speed 21.9 km/h (< 23–24),
  deep-recovery ΔdelayS<−10 share 52.7% (2387/4527 — unchanged from
  52%). **Фид не расслабился — решающий тест опять не активирован**
  (честно: третье окно подряд «без изменений при неизменном входе»).
- Plateau level (clean, stall-recovery edge excluded): window all −28
  (second half −22.0), prev=on_track **−73/−66.5**, prev=at_stop **−2**
  (invariant intact); 5-min slices −14.5…−49 no trend. Mechanism 5th
  reproduction: OLS slope **+4.18** m/s (historical +4.07…+4.55);
  ΔdelayS bands monotonic (−128/−42/−9.5/+11/+89).
- **Новый consistency point**: the 19:00 cap release (bias re-learning,
  §а) did NOT move the plateau (on_track −66.5 in h19 vs −69/−70 in
  h16–h18) — согласуется с проекционной (schedule-pace), а не
  bias/cap-стороной механизма.

### Applied (engine)

**Nothing — no engine constant passed its gate.** TOD_PACE h18 = 1.0
FINAL, h19 stays 1.0 (final next round, capless-frame legs), TOD_DWELL
h18/h19 stays 1.0. R11 decisive test not activated (feed regime
unchanged). Tooling: `replay.py` D46/D47 added (round-19 gate record with
the cap-boundary note).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 20 (ночное окно, ≥22:00)

1. **R8 ночная полоса** — последний кусок gate-1 (Sunday-night пометка).
   Health-check перед набором; мой анализ-проход 19:36–19:50 исключить
   из чистых чтений, если он попал в дренаж.
2. **R11 решающий release-тест**: вход = fleet p50 ≥ 23–24 км/ч и/или
   deep-recovery доля существенно < 52%. Предсказание: steady →
   −10…−15, on_track → −45…−55. Фид сменился + плато удержалось =
   пересмотр; не сменился = снова честное «не активирован».
3. **TOD h19 финал** (полный час) + h20+ первые замеры — **только
   capless-frame леги**: норма vs 0.62 (не 0.66!), paired feed-truth,
   replay. Все значения Sunday-night.
4. **Центр-коридор**: h18 18.7 — нижний вобл на границе капа; следить,
   вернётся ли 18.9–19.8 или это начало вечернего спуска; upward
   release watch продолжается.
5. **07:00-boundary check (будни, утро)**: предсказание механизма §а —
   центр-bias перескок ВВЕРХ на 07:00 при плоском фиде; подтверждение
   капового механизма со второй стороны. Плюс перенесённые будничные
   проверки (пики, 15:00-boundary).

### Open items after round 19

- Round-20 pre-registration above (ночь: R8 полоса + R11 тест + h19
  финал в capless-frame).
- **NEW: 19:00/07:00 cap-boundary methodology caveat** — post-19:00
  bias-norms несравнимы с дневным 0.66-нейтралом; vs 0.62 = capless
  frame; 07:00 reverse-check pending (weekday morning).
- **R11 PROPOSAL остаётся FILED** (5 окон воспроизводимости механизма;
  решающее окно всё ещё не наступило).
- **R8**: night band tonight, then live gate 2. Band x1.20–x1.27.
- Carried: %ahead-vs-device (needs device ride), upward centre release
  watch, weekday confirmations (пики, 15:00-boundary, DOW-каветка).

## Day round 20 — 2026-07-12 20:32–20:55 (HEADLINE: a THIRD stall variant — chronic periodic app-side degradation at ~9 h uptime, NO host load; app restarted 20:39:51; h19 FINAL 1.0 in the capless frame; h20 first read 1.0; SUNDAY pre-night)

### Pipeline health — CHRONIC STALL CLASS, app restarted

- Harvester PID 31497 alive (ticks through 20:31 + post-restart 20:41);
  Metro 8081 up, simulator booted. Data continuous through 20:31 drain +
  manual drain to 20:39 (1 880 693 lines total).
- **NEW stall variant (third)**: the whole 19:31–20:39 stretch shows a
  PERIODIC (~every 4 min) sim-tick integration collapse (fleet cruise
  ΔsimDist/expected p50 dipping to 0.44–0.68, p25 to 0.05–0.13) with the
  at_stop invariant breaking in the same minutes (prev=at_stop at-fix
  p50 −40…−200 vs the healthy −2), poll cadence gaps up to 50 s, thin
  minutes every 4–8 min. Unlike 15:55/18:23 this is NOT host-load: host
  12-core, 67% idle, load ~4 at 20:37, no builds/analysis running
  19:50–20:31 — but the APP process was pinned at 99.7% CPU with ~9 h
  uptime. No JS reloads (bias=1.0 share ~0%, no key re-influx).
  **Action per runbook (stall without host load): manual container drain
  20:39, app restart 20:39:51** (new PID 2869). Post-restart check
  20:41–20:44: integration p50 0.992–0.996, p25 0.93–0.95, app CPU
  11.6% — clean. Class root cause: app-side degradation at high uptime
  (GC/leak-like periodic starvation); **recommendation: proactive app
  restart every ~6–8 h of uptime**, done before tonight's night band.
- **Consequences for this round's reads**: ALL probe-level (at-fix /
  plateau / OLS) numbers in 19:31–20:39 are stall-contaminated and
  EXCLUDED as clean evidence. Feed-side legs (true feed speed, dwell
  MID/budgets via Δ(obsAt)/Δ(obsDist), delayS composition, bias norms)
  remain valid — the feed's own clock is unaffected by sim starvation
  (fix capture thinned by poll gaps; same for all configs in replay).
  Fresh sims post-20:39:51: bias reseeded, converged reads valid ~20:55+.
- Round-19 note compliance: my own 19:36–19:50 analysis pass was inside
  the stall stretch anyway; the stall is chronic on both sides of it, so
  the class attribution above stands. Round 21: exclude ~20:40–20:55
  (this round's replay pass) from clean probe reads.

### Window (analysis on data NEW since round 19)

`--since 2026-07-12 19:30` → 20:39 (68.9 min), 265–267 trams, 120.9 k
day-line records, 100% v2, night routes 0%. Cuts: h19 full hour re-cut
19:00–20:00 (115.8 k) for the final; h20 20:00–20:39 (70.6 k).

### (а) TOD h19 FINAL + h20 first read — both stay 1.0 (capless frame per the round-19 boundary discovery)

- **TOD_PACE h19 FINAL (full hour 19:00–20:00, 257 trams, capless legs
  only)**: (1) norm vs **0.62** (night prior, NOT the daytime 0.66):
  **x0.968** FULL (102/155), subsamples MIXED — halves x1.016/x0.968,
  parity x0.968/x0.968, 4-way x0.960–x1.016 (half A ABOVE) — no
  consistent side; (2) paired feed-truth leg h18→h19 null (round 19:
  Δspeed +0.42, Δbias cap-explained); (3) replay D46 (h19=0.95) on the
  full hour FLAT (|err| p50 123 vs NEW 123, signed 22 vs 24, frs p90 345
  vs 352 — marginal, not a gate pass), D47 wrong-way worse (signed 26,
  frs p90 356); full-hour gate OLD 128.3 → NEW 123.0 (−4.1%).
  **TOD_PACE h19 = 1.0 FINAL** (Sunday value, capless frame).
- **TOD_PACE h20 (first read, :00–:39)**: sample gate PASS (262 trams,
  70.6 k records). Evidence gate FAIL ×3: (1) norm vs 0.62 **x0.968**
  FULL (103/159), subsamples MIXED (halves x0.968/x0.976, parity
  x0.984/x0.968, 4-way x0.968–x1.016); (2) paired within-tram h19→h20
  (250 trams) fleet null — Δbias **+0.000** (113/124), Δspeed **+0.65**
  km/h (143/107); centre-majority Δbias −0.030 / Δspeed −1.03 (25/39)
  vs outskirts +0.010/+0.99 — the familiar zonal split, NOT TOD; (3)
  replay D48 (h20=0.97) FLAT (127/345 signed 25 vs NEW 127/345 signed
  26; frs p50 149 vs 146, p90 418 vs 428 — wash), D49 wrong-way worse
  (signed 28). **TOD_PACE h20 stays 1.0** — final next round on the
  full hour.
- True feed speed: fleet p50 **21.7** (h19 full) / **22.2** (h20);
  centre **18.8**/**19.0**, out 23.4/23.9. h19 full-hour centre 18.8 =
  same 0.1-below-floor wobble class as h18's 18.7 (r19); h20 back to
  19.0. Eighteenth/nineteenth structural-corridor hours; **upward
  release NOT yet** — watch continues into the night.
- Replay gate on the h20 extract (262 trams, n_frs=634): OLD 133.4 →
  NEW 127.2 (**−4.7%, twentieth re-validation**); frs p90 457 → 428.
- R5 копилка (closed; SIXTEENTH reading, second capless): round window
  vs 0.62 FULL **0.590 (x0.952, 91/174)**, h19full/h20 cuts 0.600
  (x0.968 both, mixed subsamples). Running log: … 18:29 → 0.650,
  19:30 → 0.640 (mixed), **20:39 → 0.590 (capless)**. NB the capless
  fleet neutral is reading slightly BELOW the night-learned 0.62
  (x0.95–0.97, not yet consistent across subsamples; stall-window
  caveat applies) — **capless-neutral watch NEW**: if the night rounds
  keep it consistently below, the 0.62 prior may want a re-estimate
  through the R5 double-gate.

### (б) R8 band part 7 (h20) — REPRODUCES (fourteenth)

- Censored per-dwell MID p50 centre/outskirts **104/83 s = x1.25** —
  mid-band (x1.20–x1.27 intact across rounds 8–20).
- Uncensored zone time-budget: centre **[13.7, 33.6]** vs out
  **[9.4, 22.1]** s/stop (1272/3087 stop-visits), midpoints 23.7 vs
  15.8 — the ~24 vs ~17 shape holds.
- Fleet-level TOD_DWELL: h19 [11.0, 25.9], h20 [10.7, 25.4] s — 18 s
  inside both; evidence gate FAIL → **stays 1.0**. Centre dwell watch:
  MID p50 104 s < 115 s — zonal entry NOT triggered.
- Gate-1: остаётся ночная полоса (22:00+, Sunday-night пометка) —
  приложение к ней перезапущено, здоровье чистое.

### (в) R11 — decisive window STILL not arrived (fourth consecutive); probe-level plateau reads THIS window discarded (stall)

- **Entry condition check (feed-side, valid)**: fleet true feed speed
  h20 **22.2** km/h (< 23–24), deep-recovery ΔdelayS<−10 share
  **51.8%** (1303/2514 — unchanged from 52%). **Фид не сменился —
  решающий тест опять не активирован** (честно: четвёртое окно подряд).
- Plateau depth / OLS / class splits for 19:31–20:39: measured (all
  −86, on_track −121, OLS +3.81) but **DISCARDED as mechanism evidence**
  — the stall class deepens at-fix probes fleet-wide (sim behind by
  starvation, not by projection drag); NOT counted as a reproduction.
- Night window tonight is the decisive candidate: night fleet
  historically fast; probe-level trustworthy again post-restart.

### Applied (engine)

**Nothing — no engine constant passed its gate.** TOD_PACE h19 = 1.0
FINAL (capless frame), h20 stays 1.0, TOD_DWELL h19/h20 stays 1.0.
R11: not activated, no probe-level evidence booked this round (stall).
Tooling: `replay.py` D48/D49 added (round-20 gate record with the
stall-class note). Ops: manual container drain + app restart 20:39:51.

jest + tsc: run this round (see commit).

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 21 (ночное окно, ≥22:00)

1. **R8 ночная полоса** — последний кусок gate-1 (Sunday-night
   пометка). Приложение свежее (20:39:51); health-check стандартный.
2. **R11 решающий release-тест** — probe-level снова доверенный
   (рестарт). Вход: fleet p50 ≥ 23–24 км/ч и/или deep-recovery доля
   существенно < 52%. Предсказание: steady → −10…−15, on_track →
   −45…−55. Фид сменился + плато удержалось = пересмотр.
3. **TOD h20 финал** (полный час 20:00–21:00; NB 20:39–20:41
   рестарт-дыра и свежие симы — bias-леги на 20:41+ данных только с
   ~20:55) + h21/h22 первые замеры — capless-рамка (vs 0.62).
4. **Capless-neutral watch (NEW)**: копилка vs 0.62 читается x0.95–0.97
   — следить за консистентностью по ночным окнам; re-estimate только
   через R5-двойной гейт.
5. **Центр-коридор**: 18.8–19.0 у пола; ночной upward release watch.
6. **Health**: исключить ~20:40–20:55 (replay-проход этого раунда) из
   чистых чтений; следить за новым stall-классом — при рецидиве на
   СВЕЖЕМ приложении (< 2 ч аптайма) это уже не uptime-класс, копать.

### Open items after round 20

- Round-21 pre-registration above (ночь: R8 полоса + R11 тест + h20
  финал + capless-neutral watch).
- **NEW: uptime-stall class (third variant)** — периодическая (~4 мин)
  деградация приложения на ~9 ч аптайма без host-load; профилактика:
  рестарт каждые ~6–8 ч. 19:31–20:39 probe-level данные бракованы.
- **R11 PROPOSAL остаётся FILED** (5 чистых окон воспроизводимости;
  это окно НЕ засчитано — стойла; решающее окно — ночь).
- **R8**: night band tonight, then live gate 2. Band x1.20–x1.27.
- Carried: %ahead-vs-device (needs device ride), upward centre release
  watch, weekday confirmations (пики, 15:00/07:00-boundary,
  DOW-каветка), 19:00/07:00 cap-boundary methodology caveat.

## Day round 21 — 2026-07-12 21:11–21:45 (HEADLINE: R11 plateau RELEASE observed at the pre-registered levels — but pinned to the 20:39:51 app restart, NOT a feed change: the h16–h19 deepening retro-attributes to the uptime-stall class; h20 FINAL 1.0; h21 first read 1.0; capless-neutral 0.59–0.60 did NOT persist; SUNDAY pre-night)

### Pipeline health — CLEAN, no stall recurrence on the fresh app

- Harvester PID 31497 alive (ticks through 21:32, 468M); app fresh
  (PID 2869, launched 20:39:51, 13.6% CPU at 56 min uptime); Metro 8081
  up; host load ~3.9 with no builds. Data through 21:32 (last harvest).
- **Stall-recurrence check (pre-registered)**: per-minute sim-tick
  integration 20:55→21:32 — p50 0.99–1.00, p25 0.94–0.95 EVERY minute,
  records steady ~2.2 k/min, no thin minutes, no periodic dips.
  **No recurrence at <1 h uptime** — consistent with the uptime-class
  hypothesis (round 20); proactive-restart recommendation stands.
- 20:40–20:55 excluded from clean probe reads per round-20
  pre-registration (round-20 replay pass); used only as a caveated
  peek below. NB my own analysis pass loaded the host ~21:36–21:43
  (6 parallel 486 MB scans + 2 replays) — AFTER the last harvested
  record (21:32), so THIS round's data is untouched, but **round 22:
  exclude ~21:35–21:45** from clean probe reads (next harvest ingests
  that stretch).

### Window (analysis on data NEW since round 20)

`--since 2026-07-12 20:39` → 21:32 (53.3 min), 266–270 trams, 116.8 k
day-line records, night routes 0%. Cuts: h20 full hour re-cut
20:00–21:00 (114.8 k, restart hole 20:39–20:41) for the final; h21
21:00–21:32 (71.5 k, fully post-restart, all sims ≥20 min converged).

### (а) HEADLINE — R11: the plateau released at the PREDICTED levels, with an UNCHANGED feed, coincident with the app restart

- **Clean post-restart probe window 20:55–21:32** (6141 probes, fresh
  app, trusted): at-fix signed p50 **−2.0** (per-5-min buckets −2…−5),
  prev=on_track **−41.5**, prev=at_stop **0.0** (invariant intact),
  centre **−2** vs outskirts **−2** (the zonal depth split is GONE),
  steady ΣΔdelayS∈[−5,+5) **−14.0**, steady-by-level [0,60) −2.0 /
  [60,∞) −7.0. Compare the last CLEAN plateau reads (r17/r18 recuts,
  16:37–17:48): all −22…−26, on_track −66…−70, steady −41…−45.5.
  **The round-17/18 release prediction (steady → −10…−15, on_track →
  −45…−55) is now satisfied** — steady −14 inside the band, on_track
  −41.5 just shallow of it.
- **But the feed did NOT change**: fleet true feed speed h20→h21
  22.2→**22.3** km/h (< 23–24), deep-recovery ΔdelayS<−10 probe share
  **52.0%** (1472/2832 — the constant 52%), fleet delayS p50 26–36 —
  flat. The pre-registered R11 decisive-test ENTRY condition has still
  never fired (fifth consecutive window).
- **Caveated peek 20:41–20:55** (excluded stretch, host-load caveat):
  the release was already fully present in the FIRST post-restart
  bucket — 20:40 all −2.0 / on_track −44.5; the single deeper bucket
  (20:45: −7/−66) coincides exactly with round-20's replay host-load
  pass, then back to −2/−38.5. **The release was INSTANT at the
  restart**, not a gradual evening decay.
- **Mechanism reproduction (6th window, healthy app)**: OLS slope
  **+4.59** m per ΔdelayS second (historical +4.07…+4.55 — top of
  band), ΔdelayS bands monotonic (<−10 → −109, −3…+3 → +3, >+10 →
  +108), at_stop control −80 under recovery. The structural
  schedule-pace projection-drag component stands.
- **RETRO-ATTRIBUTION (new)**: with feed observables flat across
  h17→h21 and the release pinned to the restart, the parsimonious read
  is that the h16–h19 plateau DEEPENING (all −22…−26, steady −25…−45,
  uptime 4.5–8.5 h) was substantially the SUBCLINICAL stage of the
  round-20 uptime-stall class, not peak feed physics. The uptime
  gradient (morning ~fresh-run −10 → h15 ~3.5 h −25 → h17 ~5.5 h −41
  → post-restart −14) is monotonic in uptime; the TOD story required a
  release that feed observables never showed. h15–h19 probe-DEPTH
  numbers now carry a retroactive uptime caveat (TOD finals unaffected
  — they gated on norms/paired/replay, not plateau depth; the
  mechanism-slope reproductions stand — slope stable across app
  states).
- **R11 PROPOSAL DOWNGRADED (stays FILED, urgency reduced)**: the
  healthy-app fleet level is ALREADY −2…−3 (the proposal's target was
  −22 → −5…−10); the remaining gap is the on_track corridor −41…−49 ≈
  designed drag + TRAIL_M (~7 s of run at 22 km/h, self-resyncing at
  every stop). Implementation condition tightened: first demonstrate a
  materially deep plateau on a HEALTHY app (weekday peak, fresh app —
  see round-22+ pre-registration); the night window remains valuable
  as a SLOPE/level test under a genuinely faster feed.

### (б) TOD h20 FINAL + h21 first read — both 1.0 (capless frame vs 0.62)

- **TOD_PACE h20 FINAL (full hour 20:00–21:00, 268 trams, capless)**:
  (1) norm vs 0.62 **x0.968** FULL (102/166), subsamples halves
  x0.968/x0.984, parity x0.968/x0.968, 4-way x0.968–x0.984 — all
  at-or-below but ≤3.2%, the h19-final class; (2) paired h19→h20 null
  (round 20: Δbias +0.000, Δspeed +0.65 zonal); (3) replay D48
  (h20=0.97) on the full-hour extract FLAT (|err| p50 122 vs NEW 122,
  signed 22 vs 23, frs p90 391 vs 393 — marginal, not a gate pass),
  D49 wrong-way worse (123, signed 25). **TOD_PACE h20 = 1.0 FINAL**
  (Sunday value, capless frame).
- **TOD_PACE h21 (first read, :00–:32)**: sample gate PASS (260 trams,
  71.5 k records). Evidence gate FAIL ×3: (1) norm vs 0.62 **x1.000
  EXACT** (130/130 — textbook neutral), subsamples mixed x0.968–x1.048;
  (2) paired within-tram h20→h21 (249 trams) — fleet Δbias **+0.020**
  (133/100), Δspeed **+0.81** km/h (143/106); centre-majority −0.040/
  −2.21 (17/38) vs outskirts +0.030/+1.73 — the familiar zonal split
  with a WEAK fleet-up hint (night acceleration watch); (3) replay
  symmetric controls D50 (0.95) / D51 (1.05) both flat-to-worse (frs
  p50 134/134 vs NEW 132; signed 14/19 vs 17). **TOD_PACE h21 stays
  1.0** — final next round on the full hour.
- True feed speed: fleet p50 **22.2** (h20 full) / **22.3** (h21);
  centre **18.9**/**19.1**, out 23.9/24.1. Twentieth/twenty-first
  structural-corridor hours; centre still at the 18.8–19.1 floor —
  **upward release NOT yet**; night watch continues.
- Replay gate re-validations: h20 full extract OLD 128.2 → NEW 122.3
  (**−4.5%, twenty-first**), frs p90 426 → 393; h21 extract OLD 125.3
  → NEW 121.4 (**−3.0%, twenty-second**), frs p90 426 → 408.
- R5 копилка (closed; SEVENTEENTH reading): round window vs 0.62 FULL
  **0.610 (x0.984, 117/149)**, halves x0.984/x1.016 MIXED; h20full
  0.600 (x0.968), h21 **0.620 (x1.000)**. Running log: … 19:30 →
  0.640 (mixed), 20:39 → 0.590 (capless, stall-contaminated), **21:32
  → 0.610 (capless, mixed)**. **Capless-neutral watch: the 0.59–0.60
  did NOT persist** — h21 reads exactly at prior, round window x0.984
  with halves straddling 1.0. Verdict so far: capless neutral 0.60–0.62
  ≈ prior; the round-20 low reading attributes to the stall window. No
  R5 re-estimate path; keep the watch through tonight's night rounds.

### (в) R8 band part 8 (h20 full + h21) — REPRODUCES (fifteenth)

- Censored per-dwell MID p50 centre/outskirts **101/82 s = x1.23** —
  mid-band (x1.20–x1.27 intact across rounds 8–21; n=950/1677).
- Uncensored zone time-budget: centre **[13.6, 33.2]** vs out
  **[9.4, 22.7]** s/stop (2993/7240 stop-visits), midpoints 23.4 vs
  16.1 — the ~24 vs ~17 shape holds.
- Fleet-level TOD_DWELL: h20 [10.5, 25.5], h21 [10.8, 26.3] s — 18 s
  inside both; evidence gate FAIL → **stays 1.0**. Centre dwell watch:
  MID p50 101 s < 115 s — zonal entry NOT triggered.
- Gate-1: остаётся ночная полоса (22:00+, Sunday-night пометка) —
  round 22.

### Applied (engine)

**Nothing — no engine constant passed its gate.** TOD_PACE h20 = 1.0
FINAL (capless frame), h21 stays 1.0, TOD_DWELL h20/h21 stays 1.0.
R11: proposal downgraded (stays FILED, no implementation); the release
observation is booked as attribution evidence, not as the decisive
feed-triggered test (entry condition never fired). Tooling:
`replay.py` D50/D51 added (h21 symmetric controls).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 22 (ночное окно, ≥22:11)

1. **R8 ночная полоса (22:00+)** — ПОСЛЕДНИЙ кусок gate-1
   (Sunday-night пометка). dwell_v2 на h22; кросс с x1.20–x1.27 и
   [~24 vs ~17] бюджетами.
2. **R11 ночной SLOPE/level тест (reframed после этого раунда)**:
   вход прежний (fleet p50 ≥ 23–24 км/ч и/или deep-recovery доля
   существенно < 52%). Предсказание механизма на ЗДОРОВОМ приложении:
   steady держится −10…−15, on_track углубляется ∝ v (наклон
   +4.1…+4.6 сохраняется), all остаётся −2…−10. **Если all уходит
   глубже −20 на свежем приложении при плоском фиде — пересмотр
   uptime-атрибуции** (значит деградация не uptime-класс).
3. **TOD h21 финал** (полный час 21:00–22:00) + h22 первый замер —
   capless-рамка (vs 0.62), стандартный двойной гейт.
4. **Capless-neutral watch**: копилка 0.600/0.620/0.610 за три
   capless-чтения — консистентного смещения НЕТ; продолжать логировать,
   re-estimate только если ночные окна дадут устойчивый одно-сторонний
   сдвиг по всем subsample.
5. **Центр-коридор**: 18.8–19.1 у пола 20+ часов подряд — ночной
   upward release watch (фиксировать час выхода).
6. **Health**: исключить ~21:35–21:45 из чистых probe-чтений (мой
   анализ-проход, host-load; данные попадут в harvest 21:42+).
   Приложение свежее (20:39:51) — к раунду 22 аптайм ~1.5–2 ч; рецидив
   стойла = НЕ uptime-класс, копать глубже. Профилактический рестарт
   не нужен до ~03:00.
7. **После полуночи**: перейти на `analysis-2026-07-13.md` (recap-шапка
   + перенос open items), harvester начнёт `sim-2026-07-13.jsonl`
   автоматически; R8/TOD кросс через date boundary вести по обоим
   файлам (`--since`/`--until` поддерживают мульти-файл в
   night_profile/plateau_diag; dwell_v2/day_prior — один файл, подавать
   оба конкатенацией при необходимости).

### Open items after round 21

- Round-22 pre-registration above (ночь: R8 полоса финал + R11
  slope-тест + h21 финал + midnight rollover).
- **R11 PROPOSAL: FILED, DOWNGRADED** (healthy-app level уже −2…−3;
  остаток = designed on_track drag −41…−49; имплементация только после
  демонстрации глубокого плато на здоровом приложении — weekday peak
  с fresh app, restart перед h15).
- **NEW: h15–h19 probe-depth retro-caveat** — uptime-градиент
  деградации; глубины тех окон не сравнивать с post-restart окнами
  без поправки. TOD-финалы h15–h19 НЕ затронуты.
- **Uptime-stall class**: no recurrence at <1 h uptime (this round's
  check clean); профилактика рестартом каждые ~6–8 ч подтверждена как
  рабочая.
- **R8**: night band tonight (gate-1 final), then live gate 2. Band
  x1.20–x1.27.
- Carried: %ahead-vs-device (needs device ride), upward centre release
  watch (18.8–19.1 floor, 20+ h), weekday confirmations (пики,
  15:00/07:00-boundary, DOW-каветка), 19:00/07:00 cap-boundary
  methodology caveat, capless-neutral watch (нейтрально пока).

## Day round 22 — 2026-07-12 22:11–22:55 (НОЧНОЙ ФИНАЛ воскресной программы. HEADLINE: R8 gate-1 ЗАКРЫТ — зонный dwell-сплит цел и ночью (h21 x1.24 в полосе, h22 x1.17 у нижнего края, направление интактно) → live-эксперимент gate-2 спланирован; R11 level-тест на свежем приложении ЧИСТ (all −2.0 всю ночь) — uptime-атрибуция ПОДТВЕРЖДЕНА; h21 FINAL 1.0; h22 первый замер 1.0; ночной переход на 91–99 к 22:33 ещё НЕ начался; SUNDAY night)

### Pipeline health — CLEAN

- Harvester PID 31497 alive (тики каждые ~10 мин, 500M к 22:33); app
  PID 2869 fresh (запуск 20:39:51, аптайм ~2 ч на конец окна); Metro
  8081 up (node 94557); sim booted. Данные до 22:33 (последний harvest
  до анализа).
- **Пре-регистрированное исключение ~21:35–21:45 соблюдено**: официальное
  R11-окно = 21:45→22:33; полное окно показало в 21:35-бакете лишь
  мягкий дип (all −5.0 / on_track −47) — след round-21 анализ-прохода
  виден, но слаб. **Рецидива stall-класса на свежем приложении НЕТ**:
  чистые бакеты 21:45–22:25 все all −2.0, records ~2.1–2.2 k/мин ровно.
- Мой анализ-проход этого раунда: 22:37:30–~22:41 (5 параллельных
  500M-сканов + группа 2 + 2 replay) — попадает в harvest 22:43+.
  **Round 23: исключить ~22:35–22:45 из чистых probe-чтений.**

### Window (analysis on data NEW since round 21)

`--since 2026-07-12 21:32` → 22:33 (61.7 min), 267 trams, 126.3 k
записей, **night-route (91–99) records 0%** — переход флота на ночные
маршруты к 22:33 ещё не начался (ожидаемо ~23:00+); h22 читается как
day-fleet. Cuts: h21 full 21:00–22:00 (131.2 k, 270 trams) для финала;
h22 22:00–22:33 (65.7 k, 247 trams) первый замер.

### (а) R8 ночная полоса — gate-1 ЗАКРЫТ (шестнадцатое воспроизведение; evidence день+вечер+ночь ПОЛНАЯ) + план live-эксперимента gate-2

- **h21 full**: censored per-dwell MID p50 centre/outskirts **102/82 s
  = x1.24** — в полосе x1.20–x1.27 (n=605/1068). Uncensored бюджеты:
  centre **[14.0, 33.8]** vs out **[9.2, 22.5]** s/stop (midpoints 23.9
  vs 15.9) — форма ~24 vs ~17 держится.
- **h22 (первый настоящий ночной час, 22:00–22:33, day-fleet)**: MID
  p50 **96/82 s = x1.17** — направление интактно, величина ЧУТЬ ниже
  полосы (n=275/482, Sunday-night, малое окно 33.7 мин, censoring floor
  прежний — fix cadence p50 45 s). Бюджеты centre **[11.8, 29.7]** vs
  out **[8.3, 20.8]** (midpoints 20.8 vs 14.6 = x1.42) — зональная
  форма цела, ночь в целом легче по обеим зонам.
- **Вердикт gate-1: EVIDENCE ПОЛНАЯ** — зонный сплит дожиданий
  (centre > outskirts) воспроизведён во всех трёх режимах дня (день:
  раунды 8–16; вечер: 17–21; ночь: 22) на n≈16 k+ dwell'ов суммарно;
  полоса x1.20–x1.27 с единственным band-edge чтением x1.17 (h22,
  каветка: воскресная ночь + пол-окна). Файлится как каветка, не как
  разрыв полосы.
- Fleet-level TOD_DWELL: h21 [10.6, 25.9], h22 [9.4, 23.5] — 18 s
  внутри обеих → **stays 1.0**. Centre dwell watch: MID p50 96–102 <
  115 — зональный entry по-прежнему не triggered.
- **ПЛАН LIVE-ЭКСПЕРИМЕНТА (gate-2), пре-регистрация**: replay не может
  гейтить зональный dwell (нет stop table) → второй гейт только live.
  1. Кандидат: зональный множитель DEFAULT_DWELL_S по CENTER bbox —
     centre **x1.30**, outskirts **x0.90** (из uncensored midpoints
     ~24/18 и ~16/18; флит-среднее ≈ нейтрально при наблюдаемом миксе
     стопов ~28%/72%); финальные значения пересчитать по полному
     day-агрегату перед стартом.
  2. Дизайн: per-tram parity A/B за dev-флагом — чётные бортовые =
     treatment, нечётные = control (обе руки в одном фиде/TOD);
     будний день, ≥2 ч, покрыть пиковый час; приложение свежее
     (restart перед стартом — uptime-класс!).
  3. Метрики успеха (пре-регистрированные): (i) paired
     treatment-vs-control centre at-fix signed p50 — сдвиг к 0;
     (ii) стационарная доля времени сима — к измеренной (centre
     15–35%, out 11–27%); (iii) frs p50/p90 и teleport-rate НЕ хуже
     control (>5% регресс frs p90 = откат).
  4. jest-покрытие композиции множителя (zone × TOD_DWELL × jitter)
     до включения флага; НЕ трогать braking envelope.

### (б) R11 ночной slope/level-тест — уровень на свежем приложении ЧИСТ, uptime-атрибуция ПОДТВЕРЖДЕНА; механизм воспроизведён 7-й раз

- **Entry condition опять НЕ сработал (шестое окно подряд)**: fleet
  true feed speed h21 **22.2** / h22 **21.9** km/h (< 23–24) —
  ночного разгона в day-fleet НЕТ (переход на 91–99 не начался);
  deep-recovery ΔdelayS<−10 доля **49.4%** (1617/3276, clean) /
  49.3% full — впервые чуть ниже константных 52%, но ~2.6 pt — не
  «существенно».
- **Level-тест (решающий, пре-регистрированный)**: чистое окно
  21:45→22:33 (7133 probes, аптайм 1–2 ч) — at-fix **all p50 −2.0**,
  КАЖДЫЙ чистый 5-мин бакет −2.0 (21:45…22:25); prev=on_track −37.0,
  prev=at_stop +1.0, centre −2 = outskirts −2 (зональной глубины нет).
  **Probe-уровни НЕ ушли глубже −20 → пересмотр uptime-атрибуции НЕ
  требуется**; ретро-каветка h15–h19 и рекомендация профилактического
  рестарта стоят. Единственный глубокий бакет — краевой 22:30 (partial,
  −11/−65, n=503, v_fix 23.6) — data-edge каветка, не тренд.
- **Механизм (7-е воспроизведение)**: OLS slope **+4.40** m/ΔdelayS-s
  clean (+4.39 full; полоса +4.07…+4.59), полосы монотонны (<−10 →
  −109, −3…+3 → +6.5, >+10 → +104), at_stop control −66 под recovery.
- Честная нота: steady ΣΔdelayS∈[−5,+5) **−25.0** (n=252) — глубже
  round-21 (−14.0) и ниже предсказанной полосы −10…−15;
  steady-by-level −15.0/−15.5 у края полосы. Малое n + рост fleet
  delayS в окне; следить, триггер пересмотра (all < −20) не задет.
- **Новый feed-observable**: fleet delayS p50 вырос 32→65 s за
  21:50→22:25 при probe ΔdelayS → 0 (recovery прекращается) —
  позднoночное расписание «отпускает» флот; вероятный предвестник
  ночного перехода. Watch на round 23.
- **R11 остаётся FILED с понижением** (per round-21): уровень здорового
  приложения −2…−3 уже у цели; остаток = designed on_track drag −37…−49.

### (в) TOD h21 FINAL + h22 первый замер — оба 1.0 (capless frame vs 0.62)

- **TOD_PACE h21 FINAL (full hour, 270 trams, 131.2 k, capless)**:
  (1) norm vs 0.62 **x0.984** FULL (120/145), halves x1.016/x0.984
  MIXED, parity x0.984/x0.984, 4-way x0.976–x1.024 стрэддлит; (2)
  paired h20→h21 (252 trams) fleet Δbias **+0.010** (135/100), Δspeed
  **+0.13** km/h — near-null; centre-majority −0.035/−1.95 (10/28) vs
  out +0.020/+0.30 — знакомый зональный сплит; (3) replay D50/D51 на
  full-hour extract оба flat (frs p50 124/122 vs NEW 124; signed 16/18
  vs 18). **TOD_PACE h21 = 1.0 FINAL** (Sunday, capless frame).
- **TOD_PACE h22 (первый замер, :00–:33, day-fleet)**: sample gate
  PASS (247 trams, 65.7 k, at-fix n=4848 ≥ 300). Evidence gate FAIL ×3:
  (1) norm vs 0.62 **x1.016** (121/109), halves x0.968/x0.984, 4-way
  x0.952–x1.016 стрэддлит; (2) paired h21→h22 (223 trams) fleet
  **+0.010/+0.37** (117/106) — слабый up, outskirts-led (+0.040/+1.28)
  vs centre (−0.055/−2.02); (3) replay D52/D53 оба flat-to-worse (frs
  p50 132/132 vs NEW 130). **h22 stays 1.0** — финал в round 23 на
  полном часе С СЕГМЕНТАЦИЕЙ по ночным линиям (91–99 появятся ~23:00+;
  night_profile --lines).
- True feed speed: fleet p50 **22.2** (h21) / **21.9** (h22); centre
  **19.1/19.0** — пол 18.8–19.1 уже **22+ часа**, upward release NOT
  yet; out 23.8/23.6. Ночной рамки (рост к 23–24+) в day-fleet нет.
- Replay gate re-validations: h21 extract OLD 121.5 → NEW 118.8
  (**−2.2%, двадцать третья**), frs p90 390.9 → 372.9; h22 extract OLD
  120.5 → NEW 116.1 (**−3.6%, двадцать четвёртая**), frs p90 393.8 →
  384.4.
- At-fix здоровье окна: signed p50 −2.0 / |e| p50 72 / %ahead 41.1
  (vs pre-R1 device baseline −38/86/27.1).

### (г) R5 копилка (closed; ВОСЕМНАДЦАТОЕ чтение) — capless-neutral ПОДТВЕРЖДЁН

- Round window vs 0.62 FULL **0.610 (x0.984, 116/146)**, halves
  x0.984/x1.016 MIXED, parity x0.984/x0.984; h21full 0.610 (x0.984),
  h22 0.630 (x1.016). Running log: … 20:39 → 0.590 (stall-contaminated),
  21:32 → 0.610 (mixed), **22:33 → 0.610 (capless, mixed)**.
- Четыре capless-чтения: 0.600 / 0.620 / 0.610 / 0.610–0.630 —
  стрэддлят prior с обеих сторон, одностороннего сдвига НЕТ по всем
  subsample. **Watch закрывается вердиктом: capless neutral ≈ prior
  0.62; re-estimate path отсутствует.** Логировать дальше только
  попутно (без отдельного пункта пре-регистрации).

### Applied (engine)

**Nothing — no engine constant passed its gate.** TOD_PACE h21 = 1.0
FINAL (capless frame), h22 stays 1.0, TOD_DWELL h21/h22 stays 1.0
(зональный entry не triggered). R8: gate-1 закрыт evidence'ом, БЕЗ
имплементации — следующий шаг только live gate-2 по плану выше. R11:
FILED (downgraded), level-тест чист — имплементация по-прежнему
заблокирована условием «глубокое плато на здоровом приложении».
Tooling: `replay.py` D52/D53 added (h22 symmetric controls, round-22
gate record).

jest 22 suites / 359 tests green; `npx tsc --noEmit` clean.

### ПРЕ-РЕГИСТРАЦИЯ РАУНДА 23 (глубокая ночь, ≥23:11; вероятно кросс через полночь)

1. **Ночной переход 91–99**: сегментировать h23 day-lines vs
   night-lines (night_profile --lines 91,...,99); первые ночные замеры
   TOD_PACE h23 в ОБОИХ сегментах; day-fleet «хасанят»-гипотеза
   (x1.1–1.25 ночью) наконец тестируема на настоящем ночном флоте.
2. **TOD h22 финал** (полный час 22:00–23:00, day-fleet) — стандартный
   двойной гейт, capless-рамка.
3. **R8**: наблюдательная фаза ЗАКРЫТА — ничего не мерить специально;
   live gate-2 по плану (будний день, НЕ ночью). Попутно: ночной dwell
   band у ночного флота (91–99) — новая популяция, отдельная полоса.
4. **R11**: следить delayS-slippage (32→65 s) и скорость ночного флота;
   если ночной сегмент даст fleet p50 ≥ 23–24 — механизменный
   slope-тест на быстром фиде наконец активен.
5. **Центр-коридор**: пол 18.8–19.1 (22+ ч) — зафиксировать час
   upward release, если случится ночью.
6. **Health**: исключить ~22:35–22:45 (этот анализ-проход); аптайм
   приложения к 03:00 ≈ 6 ч — профилактический рестарт ~03:00 при
   продолжении программы; рецидив стойла при аптайме < 6 ч = не
   uptime-класс.
7. **После полуночи**: сводки → `analysis-2026-07-13.md` (recap-шапка +
   перенос open items); harvester сам начнёт `sim-2026-07-13.jsonl`;
   кросс-полуночные окна подавать оба файла (night_profile/plateau_diag
   мульти-файл; dwell_v2/day_prior — конкатенацией).

### Open items after round 22

- Round-23 pre-registration above (ночной флот 91–99 + h22 финал +
  midnight rollover).
- **R8: gate-1 CLOSED (evidence день+вечер+ночь), live gate-2
  pre-registered** — будний день, parity A/B, centre x1.30 / out x0.90
  (пересчитать по day-агрегату), метрики выше. Каветка: h22 чтение
  x1.17 чуть ниже полосы (Sunday-night, пол-окна).
- **R11 PROPOSAL: FILED, DOWNGRADED** — level-тест на свежем приложении
  чист (all −2.0), uptime-атрибуция подтверждена; имплементация только
  после глубокого плато на здоровом приложении (weekday peak, fresh
  app). Steady −25 (глубже полосы −10…−15, малое n) — follow-up.
- **Uptime-stall class**: рецидива нет (аптайм ~2 ч); профилактика
  рестартом каждые ~6–8 ч стоит; h15–h19 probe-depth ретро-каветка
  стоит.
- **R5 копилка / capless-neutral watch: CLOSED** (нейтрально, 4 чтения).
- **NEW: late-night delayS slippage** (fleet p50 32→65 s к 22:25) —
  наблюдать связь с ночным переходом.
- Carried: %ahead-vs-device (needs device ride), upward centre release
  watch (18.8–19.1, 22+ ч), weekday confirmations (пики,
  15:00/07:00-boundary, DOW-каветка), 19:00/07:00 cap-boundary
  methodology caveat.

### RÉSUMÉ ВОСКРЕСНОЙ ПРОГРАММЫ — раунды 0–22, 2026-07-12 00:29 → 22:55

**Что измерено (методология + факты).**
- **TOD_PACE h0–h22: ВСЕ измерены = 1.0** (h5 — структурно усечённый
  ночной полу-бакет; h22 — первый замер, финал в round 23). Гипотеза
  формы «~0.8 пики / ~1.1–1.25 ночь» из plan.md на воскресенье
  ОПРОВЕРГНУТА для day-fleet: конвергированный paceBias поглощает
  дневную динамику, остаточного TOD-сигнала нет ни в одном часе (три
  проверки на час: norm vs prior × subsamples, paired within-tram,
  replay-гейт с wrong-way контролем; ≥300 at-fix samples на час).
- **TOD_DWELL h0–h22: все 1.0** — engine default 18 s внутри
  uncensored бюджета [≈9–26] s/stop в каждом часе.
- **R5 (prior 0.62): CLOSED навсегда** — 18 чтений, оценщик 0.60–0.67
  стрэддлит prior; capless-рамка (после 19:00) отдельно подтверждена
  нейтральной (4 чтения).
- **R8 (зонный dwell-сплит): gate-1 CLOSED** — centre/outskirts MID
  p50 x1.20–x1.27 (16 воспроизведений, день+вечер+ночь; h22 x1.17
  band-edge каветка); бюджеты ~24 vs ~17 s/stop стабильны весь день.
- **R11 (behind-плато, schedule-pace projection drag): механизм
  воспроизведён 7×** (OLS +4.07…+4.59 m/ΔdelayS-s, монотонные полосы,
  at_stop-контроль) — это ДИЗАЙН (0.75-блендовая проекция + TRAIL_M),
  не баг; сама глубина плато h15–h19 (−22…−26) ретро-атрибутирована
  subclinical-стадии uptime-stall (release −2.0 мгновенно на рестарте
  20:39:51 при плоском фиде; ночной level-тест round 22 подтвердил).
- **Структурный центр-коридор**: true feed centre p50 18.8–19.1 km/h
  22+ часа подряд (не TOD — постоянный зональный факт); outskirts
  ~21.5–24.1. Fix cadence p50 45–46 s (censoring floor всех
  dwell-оценок). Зональный paired-сплит (centre замедляется к вечеру,
  out ускоряется) — постоянный, bias-нейтральный.
- **DOW-каветка**: ВСЯ программа — воскресенье; пики h7–h9/h15–h18
  требуют будничного подтверждения.

**Что отгружено (движок; всё с replay-гейтом + jest + tsc).**
- **R1** — PACE_BIAS_PRIOR 0.62 + bias-наследование через телепорты /
  trip-change (paceBiasMemory TTL 15 мин); телепорт-фиксы исключены из
  калибровки. **R2** — terminal un-latch (150 m). **R3** —
  V_CRUISE_REF_MS 11.7 (42 km/h) как cruise-референс (caps остались
  caps). Совокупный replay-гейт OLD→NEW: median |fresh-fix err| −2…−5%
  на КАЖДОМ из 24 последовательных окон дня, frs p90 −2…−9%.
- **TOD-хук** (todPaceFactor/todDwellFactor, hour-blended) + фикс
  композиции updatePaceBias×TOD — отгружены нейтральными; таблицы
  заполняются только через двойной гейт (пока весь день = 1.0).
- **Ops/tooling**: harvest.sh container-drain fix (R9 data-loss
  закрыт), v2-схема логов (obsAt/statePos/delayS/nextSeq → real dwells
  + true feed speed), скрипты day_prior/tod_paired/dwell_v2/
  plateau_diag/night_profile/replay (D-варианты D20–D53 = полный
  gate-record дня).
- **Из бэклога приложения** (вне калибровки, тем же днём): TramFeed
  boundary, fleet browser, follow-chip, T3-consists, live-cam fix.

**Что закрыто.** R5 (prior) + capless-watch; R8 gate-1; R9
(data-loss); R10 (16:00-boundary — фальсифицирована); 19:00-boundary
h19-драп (cap-граница, не TOD); 15:00-transient (sim-side, feed чист);
uptime-stall атрибуция плато (3 варианта стойл задокументированы,
профилактика — рестарт каждые ~6–8 ч); h0–h21 TOD-финалы.

**Что ждёт понедельника (и дальше).**
1. **R8 live gate-2** — parity A/B зонального dwell (план в этом
   раунде): будний день, свежее приложение, ≥2 ч с пиком.
2. **Будничные подтверждения TOD-финалов пиков** (h7–h9, h15–h18) +
   15:00/07:00-boundary + DOW-каветка в целом.
3. **R11 decisive** — глубокое плато на здоровом приложении (weekday
   peak, рестарт перед h15); иначе proposal остаётся filed.
4. **Device ride** (%ahead-vs-device) — нужна реальная запись.
5. **Ночной флот 91–99** — сегодня round 23 (первая настоящая ночная
   популяция; «хасанят» — тест).
6. **Профилактический рестарт приложения** каждые ~6–8 ч как ops-норма
   до фикса app-side деградации (R12-кандидат в бэклог приложения).

## Round 23 (23:11 → closed by orchestrator, reduced scope)

The round agent stalled twice on whole-file passes over the now-542 MB Sunday session —
scope reduced honestly per runbook. Verified streaming-cheap essentials:
- **Pipeline healthy**: harvester alive; midnight rollover ALREADY handled automatically
  (sim-2026-07-13.jsonl created and growing); app PID 2869 (uptime ~2.5 h, fine).
- **Night transition underway** at ~23:49 (day lines 22/12/17 still out, night 92 appearing).
- **Late-night watch confirmed in sample**: end-of-service churn visible — terminal trams
  with delayS up to 380 s and large devM on winding-down runs (matches the 22:25 slippage
  watch; not a sim defect class — service-end reality).
- **Carried over to Monday**: TOD h22/h23 finals; steady −25 follow-up; R11 entry.
- **Tooling rule added**: NEVER run whole-file passes over closed session files — all
  scripts must stream with --since (the two agent stalls were exactly this).
